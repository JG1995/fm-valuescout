# Release Procedure Moved

The current release procedure is the repository-local [`create-release` skill](../../.pi/skills/create-release/SKILL.md).

This redirect remains only for links from completed historical records. It is not a runbook and does not define the current release contract.
