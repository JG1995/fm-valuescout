# Tactic Columns for Player Tables (Search, Moneyball, Shortlist)

## Status

Active

**Ledger schema:** 2

## Delivery authorization

**Delivery fingerprint:** a5490b97e74a67126ba8241bf73c8b5d0cb778fcfe98d7d9a9f0c5e1c739f95a

## Intent

Add tactic-aware player evaluation to the three player tables (`search`, `moneyball-search`, `shortlist`) by exposing the active save's Planner tactic as optional display-only columns. Each column represents one of the 11 Planner lanes, showing a blended IP+OOP adjusted score derived from the lane's roles, weight, fit, and foot rules. Users can append a "Current" group (11 columns), a "Potential" group (11 columns), or both interleaved, ordered by `TACTIC_POSITION_ORDER`. This closes the loop between tactic design (Planner) and player search: after configuring a tactic, the user can immediately rank any player pool by how well each player fits each lane in the current or potential basis, without leaving Search.

## User-visible behavior

- In every Search view (General, Moneyball, Shortlist) the filter strip area shows two toggle buttons next to `Edit Filters`: `Add Tactic (Current)` and `Add Tactic (Potential)`.
  - Inactive state: secondary/ghost button, not highlighted.
  - Active state: highlighted (primary/filled or `primary-container` per design system) when the table layout contains all 11 lane IDs of that group.
- Clicking an inactive button:
  - Resolves the immutable save context `{ saveId, contextToken }` from `savesQuery` (`SaveSummary` at `src/features/snapshot/types/save.ts:1` via `find(isActive)`, analogue `ClubDnaContext` at `src/features/club-dna/types/club-dna.ts:1` and `activeClubDnaContext` at `src/app/routes/my-club.tsx:377`). If no active save context, show inline error `"No active save — configure a save before adding tactic columns"` and add no columns.
  - Fetches the tactic + options via immutable-context-scoped queries `plannerTacticQueryOptions(context)` (`plannerKeys.tactic({ saveId, contextToken })` analogue `clubDnaKeys.definition` at `src/features/club-dna/api/club-dna-keys.ts:5`) and `plannerTacticOptionsQueryOptions(context)` (gated on `context != null && isMatched && !isFetching && !isError`, reset on save/snapshot change). Queries carry exact camelCase `{ saveId, contextToken }` to `get_planner_tactic`/`get_planner_tactic_options`/`save_planner_tactic` which validate via snapshot-owned `pub(crate) ensure_active_save_context(&Transaction, save_id, context_token)` at `src-tauri/src/features/snapshot/service.rs` (snapshot owns `saves` table; analogue `src-tauri/src/features/club_dna/service.rs:139`) — first `SELECT EXISTS(SELECT 1 FROM saves WHERE id=?1)` distinguishes unknown numeric save (`Save {id} not found`), then `SELECT EXISTS(... WHERE id=?1 AND context_token=?2 AND is_active=1)` distinguishes existing-but-stale/inactive token (`Save changed or is no longer active`) inside exactly one `unchecked_transaction` held under the command's `Db` `Mutex`; read commands call `get_tactic_in_tx(&Transaction, save_id)` (default initialization via `save_tactic_in_tx` in the same `tx`), mutation `save_planner_tactic` calls `ensure_active_save_context` then `save_tactic_in_tx(&Transaction, save_id, &tactic)` in the same `tx`, options validates via `ensure_active_save_context` in one read `tx` then returns static `get_tactic_options()`; no nested transactions, no duplicate command-local validator. Trusted internal `get_tactic(&Connection, save_id)` and `save_tactic(&Connection, save_id, &tactic)` signatures are preserved for Search/depth/tests callers and delegate to the `*_in_tx` helpers by opening one `tx`. Numeric ID reuse isolated via immutable `{ saveId, contextToken }` (`{1,"tokenA"}` vs `{1,"tokenB"}` distinct, `contextA-recreated` also distinct). Frontend fetching is component-owned non-throwing: initial `isError` (no cached `data`) renders actionable `Retry` (`Could not load tactic` + `Retry` via `queryClient.refetchQueries`), cached `isRefetchError` retains `data` read-only; stale/inactive/unknown mutation `save_planner_tactic({ saveId, contextToken, tactic })` is rejected in the same `tx` with the same two distinct errors (`Save {id} not found` / `Save changed or is no longer active`) and surfaced inline. If either query is pending/error (initial without cached data) or `validateTacticDraft` returns a non-null error string, show an inline error in the panel (or toast via existing app feedback) — do not add columns. Labels use save tactic role selection (`PlannerTactic.lanes` at `src/features/planner/types/tactic.ts`) + shared `TacticOptions` catalog `displayName`.
  - Otherwise clears any partial/stale columns of that group from `columnIds` for that `tableId`, then appends the full 11 IDs for that group to the far-right of `columnIds` in `TACTIC_POSITION_ORDER` order (IP position primary, OOP tie-break via `compareTacticPositions`) via the store's atomic layout-replace action.
  - Marks the button active. Persistence is via `usePlayerTableStore` per `tableId`: `search`, `moneyball-search`, `shortlist`.
- Clicking a fully active button removes all 11 IDs of that group from `columnIds` via the same atomic store action and marks that button inactive. Removing a group when both were active re-compacts the remaining group to straight tactic order (no interleaving gaps). Interleaving is only applied when both groups are active: iteration over lanes in `orderedTacticLanes` (i.e. `TACTIC_POSITION_ORDER`) emits `tactic_current.<laneId>` then `tactic_potential.<laneId>` per lane at the far-right edge, after any non-tactic columns. When only one group is active, that group's 11 IDs appear straight in tactic order at the far-right.
- Removing any single tactic column via the header `X` (existing `onRemoveColumn` in `PlayerTableHeader`) immediately deactivates its group's button, because the group is no longer full (active iff all 11 IDs of that group are present in `columnIds` for that table), and re-compacts the surviving tactic block to deterministic straight order contiguous at the far-right (no interleaving gaps). The header-X path and the toggle paths share the same atomic store action `replaceLayout(tableId, nextColumnIds)` which replaces `columnIds` and prunes `widths` in one validated write.
- Column identity:
  - ID pattern: `tactic_current.<laneId>` and `tactic_potential.<laneId>` (e.g. `tactic_current.goalkeeper`, `tactic_potential.right_winger`). Lane IDs are the canonical `TACTIC_LANE_IDS` defined in `src/utils/tactic-ids.ts` (neutral shared owner, single source) and re-exported by `src/features/planner/types/tactic.ts` via `import { TACTIC_LANE_IDS } from "@/utils/tactic-ids"`; both are asserted equal to `src-tauri/src/features/planner/tactic.rs` `DEFAULT_LANE_IDS`).
  - Never appears in filter list, metric picker (`PLAYER_METRICS` / `MONEYBALL_SEARCH_METRICS`), or `FilterAst` validation. They are synthetic display-only columns owned by the neutral `src/utils/tactic-ids.ts` (IDs, prefix constants, lane allowlist, `isTacticColumnId`/`isValidTacticColumnId`) and `src/features/search/utils/tactic-columns.ts` (view-model helpers that import only neutral shared modules and `src/utils/position-order.ts`, never `@/features/planner`).
- Column header label format: `"{IP Position} ({IP Role DisplayName}) / {OOP Position} ({OOP Role DisplayName})"` — e.g. `"MC (Deep-Lying Playmaker) / DM (Screening Defensive Midfielder)"`. The label view-model is produced in `src/app/routes/search.tsx` (which owns Planner imports: `validateTacticDraft`, `plannerTacticQueryOptions`, `TacticOptions` role `displayName` lookup) and passed as `laneLabels: Map<laneId, string>` into Search components. Search helpers never import Planner internals; shared/stores never import features (enforced by `biome.json` `noRestrictedImports`).
- Column rendering: right-aligned, default width 112, `ScoreBadge` for numeric score, `"—"` (em dash in `on-surface-variant`) for unavailable. Unavailable when: combined score missing, or eligibility fails (see below), or Moneyball mapping has no counterpart for this lane/position (see Moneyball contract).
- Column sorting: clicking the header sorts by combined numeric score. Nulls (`"—"`) are last regardless of direction, consistent with `Club DNA` and `Moneyball role` null-last ordering in `src-tauri/src/features/search/query.rs`. Direction toggles asc/desc with `players.uid ASC` tie-breaker. Sort field acceptance is route-owned and precedes UI exposure: `isVisibleSortField` in `src/features/search/utils/dynamic-columns.ts` plus `validateSearch` in `src/app/routes/search.tsx` accept a canonical tactic sort ID only when that exact ID is present in the current view's persisted `layouts[tableId].columnIds` (synthetic allowlist, not `getFilterField`). Invalid, removed, or wrong-table tactic sorts are rejected and fallback to `defaultSearchSort(view)`. This is validated against tour: header sort, direct URL restoration with valid tactic ID present, URL with removed/invalid/wrong-table ID falls back, and header-X removal navigates away from a removed tactic sort.
- No-snapshot composition: when active immutable save context `{ saveId, contextToken }` is present but `snapshot` is `null` or `snapshot.saveId !== context.saveId` (snapshot null/mismatch), the route keeps tabs + `SearchFilterBar` (filter strip) mounted above the results panel; only `SearchResultsPanel` shows `EmptyState`/`Loading`. `TacticContextBoundary` (and its child tactic queries) is not mounted when there is no matched snapshot (`context == null` or `!isMatchedSnapshot` where `isMatchedSnapshot = snapshot != null && snapshot.saveId === context.saveId`), so zero `get_planner_tactic`/`get_planner_tactic_options`/`save_planner_tactic` IPC fires; `isMatchedSnapshot` gates the boundary mount. `TacticColumnToggles` remain mounted (from commit 7) but operable via gated handler: click when no matched snapshot shows `"No snapshot loaded — use Load Data"` and adds no columns, without mutating layout and without firing IPC. Save switching changes immutable context `{ saveId, contextToken }` -> `plannerKeys.tactic(context)` cache miss (numeric ID reuse isolated via `contextToken`), fetches for new context; `laneLabels` update for new context while table-scoped `columnIds`/`widths` are preserved across save switches because layout is per `tableId` not per save (commit 7 asserts). Non-throwing fetch preserves `isRefetchError` -> read-only cached tactic during refresh.
- Eligibility & scoring (mirrors `src-tauri/src/features/planner/fit.rs` `lane_fit_score` plus `combine_role_scores`, fit adjustments):
  - Require base familiarity `>=12` on both IP and OOP base positions (`base_position(placement)` normalizes `DCR/DC/DCL->DC`, `DMCR/DM/DMCL->DM`, `MCR/MC/MCL->MC`, `AMCR/AMC/AMCL->AMC`, `STCR/ST/STC/STCL->ST`) else `"—"`.
  - Strict foot mismatch -> `"—"` when `foot_matches` is false and `footPreference === "strict"`.
  - Otherwise blended score = `combine_role_scores(ipRoleScore, oopRoleScore, lane.ipWeight)` then penalties: soft foot mismatch `-5` (when `preferredFoot != any` and `footPreference === "preferred"` but foot does not match), familiarity `<16` => `-5` per phase (up to `-10`), total max `-15`, `saturating_sub` at 0.
  - Source scores:
    - For Search (General) and Shortlist (General metrics): Current group reads `role.<ipRoleId>` / `role.<oopRoleId>` from `player_role_metrics` compact current columns; Potential group reads `potential_role.<ipRoleId>` / `potential_role.<oopRoleId>` with fallback `age >= 29 -> current` (same as `features/player` `get_player` potential contract). Shortlist uses General attribute-based scores.
    - For Moneyball view: same lane roles/weights but reads `moneyball_role.<roleId>` derived scores via the Moneyball cohort path (`score_role`). If a tactic role ID has no matching Moneyball catalog entry for this lane's normalized position tag, score is `None` -> `"—"`. Mapping is deterministic (see Moneyball contract below), not a doc gap.
- Persistence: `usePlayerTableStore` layouts per `tableId`. Synthetic IDs are validated via the neutral allowlist (`isValidTacticColumnId` from `src/utils/tactic-ids.ts`) and excluded from `getPlayerMetric`/`getMoneyballSearchMetric` allowlists. Widths persisted, clamped `72..360`. New synthetic columns participate in `sanitizeLayout` retention and reorder/move logic. The store exposes one atomic action `replaceLayout(tableId, nextColumnIds)` (or `setLayout`) that validates the incoming ID list against the neutral allowlist + metric allowlists, deduplicates, replaces `columnIds` wholesale, prunes `widths` to retained IDs in a single `set`, and falls back to `defaultLayout(table).columnIds` when the result would be empty (tactic-only toggle-off) — used by toggles and by tactic-aware header-X removal to avoid interleaved partials.
- Table targets: `search` (General), `moneyball-search`, `shortlist`. Squad (`squad`) is out of scope.
- Error paths: no active save context (`{ saveId, contextToken }` null) -> `"No active save — configure a save before adding tactic columns"`; active immutable save context present + snapshot null/mismatch (`snapshot == null` or `snapshot.saveId !== context.saveId`) -> `TacticContextBoundary` not mounted, zero `get_planner_tactic`/`get_planner_tactic_options`/`save_planner_tactic` IPC, filter strip + tabs remain present, gated toggle click shows `"No snapshot loaded — use Load Data"` without mutating layout; no tactic with `isError` without cached data -> `Could not load tactic` + actionable `Retry`, `isRefetchError` with cached data -> read-only, or invalid tactic (`validateTacticDraft` error) -> button click shows error via gated handler (button remains operable, not `disabled`) and adds no columns; no `try/catch`/`Promise.all(...).catch(()=>null)` swallowing. Switching saves changes immutable context `{ saveId, contextToken }` -> `plannerKeys.tactic(context)` distinct (numeric ID reuse isolated) and refetches via `TacticContextBoundary` mount with `isMatchedSnapshot`; initial `isError` -> `Retry`, cached `isRefetchError` retains `data` read-only. An empty snapshot's zero-row cohort is handled by existing empty-state in `SearchResultsPanel`; toggles remain rendered (operable with gated handler when active save context present + snapshot null/mismatch) and tactic columns produce 0 rows when activated after snapshot loads.

## Invariants

- Tactic columns are display-only: they never enter `FilterAst`, never become a `MetricField` for filters, never appear in the categorized metric picker, and never participate in `getPlayerMetric`/`getMoneyballSearchMetric` validation for filters.
- Column group membership is derived from layout: a group (`current` or `potential`) is active iff all 11 canonical lane IDs of that prefix are present in `columnIds` for that `tableId`. No separate persisted boolean.
- Store truth: `PlayerTableId` remains the persisted owner; no new persisted table ID. Synthetic IDs live inside the existing `layouts[<tableId>].columnIds`.
- Module-boundary invariant (enforced by `biome.json` `noRestrictedImports`): `src/utils/**` and `src/stores/**` never import `@/features/**` or `@/app/**`; `src/features/**` never imports `@/features/*/**` or `@/app/**`. The canonical 11 lane IDs and synthetic-ID predicates live in the neutral shared module `src/utils/tactic-ids.ts` (or equivalently `src/utils/tactic-positions.ts`). `src/features/search/utils/tactic-columns.ts` imports only that neutral module and `src/utils/position-order.ts`. All Planner imports (`validateTacticDraft`, `plannerTacticQueryOptions`, `TacticOptions`/`PlannerTactic` types) live in `src/app/routes/search.tsx` which composes Search components by passing a normalized view model (`orderedLanes`, `laneLabels`, `laneRoles`, `onToggle` callbacks). Stores import only the neutral module.
- Lane ID truth is single-source: `src/utils/tactic-ids.ts` defines the 11 canonical lane IDs; `src/features/planner/types/tactic.ts` imports and re-exports them (no mirroring, no duplication); both are asserted equal to `src-tauri/src/features/planner/tactic.rs` `DEFAULT_LANE_IDS` (length+value) at test time.
- Immutable save context truth: every Planner tactic read (`get_planner_tactic`, `get_planner_tactic_options`) and mutation (`save_planner_tactic`) requires immutable context `{ saveId, contextToken }` (exact camelCase, required/validated, reuse analogue `ClubDnaContext` at `src/features/club-dna/types/club-dna.ts:1`); frontend carries it via `SaveSummary` at `src/features/snapshot/types/save.ts:1` (`{ id, contextToken }`), fetchers `fetchPlannerTactic({ saveId, contextToken })` / `fetchPlannerTacticOptions({ saveId, contextToken })` / `savePlannerTactic({ saveId, contextToken, tactic })` at `src/features/planner/api/fetch-planner-tactic.ts` / `fetch-planner-tactic-options.ts` / `save-planner-tactic.ts`, keys `plannerKeys.tactic({ saveId, contextToken })` / `tacticOptions({ saveId, contextToken })` analogue `clubDnaKeys.definition` at `src/features/club-dna/api/club-dna-keys.ts:5`, and `plannerTacticQueryOptions(context)` / `plannerTacticOptionsQueryOptions(context)` with component-owned `enabled: context != null && isMatched` (non-throwing: initial `isError` -> actionable `Retry`, cached `isRefetchError` retains `data` read-only) plus `useMutation` for `savePlannerTactic(context)`. Rust validates `{ saveId, contextToken }` via snapshot-owned `pub(crate) ensure_active_save_context(&Transaction, save_id, context_token)` at `src-tauri/src/features/snapshot/service.rs` (snapshot boundary; analogue `src-tauri/src/features/club_dna/service.rs:139`) with two-step distinct errors `Save {id} not found` (unknown numeric save) vs `Save changed or is no longer active` (existing-but-stale/inactive token) inside exactly one `unchecked_transaction` under the command-held `Db` lock; `get_tactic_in_tx`/`save_tactic_in_tx` at `src-tauri/src/features/planner/tactic.rs` are `pub(crate)` transaction-scoped helpers (default initialization via `save_tactic_in_tx` in same `tx`), trusted `get_tactic(&Connection, save_id)` / `save_tactic(&Connection, save_id, &tactic)` are preserved for Search/depth/tests callers and delegate by opening one `tx` to the helpers — no nested transactions, no duplicate command-local validator; mutation `save_planner_tactic` uses `ensure_active_save_context` + `save_tactic_in_tx` in the same `tx` and rejects stale/inactive/unknown with the same distinct errors (tested). Numeric ID reuse is isolated because query/mutation keys include `contextToken` (`{1,"tokenA"}` vs `{1,"tokenB"}` and `contextA-recreated` distinct, `contextKey({ saveId, contextToken })` analogue at `src/features/club-dna/components/club-dna-definition.tsx:67`). Editor cache writes use `queryClient.setQueryData(plannerKeys.tactic(context), nextTactic)` with same context; `src/testing/setup.ts` global mock and `e2e/tauri-ipc-stub.ts` stub validate exact camelCase for reads and mutation and are context-keyed with immutable context-keyed delayed A/B test-local `Deferred` per context (no global `pendingPlannerTacticDeferred` / `setPlannerTacticPending`). Exact consumers enumerated after source inspection: Rust `get_planner_tactic`, `get_planner_tactic_options`, `save_planner_tactic` at `src-tauri/src/features/planner/commands.rs`; frontend `fetchPlannerTactic`/`fetchPlannerTacticOptions`/`savePlannerTactic`, `plannerKeys.tactic`/`tacticOptions`, `plannerTacticQueryOptions`/`plannerTacticOptionsQueryOptions`, `PlannerTacticEditor` `save` mutation (`savePlannerTactic({ saveId, contextToken, tactic })` + `setQueryData`/`invalidateQueries`), `my-club.tsx` callers (`ensureQueryData`/`getQueryData`/`setQueryData`), `search.tsx` read callers, `src/testing/setup.ts` mock, `src/testing/planner-ipc-mock.ts` context-keyed store + `resolveSavePlannerTacticIpcMock`, `e2e/tauri-ipc-stub.ts` stub.
- Score truth: computation is single-source. For General/Shortlist the Rust resolver owns sort ordering and numeric value; the frontend renders `dynamicValues[fieldId]` as a number or null. For Moneyball the same resolver owns the cohort before scoring. The frontend never recomputes scores for sort.
- Moneyball mapping is deterministic, not a gap: select the Moneyball definition by `(attribute_role_id == lane.ipRoleId / oopRoleId) AND (normalized tactic placement tag ∈ definition.position_tags)` where normalization is `tactic.rs::base_position` on the lane's `ipPosition`/`oopPosition`. The catalog `src-tauri/src/features/moneyball/builtin_role_definitions_v1.json` has 88 Moneyball defs (77 map via `attribute_role_id` to a General role, 11 are Moneyball-only with `attribute_role_id: null`), and all 68 General `role_id`s are represented. Matching by that compound key is unique and covers 103/111 General `(role_id, base_position)` combos. The 8 uncovered combos are unavailable by design (return `None` -> `"—"`): `holding_wing_back_oop`+`DL`, `holding_wing_back_oop`+`DR`, `pressing_wing_back_oop`+`DL`, `pressing_wing_back_oop`+`DR`, `box_to_box_midfielder_ip`+`MC`, `box_to_box_playmaker_ip`+`MC`, `deep_lying_playmaker_ip`+`MC`, `second_striker_ip`+`ST`. Tests must cover one mapped numeric and one uncovered `NULL` case and assert the mapping contract.
- Null handling is invariant: unavailable scores render `"—"` with `ScoreBadge` omitted, are last in sort, and use `UID ASC` tie-breaker.
- Familiarity and foot rules are invariant with `fit.rs` / `combine.rs`: correct base-position normalization, thresholds, and penalty values. Age fallback `>=29` uses current as potential.
- Interleaving invariant: with a single active group, order equals `orderedTacticLanes` in tactic-position order; with both active, order is deterministic interleaved `cur,pot` per lane still respecting tactic-position order. Removing a group or a single header-X when both were active re-compacts the surviving tactic block to contiguous straight order at the far-right via the single atomic store action (no interleaving gaps). Deterministic post-X ordering: `nonTacticIds` in persisted order + `straightTacticIds(survivingGroup, orderedTacticLanes)` in tactic-position order.
- Sort-acceptance invariant: a tactic `sortBy` is visible only when the exact field ID is present in the persisted layout for the current view's `tableId`; otherwise `validateSearch` and `isVisibleSortField` reject it and the route falls back to `defaultSearchSort(view)` (or the panel's `removeColumn` sort fallback when header X removes the active sort).
- No snapshot history for tactic columns: sorting reads the effective current snapshot's compact metric row; historical snapshots are not tactic-scored.

## Non-goals

- No filter, sort-only-outside-SQL, or picker support for tactic columns.
- No new metrics in `player-metrics.ts` / `moneyball-search-metrics.ts` catalogs for tactic IDs; no addition to `FilterAst` or `player_metrics::resolver` allowlists for filters.
- No Squad player table (`squad`) tactic columns, no Planner depth matrix changes, no CSV import changes, no Club DNA interaction.
- No new migration, no schema change, no new IPC for saving layouts, no revision of `TACTIC_POSITION_ORDER`.
- No IndexedDB/SQLite layout migration beyond `sanitizeLayout` tolerance; stale tactic IDs are cleared as incomplete groups on next toggle via the atomic action, but legacy layouts remain loadable.
- No client-side full recompute of scores for sort; Moneyball tactic mapping uses the deterministic compound key above, not a new joint catalog, and uncovered combos intentionally remain `NULL`.
- No BepInEx, FM write, or bridge changes.

## Current-state map

- Relevant components:
  - `biome.json` — enforces `noRestrictedImports`: `src/features/**` forbids `@/app` and `@/features/*/**`; `src/utils/**` and `src/stores/**` forbid `@/app` and `@/features/**`. Prior plan violated this by having Search helpers import Planner internals and `use-player-table-store` import a Search helper. The corrected plan places canonical IDs/predicates in neutral `src/utils/tactic-ids.ts` and keeps Planner query/validation in `src/app/routes/search.tsx`.
  - `src/app/routes/search.tsx` — route owns `view` (`general`/`moneyball`/`shortlist`), `comparisonPool`, filters/combine, sort/dir, and `SearchFilterBar` + `SearchResultsPanel` composition; tab switch clears filters and resets sort. `validateSearch` uses `isVisibleSortField` which currently gates non-basic sorts on `getFilterField` success — therefore excludes synthetic tactic IDs, so tactic sort cannot pass today. The route also early-returns `<Panel>No data loaded…</Panel>` when `!snapshot` before rendering the filter strip, contradicting the promise that tactic toggles remain with zero rows.
  - `src/features/search/components/search-results-panel.tsx` — `SearchResultsPanel` owns `usePlayerTableStore(tableId)` layout (`search`/`moneyball-search`/`shortlist`), derives `columns` via `tableColumnForMetric` + `metrics` prop override, computes `requestedFields` from non-basic columns, runs `searchPlayersQueryOptions` committed/requested sort-replacement cache, renders `VirtualizedPlayerTable` + `PlayerTableHeader` + `ScoreBadge`/`"—"` cells; `tableColumnForMetric` dispatches on `MONEYBALL_SEARCH_METRICS` vs `PLAYER_METRICS`. `removeColumn` guards last column, handles sort fallback to `ca` or `moneyball.average_rating`.
  - `src/features/search/components/search-filter-bar.tsx` + `src/utils/player-metrics.ts` + `src/utils/moneyball-search-metrics.ts` — filter editor and categorized metric picker; every metric has `sortable` flag used by `getPlayerMetric` filter.
  - `src/stores/use-player-table-store.ts` — `PlayerTableId = search | moneyball-search | shortlist | squad | staff-search | my-staff | staff-shortlist`, `PlayerTableLayout { columnIds, widths }`, `PLAYER_TABLE_LAYOUT_VERSION=6`, `sanitizeLayout` filters by `getPlayerMetric(...).sortable` / `getMoneyballSearchMetric(...).sortable` plus identity columns, `migratePersistedState` handles version shifts, `addColumns`/`removeColumn`/`moveColumn`/`setColumnWidth` are the only mutators (no atomic `replaceLayout` yet, so toggle and header-X paths would be two writes risking interleaved partials); `merge` re-sanitizes persisted state.
  - `src/utils/position-order.ts` — `TACTIC_POSITION_ORDER` (GK, DR, DCR, DC, DCL, DL, WBR, DMCR, DM, DMCL, WBL, MR, MCR, MC, MCL, ML, AMR, AMCR, AMC, AMCL, AML, STCR, STC, STCL) plus `compareTacticPositions`, `orderedTacticPositions`.
  - `src/utils/tactic-ids.ts` (new neutral owner after commit 2) — `TACTIC_LANE_IDS` (11) canonical definition; `src/features/planner/types/tactic.ts` — `TACTIC_LANE_IDS` re-exported via `import { TACTIC_LANE_IDS } from "@/utils/tactic-ids"`, plus `TacticLane { laneId, ipWeight, importanceRank, preferredFoot, footPreference, ipPosition, ipRoleId, oopPosition, oopRoleId }`, `PlannerTactic`, `TacticOptions { placements, roles: TacticRoleOption[] }`.
  - `src/features/planner/utils/tactic-editor.ts` — `orderedTacticLanes` (sort by IP then OOP via `compareTacticPositions`), `validateTacticDraft(tactic, options): string | null`, `phasePosition`, `basePosition`, `roleLabel`, `linkedPositionDescription` pattern for header text.
  - `src/features/planner/api/planner-keys.ts` + `src-tauri/src/features/planner/tactic.rs` + `commands.rs` — `plannerKeys.tactic({ saveId, contextToken })` (`get_planner_tactic` with `{ saveId, contextToken }`) , `plannerKeys.tacticOptions({ saveId, contextToken })` (`get_planner_tactic_options`) and `save_planner_tactic` (`save_planner_tactic` with `{ saveId, contextToken, tactic }`), save-scoped `planner_tactic_lanes` table seeded with `default_tactic()` 11 linked lanes; `base_position` normalization and validation in Rust mirrors TS `basePosition`; Rust will validate immutable context `{ saveId, contextToken }` via snapshot-owned `pub(crate) ensure_active_save_context(&Transaction, save_id, context_token)` at `src-tauri/src/features/snapshot/service.rs` (snapshot boundary; analogue `src-tauri/src/features/club_dna/service.rs:139`) with two-step distinct `Save {id} not found` vs `Save changed or is no longer active` under exactly one `unchecked_transaction` held under the command's `Db` lock; `src-tauri/src/features/planner/tactic.rs` preserves trusted `get_tactic(&Connection, save_id)` / `save_tactic(&Connection, save_id, &tactic)` for Search/depth/tests and adds `pub(crate) get_tactic_in_tx(&Transaction, save_id)` (including default initialization via `save_tactic_in_tx`) and `pub(crate) save_tactic_in_tx(&Transaction, save_id, &tactic)` used by context-bound commands; wrappers open one `tx` and delegate to the helpers so no nested transactions and no duplicate validator; numeric ID reuse isolated via `contextToken`-keyed query/mutation keys (analogue `ClubDnaContext` at `src/features/club-dna/types/club-dna.ts:1` and `clubDnaKeys.definition` at `src/features/club-dna/api/club-dna-keys.ts:5`). Current `plannerTacticQueryOptions` in `src/features/planner/api/planner-tactic-query-options.ts` is still save-independent (`queryKey: plannerKeys.tactic()` with no context, `plannerKeys.tactic(activeSaveId ?? -1)` sentinel) while `get_planner_tactic`/`save_planner_tactic` in `src-tauri/src/features/planner/commands.rs` resolve `service::active_save_id(&conn)` implicitly and `ensure_save_exists` only checks `id` existence (no `contextToken`/`is_active` distinguish, no nesting-free ordering, mutation not context-bound) — therefore stale across save switches, not gated, not context-validated, mutation not rejected on stale token, and numeric ID reuse not isolated. Other planner queries (`plannerKeys.slotCandidates(activeSaveId, ...)`, `plannerKeys.roleReference(activeSaveId,...)`) are correctly save-scoped but not context-token validated (analogue for tactic is `ClubDnaContext` pattern).
  - `src-tauri/src/features/player_metrics/resolver.rs` — `MetricField`, `MetricSource`, `MetricValueKind`, `MAX_REQUESTED_FIELDS=256`, closed-catalog `player_current_column`/`player_potential_column` via `compact.rs`, `sql_expression` + `sql_sort_expression_with_club_dna`, `DynamicValue`, `read_dynamic_value`, `scalar_metric`, `POSITION_KEYS`, `HIDDEN_ATTRIBUTE_KEYS`; `is_moneyball_search_field` gate for Moneyball mode.
  - `src-tauri/src/features/player_metrics/compact.rs` — 68 current + 68 potential nullable columns in `player_role_metrics` (140 total cols), `PROJECTION_MODEL_VERSION=2`, `SCORE_MODEL_VERSION=1`, `player_metrics_join`, `assert_read_models_complete`, `prepare_player_derived`, `persist_rows_borrowed`.
  - `src-tauri/src/features/scoring/combine.rs` — `combine_role_scores(ip, oop, ipWeight) -> Option<u8>`, rejects non-finite/out-of-range weight, `DEFAULT_IP_WEIGHT=0.5`, round to nearest.
  - `src-tauri/src/features/planner/fit.rs` — `lane_fit_score`, `phase_fit_score`, `foot_matches`, `suitable_familiarity` (>=12), `foot_penalty` (strict -> None, preferred mismatch -> 5), `familiarity_penalty` (<16 -> 5), `adjust_score` saturating.
  - `src-tauri/src/features/search/query.rs` + `filter.rs` + `search/mod.rs` — `SearchView`/`ComparisonPool`, `SortField`/`SortDir`, `PlayerSummary { uid, name, age, nationalities, club/division, ca/pa, dynamicValues, moneyballPercentiles }`, `search_players_in_view` with `player_metrics_join`, Club DNA left join nullable-last sort, filtered `INNER JOIN player_moneyball_stats shortlist ON (snapshot_id, player_uid)` for Shortlist, Moneyball `moneyball_role.*` scoring via `role_score.rs`; `SortField::Dynamic(MetricField)` owns sortable dispatch; `ComparisonPool::FullCsv` vs `Filtered` affects percentiles.
  - `src-tauri/src/features/moneyball/role_catalog.rs` + `role_score.rs` + `src/utils/moneyball-search-metrics.ts` + `src/utils/moneyball-role-catalog.ts` — Moneyball role scoring with distinct catalog (`src-tauri/src/features/moneyball/builtin_role_definitions_v1.json`: 88 definitions, 77 with `attribute_role_id` pointing at a General role, 11 Moneyball-only `attribute_role_id: null` families, all 68 General `role_id`s represented; see Moneyball contract in Invariants). `moneyball_role.<id>` dynamic handling in resolver produces `MetricSource::MoneyballRole` -> needs bounded cohort then `score_role`.
  - `src/components/player-table/player-table-header.tsx` + `virtualized-player-table.tsx` — header owns sort caret, `aria-sort`, move/add/remove via context menu, resize handles (keyboard + pointer), virtual 50-row paging.
  - `src/components/ui/score-badge/score-badge.tsx` — `ScoreBadge` renders integer 0-100 with tier ramp.
- Data model:
  - `player_role_metrics(snapshot_id, uid, score_model_version, projection_model_version, 68 current cols, 68 potential cols)` — one row per current player per effective snapshot; accessed via `player_metrics_join`.
  - `player_moneyball_stats(snapshot_id, player_uid, statistics_json, percentiles_json)` — snapshot-owned cumulative Moneyball rows; joined for Moneyball/Shortlist.
  - `players(snapshot_id, uid, attributes_json, positions_json, preferred_foot, age, ca/pa, current_club/division, ...)` — `positions_json` holds `GK..WBL/WBR` familiarity values (0-20), `preferred_foot` is `"left"|"right"|"either"`.
  - `planner_tactic_lanes(save_id, lane_order, lane_id, ip_weight, importance_rank, preferred_foot, foot_preference, ip_position, ip_role_id, oop_position, oop_role_id)`.
  - No new table for tactic columns; tactic column state lives in `localStorage` key `fm-valuescout-player-table-layouts` via Zustand persist.
- Persistence and migrations:
  - `usePlayerTableStore` persisted under `PLAYER_TABLE_LAYOUT_STORAGE_KEY` with version `6`; `defaultPlayerTableLayouts` gives defaults per `tableId`; `sanitizeLayout` deduplicates and validates against metric `sortable`, retains unknown `metricId.length>0` for staff tables, clamps widths to `72..360`; migrations handle dedup of `club`/`division` and identity-only cases.
- Existing behavioral assumptions:
  - `tableColumnForMetric(metricId, view)` returns `undefined` when metric unknown in that view — column is filtered out before render.
  - `SearchResultsPanel` builds `requestedFields` from non-basic columns; `parse_requested_fields_for_moneyball` requires `moneyball` flag for Moneyball mode; General/Shortlist use attribute-based `role.*`/`potential_role.*` columns.
  - Moneyball Search has its own persisted layout, closed catalog; `Shortlist` reuses General read model but restricts rows to `INNER JOIN player_moneyball_stats`.
  - Search sort uses whitelist + `sql_sort_expression_with_club_dna`; null-last via `... IS NULL ASC` for Moneyball roles and Club DNA.
  - Tactic tactic-options `displayName` is authoritative for role label; `TacticLane` weight/foot/positions/roles are validated both in Rust `validate_tactic` and TS `validateTacticDraft`.
- Architectural seams:
  - Frontend store (`usePlayerTableStore`) <-> panel (`SearchResultsPanel`) via `columnIds`/`widths` observables.
  - Frontend IPC (`searchPlayersQueryOptions` -> `search_players_in_view` via `tauri-client.ts`) with `SearchView`/`ComparisonPool` and `requested_fields` string array.
  - `player_metrics::resolver` owns the trusted `MetricField` parse -> `MetricSource` -> SQL expression expansion. Every new sortable column must extend `MetricSource` and its `sql_expression*` methods, and for Moneyball roles the cohort post-score path in `query.rs`.
  - `planner/tactic.rs` owns save-scoped tactic retrieval/validation; `plannerKeys.tactic` is the frontend read (but currently not save-scoped — see risk).
  - Scoring: `scoring/catalog.rs` owns role IDs, `scoring/combine.rs` blends IP+OOP, `planner/fit.rs` applies familiarity/foot adjustments.
- Project validation commands:
  - `./scripts/dev check` (Biome + `tsc -b` + secretlint + `cargo fmt --check` + Clippy + `(cd src-tauri && cargo test)`) — commit gate (Rust package is `app` / `app_lib`, not `fm-valuescout`; there is no cargo workspace at the repo root, so bare `cargo test` at root is invalid).
  - `./scripts/dev check-app` (Biome + `tsc -b` + full-tree secretlint) — frontend CI gate.
  - `./scripts/dev check-rust` — Rust gate (runs `cargo fmt --check`, clippy, and `cargo test` via the `src-tauri` manifest).
  - `./scripts/dev test [target...]` — `vitest run`.
  - `./scripts/dev smoke` — Playwright smoke with IPC stub.
  - Classifiers are positional installed scripts at `/home/jonas/projects/PI_SETUP/scripts/ledger_state.py` and `/home/jonas/projects/PI_SETUP/scripts/delivery_state.py` (with `publication_state.py` alongside) — not `scripts/ledger_state.py` or `.pi/scripts/ledger_state.py`.
  - Branch reality (corrected 2026-09-03): feature branch `feat/tactic-table-columns` is active (`HEAD d9d2737 feat(search): compute tactic lane scores and sort`, Commit 5 completed). Base `main` at `406dca7` is synchronized with `origin/main` (verified `git branch -vv`; `git merge-base --is-ancestor 406dca7 HEAD` true). Failed Commit 6 remains uncommitted (large diff `git status --porcelain` shows 15 files, `src/lib/with-abort.ts` untracked). Historical note: planning-time work was staged on `release/v0.15.0` with `main` behind `origin/main`; activation from synchronized `main` is reserved for `/skill:workflow-deliver-feature`.
- Primary risks:
  - Tactic column IDs colliding with filter/metric validation or being persisted unsafely (must be allowlisted only for table layout, not filters) — mitigated by neutral allowlist and `biome.json` boundary.
  - SQL sort correctness for null-last combined adjusted scores (distinct from scalar/Club DNA patterns).
  - Double-counting or wrong prerequisites (positions_json shape / missing foot strings).
  - Moneyball role ID gap is bounded to the 8 uncovered `(role, position)` combos above; remaining 103 are uniquely mapped.
  - Interleaving vs straight-order re-compaction bug when toggling or header-X removes one lane while both groups active — mitigated by the single atomic `replaceLayout` action.
  - Tactic validation error UX inconsistent with existing Search panel errors.

## Feature architecture

- Frontend ownership:
  - `src/utils/tactic-ids.ts` (new, neutral shared owner) — single owner of the synthetic column contract's identity: constants `TACTIC_CURRENT_PREFIX = "tactic_current."`, `TACTIC_POTENTIAL_PREFIX = "tactic_potential."`, canonical `TACTIC_LANE_IDS` (defined here as the single source; `src/features/planner/types/tactic.ts` imports and re-exports it — no mirroring), helpers `isTacticColumnId`, `tacticGroupForColumnId`, `tacticLaneIdForColumnId`, `isValidTacticColumnId`, `allTacticColumnIdsForGroup(group, orderedLanes)`, `isFullTacticGroup(columnIds, group)`, `sanitizeTacticIds(columnIds)` to validate lane suffix, and `TACTIC_COLUMN_DEFAULT_WIDTH = 112`. No React, no Zustand, no `@/features` or `@/app` imports.
  - `src/features/search/utils/tactic-columns.ts` — view-model helpers that import only `src/utils/tactic-ids.ts` and `src/utils/position-order.ts` (and pure `planner/types` lane type as a type-only import is forbidden by `biome.json` — therefore the module operates on plain `{laneId, ipPosition, oopPosition}` shapes or the route passes ordered lane IDs directly). Provides `buildTacticColumnOrder(orderedLaneIds, currentActive, potentialActive)` returning interleaved vs straight array, `tacticColumnHeaderLabelParts` helpers are NOT here — header labels are produced in the route from Planner `TacticOptions.displayName`. This module has no `@/features/planner` runtime import.
  - `src/stores/use-player-table-store.ts` — extend `sanitizeLayout` to retain synthetic IDs via `isValidTacticColumnId` from the neutral module for `tableId` in `search | moneyball-search | shortlist` (still rejected on squad/staff). Add one atomic action `replaceLayout(tableId, nextColumnIds: string[])` (or `setLayout`) that validates every ID against the combined allowlists (`getPlayerMetric`/`getMoneyballSearchMetric` or `isValidTacticColumnId`), deduplicates preserving first-seen order, replaces `columnIds` wholesale, and prunes `widths` to retained IDs with `clampWidth`. All toggle and header-X tactic paths use this single action to avoid interleaved partials. Keep `defaultPlayerTableLayouts` unchanged (no default tactic columns). Store does NOT implement group toggling logic beyond accepting/rejecting synthetic IDs and exposing the atomic replace.
  - `src/app/routes/search.tsx` — owns all Planner imports (`validateTacticDraft` from `src/features/planner/utils/tactic-editor.ts`, `plannerTacticQueryOptions`/`plannerKeys.tacticOptions`, `TacticLane`/`TacticOptions` types). Resolves immutable `{ saveId, contextToken }` from `savesQuery` (`SaveSummary` at `src/features/snapshot/types/save.ts:1` via `find(isActive)`) and `snapshot.saveId` equality, creates `plannerTacticQueryOptions(context)` / `plannerTacticOptionsQueryOptions(context)` with `plannerKeys.tactic({ saveId, contextToken })` (exact camelCase, no `activeSaveId` bare id, no `?? -1` sentinel; numeric ID reuse isolated via `contextToken`), gated `enabled: context != null && isMatched`, reset on immutable context / `snapshot.id` change, component-owned non-throwing (`isError` → `Retry`, `isRefetchError` retains data). Composes the filter strip to always render `SearchFilterBar` + `TacticColumnToggles` even when `!snapshot` (only `SearchResultsPanel` branches to empty-state); widths persist while labels update for new context. Passes a normalized view model to `TacticColumnToggles` and `SearchResultsPanel`: `orderedLaneIds: string[]` (derived via `orderedTacticLanes` + `compareTacticPositions` using IP primary / OOP tie-break), `laneLabels: Map<laneId, string>` (formatted `"{IP Position} ({IP Role DisplayName}) / {OOP Position} ({OOP Role DisplayName})"` per lane from `TacticOptions.roles[].displayName`), `currentActive`/`potentialActive` derived from layout, and `onToggle(group)` / `onRemoveTacticColumn` callbacks that compute `nonTacticIds + tacticSuffix` via `buildTacticColumnOrder` and call `replaceLayout`. Search components never import Planner internals directly.
  - `src/features/search/components/search-results-panel.tsx` + `src/features/search/components/tactic-column-toggles.tsx` (new) — panel renders the tactic column space: `tableColumnForMetric` gets a fallback branch that returns a synthetic `PlayerTableColumn { id, label: laneLabels.get(laneId) ?? laneId, align: "right", width: widths[id] ?? TACTIC_COLUMN_DEFAULT_WIDTH }` when `isTacticColumnId(id)` and `isValidTacticColumnId` is known, otherwise filters out unknown tactic IDs. Cell rendering uses `ScoreBadge` when `typeof dynamicValue === "number"` else `"—"` with variant. Sorting dispatches through `requestedFields` inclusion; the panel passes synthetic IDs through `requestedFields` sorted alongside other non-basic columns. `TacticColumnToggles` is a presentational component receiving the normalized view model and callbacks (no direct Planner or store import beyond the neutral predicates).
  - `src/utils/role-catalog.ts` + `src/utils/position-order.ts` — reused without change for ordering; no duplication.
  - `src/features/search/utils/dynamic-columns.ts` — extend `isVisibleSortField` plus route `validateSearch` to accept canonical tactic sort IDs only when present in the current view's persisted tactic layout (`layouts[tableId].columnIds` includes the exact `tactic_current.*` / `tactic_potential.*` ID). General tactic sorts are not gated on `getFilterField`. Invalid/removed/wrong-table tactic sort IDs are rejected and fallback to `defaultSearchSort(view)`. This change lands before the toggle UI commit so any exposed UI sort is already trunk-safe.
- Backend ownership:
  - `src-tauri/src/features/player_metrics/resolver.rs` — extend `MetricSource` with `TacticCurrent { laneId, roleIds }` and `TacticPotential { laneId }` or a unified `Tactic { basis: Current|Potential, laneId }` that does not resolve to a single compact column but to a computed expression over two compact columns + positions/ foot; add `parse_tactic_field_id(field, moneyball)` helper that returns a `MetricField` for `tactic_current.*` / `tactic_potential.*` when `moneyball` is false-or-both depending on view (see view gating). Provide `is_tactic_field` guard, `tactic_column_width` defaults, and ensure `MetricField::kind` is `Integer` (0-100 nullable), `sql_expression_with_club_dna` routes to a placeholder that `query.rs` replaces with a joined computed expression rather than a single column.
  - `src-tauri/src/features/search/query.rs` (and `src-tauri/src/features/squad/query.rs` is NOT touched — Squad stays without tactic columns):
  - Extend prerequisites discovery: detect `uses_tactic_current` / `uses_tactic_potential` from `requested_fields` and `sort_by`. Call `assert_read_models_complete` with `require_score_model = uses_tactic_current || uses_tactic_potential` and `require_projection_model = uses_tactic_potential`.
  - Fetch tactic once per query: `let tactic = load_tactic(conn, save_id)?` (save-scoped). If fetch fails (no save or tactic corruption), surface `Err` that the frontend will render as error banner (distinct from layout mutation error; both use same string when invalid tactic was allowed past validation — defensive second check).
  - For Moneyball view: route tactic columns through the same bounded cohort path that `moneyball_role.*` uses when `view == Moneyball` and tactic column is present. Select the Moneyball role def deterministically by `(attribute_role_id, base_position(phasePosition))` where `base_position` is `tactic.rs::base_position` and `phasePosition` is the lane's `ipPosition` / `oopPosition` normalized. For uncovered combos return `None` -> `NULL`. Reuse `filtered_moneyball_percentiles` + `score_role` per lane then `combine + adjust`; do not duplicate Moneyball cohort logic. Ensure `ComparisonPool` selection (`FullCsv` vs `Filtered`) flows through. Document the 8 uncovered combos in an inline comment and in ledger artifacts — no standalone docs-only commit.
  - For General/Shortlist: for each tactic field in `dynamicFields` + `sort_by`, build one `CASE` expression per lane via a helper `tactic_sql_expression(player_alias, lane, group, age_col, positions_json_col, foot_col, current_col, potential_col, ip_weight)` that mirrors `combine_role_scores` + `fit` penalties. Keep null-last: `ORDER BY (<expr> IS NULL) ASC, <expr> <dir>, players.uid ASC` via the existing `order_sql` branch for tactic sort.
  - Map each requested tactic column into `dynamic_values[fieldId]` as `Some(Integer(score_i64))` or `None`. For `Potential` with age fallback: expression uses `CASE WHEN players.age >= 29 THEN player_metrics.<current_col> ELSE player_metrics.<potential_col> END` for each of the two role reads before combine; alternatively evaluate fallback in Rust per-row after fetching both columns. Choose single-expression path to keep SQL sort correct.
  - `src-tauri/src/features/search/filter.rs` — ensure no tactic ID passes `compile_filters`; this is enforced because `MetricField::parse_for_moneyball` accepts tactic fields but `parse_filter_ast` rejects any rule whose `field` starts with `tactic_current.`/`tactic_potential.` with an `unknown field` error. Document this rejection as intentional.
  - `src-tauri/src/features/scoring/combine.rs` + `src-tauri/src/features/planner/fit.rs` — reuse as pure fns, guarded by `#[cfg(test)]` coverage of penalties and saturation; do not duplicate logic.
- Query & sort seam:
  - `SearchPlayersRequest { view, requested_fields, sort_by, sort_dir }` — `requested_fields` includes tactic IDs when columns are visible; server returns them in `dynamic_values[fieldId]` as `Option<i64>` (value) where `Some(INT)` maps to `DynamicValue::Integer`.
  - `ORDER BY <sort_expression> <dir>, players.uid ASC` with null-last for tactic columns (`... IS NULL ASC` branch similar to Club DNA / Moneyball role).
- Cross-cutting constraints:
  - No WebView-supplied SQL: `MetricField::parse_*` remains the only path that produces a tactic column SQL expression.
  - Width 112 is owned by `src/utils/tactic-ids.ts` default, still clamped via store's `clampWidth`.
  - Error string from `validateTacticDraft` is user-visible; preserve its exact wording for the inline message.

## Uncertainty register

### Known

- Exactly one layout bucket per tableId: `search`, `moneyball-search`, `shortlist`. Squad not included.
- 11 lane IDs are fixed in `TACTIC_LANE_IDS` / `DEFAULT_LANE_IDS`; `TACTIC_POSITION_ORDER` ordering uses `compareTacticPositions` with OOP tie-break. Both are repo-truth.
- Scoring penalties: soft foot `-5`, familiarity `<16` => `-5` per phase, max `-15`, `saturating_sub(0)`.
- Familiarity threshold `>=12` required on both base positions, strict foot mismatch produces unavailability.
- Age fallback `>=29` potential reads current visible attributes — implemented in `features/player::get_player` and `prepare_player_derived`.
- Moneyball mapping is deterministic (evidence: `src-tauri/src/features/moneyball/builtin_role_definitions_v1.json` 88 defs, 77 with `attribute_role_id`, 11 Moneyball-only, all 68 General `role_id`s represented; compound key `(attribute_role_id, TACTIC_POSITION_ORDER base via tactic.rs::base_position)` is unique and covers 103/111 General `(role_id, base_position)` combos; 8 uncovered combos enumerated in Invariants). Frontend and Rust share `base_position` normalization.

### Assumptions

- The existing `SearchResultsPanel` error pattern (retry button + message) is acceptable for tactic validation errors. The worker must inspect current Search error rendering before choosing inline vs toast and keep the chosen pattern in the commit.
- Synthetic IDs `tactic_current.<laneId>` / `tactic_potential.<laneId>` are globally unique and cannot collide with future catalog metrics because catalog metrics use prefixes `role.`, `potential_role.`, `attr.`, `pos.`, `moneyball.`, `moneyball_role.`, `club_dna`, etc. The worker must assert this in `src/utils/tactic-ids.ts` with a compile-time or unit test.
- TypeScript `TacticOptions.roles` `displayName` is the desired header displayName without further formatting (not a lowercase-normalized variant).

### Decisions

- One PR is the correct delivery boundary. Justification: no migration, no new IPC service that must land before UI can merge safely, no separate library that benefits from independent review. Every commit remains trunk-safe via synthetic-ID allowlisting and feature-flag-free panels; a two-PR split would only add branch overhead.
- Neutral shared ownership for tactic identity: `src/utils/tactic-ids.ts` owns the canonical 11 lane IDs, prefix constants, synthetic-ID predicates, lane allowlist check, and default width. `src/features/search/utils/tactic-columns.ts` (if retained) imports only that neutral module plus `src/utils/position-order.ts` and never `@/features/planner`. Route `src/app/routes/search.tsx` owns all Planner imports and passes a normalized view model (ordered lane IDs, laneLabels, lane role/position data, toggle callbacks) into Search components. Stores import only the neutral module. Rejected: Search helper importing Planner internals and stores importing Search helper (violates `biome.json` `noRestrictedImports`; fixed by this decision).
- Display-only column definition is owned by `src/utils/tactic-ids.ts` (identity) plus `src/features/search/utils/tactic-columns.ts` (view-model helpers, no Planner import) on the frontend and by `player_metrics::resolver` (backend) as synthetic metric IDs — never in `PLAYER_METRICS` / `MONEYBALL_SEARCH_METRICS` / filter allowlists. Rejected alternative: adding them to `player-metrics.ts` catalog — would incorrectly make them filterable and conflate the global picker with a tactic-local header label that depends on the active tactic.
- Single atomic store action `replaceLayout(tableId, nextColumnIds)` for all tactic layout mutations (toggle append/remove, interleaving/strip, and tactic-aware header-X removal). Replaces `columnIds` wholesale and prunes `widths` in one validated write, guaranteeing the surviving tactic block is contiguous straight order at the far-right after any X or toggle-off while both were active. Rejected: successive `removeColumn`/`addColumns` calls that leave interleaved partials.
- Rust-owned SQL sort for tactic columns (single `CASE` expression per field) is preferred over client-side post-sort. Rejected alternative: compute in frontend and sort via JS — would require requesting additional fields (positions/foot/role scores per lane) and would break SQL pagination sort stability.
- Tactic header label format is exactly `"{IP Position} ({IP Role DisplayName}) / {OOP Position} ({OOP Role DisplayName})"` per lane; no abbreviation or dual-line stacking, consistent with the dispatch. Labels are produced in the route, not in shared/search helpers.
- Interleaving rule: when both groups active, iterate `orderedTacticLanes(tactic.lanes)` (i.e. tactic-position order) and emit `current` then `potential` per lane. Only when both active; otherwise straight order at far-right via the atomic action.
- Save-scoped tactic acquisition with immutable context (reads + mutation): `plannerTacticQueryOptions(context)` / `plannerTacticOptionsQueryOptions(context)` / `savePlannerTactic({ saveId, contextToken, tactic })` where `context={ saveId, contextToken }` (reuse `ClubDnaContext` at `src/features/club-dna/types/club-dna.ts:1`, `SaveSummary` at `src/features/snapshot/types/save.ts:1`) with `queryKey: plannerKeys.tactic({ saveId, contextToken })` / `tacticOptions({ saveId, contextToken })` and `mutationKey: ["planner", "tactic", { saveId, contextToken }]` analogue `clubDnaKeys.definition` at `src/features/club-dna/api/club-dna-keys.ts:5`, `enabled: context != null && isMatched && !isFetching && !isError` and `queryFn` that calls `fetchPlannerTactic({ saveId, contextToken })` / `fetchPlannerTacticOptions({ saveId, contextToken })` / `savePlannerTactic({ saveId, contextToken, tactic })` with exact camelCase `{ saveId, contextToken }` (+ `tactic` for mutation), gated on active save context and `snapshot.saveId===context.saveId`. Rust validates via snapshot-owned `pub(crate) ensure_active_save_context(&Transaction, save_id, context_token)` at `src-tauri/src/features/snapshot/service.rs` (snapshot boundary) with two-step distinct `Save {id} not found` (unknown numeric save) vs `Save changed or is no longer active` (existing-but-stale/inactive token) inside exactly one `unchecked_transaction` under the command-held `Db` lock; read commands use `get_tactic_in_tx` (default via `save_tactic_in_tx` in same `tx`), mutation uses `ensure_active_save_context` + `save_tactic_in_tx` in same `tx`, options validates then returns static `get_tactic_options()`; trusted wrappers `get_tactic(&Connection, save_id)` / `save_tactic(&Connection, save_id, &tactic)` are preserved for Search/depth/tests and delegate by opening one `tx` to the helpers — no nested transactions, no duplicate validator, numeric ID reuse isolated via immutable `{ saveId, contextToken }`-keyed keys (`contextKey({ saveId, contextToken })` analogue at `src/features/club-dna/components/club-dna-definition.tsx:67`). Component-owned non-throwing reads: initial `isError` renders actionable `Retry`, cached `isRefetchError` retains `data` read-only; mutation stale/inactive/unknown is rejected in same `tx` with same distinct errors (no `active_save_id` fallback). Invalidates/resets on `context` / `snapshot.id` change. Route forbids tactic mutation when `context==null` (no active save) and composes filter strip + toggles even when `!snapshot` (only panel branches, no tactic fetch). Rejected: save-independent `plannerTacticQueryOptions` that resolves `active_save_id` implicitly, and bare `saveId` without `contextToken` (no ID reuse isolation, no stale-token detection). Exact consumers: Rust `get_planner_tactic`/`get_planner_tactic_options`/`save_planner_tactic` at `src-tauri/src/features/planner/commands.rs`; frontend `fetchPlannerTactic`/`fetchPlannerTacticOptions`/`savePlannerTactic`, `plannerKeys`, `plannerTacticQueryOptions`/`plannerTacticOptionsQueryOptions`, `PlannerTacticEditor` mutation + `queryClient.setQueryData(plannerKeys.tactic(context), ...)`, `my-club.tsx` + `search.tsx` callers, `src/testing/setup.ts`, `src/testing/planner-ipc-mock.ts`, `e2e/tauri-ipc-stub.ts`. `src/testing/setup.ts` + `e2e/tauri-ipc-stub.ts` are context-keyed with exact camelCase validation and immutable context-keyed delayed A/B test-local `Deferred` per context.
- Route sort acceptance before UI exposure: `isVisibleSortField` + `validateSearch` accept a canonical tactic sort only when the exact ID is present in the current view's persisted layout (`layouts[tableId].columnIds`). Invalid/removed/wrong-table IDs fallback to `defaultSearchSort(view)`. This commit precedes toggle UI so every exposed sort is already trunk-safe.
- Fold the speculative docs-only gap commit into implementation commit 5 plus durable ledger/docs: comment the Moneyball-`NULL` branch and the 8-case invariant directly in `resolver.rs`/`query.rs`; do not create a standalone docs-only commit and do not copy catalog counts into `src/utils/tactic-ids.ts`.
- Tooling facts: ledger/delivery classifiers are positional scripts at `/home/jonas/projects/PI_SETUP/scripts/ledger_state.py` and `/home/jonas/projects/PI_SETUP/scripts/delivery_state.py` (with `publication_state.py` alongside); Rust crate is `app` (`app_lib`) with no repo-root `Cargo.toml` — run Rust tests via `(cd src-tauri && cargo test <filter>)` or `--manifest-path src-tauri/Cargo.toml`, never `cargo test -p fm-valuescout` or bare `cargo test` at root. Branch reality (2026-09-03): `feat/tactic-table-columns` is active (`HEAD d9d2737`), base `main` `406dca7` synchronized with `origin/main`; failed Commit 6 remains uncommitted. Historical note: planning-time review branch was `release/v0.15.0` with `main` behind `origin/main`.

### Unknowns

- Whether `position_order.ts` `compareTacticPositions` with IP-primary + OOP tie-break already matches the design's intent vs pure IP order — the plan specifies `compareTacticPositions(ipPosition)` primary, OOP only for ties, which aligns with `orderedTacticLanes`.

### Risks

- Resolver/SQL expression for tactic columns expands to 22 possible dynamic fields (11 lanes x 2 bases) all referencing `json_extract(positions_json, ...)` and two compact columns each. A naive per-column expression duplicates identical subexpressions and risks query size/CPU growth for clients that request all 22 columns. Mitigation: the helper generates a minimal `CASE` with shared `json_extract` per base position for the two shapes; cap total `requested_fields` at `MAX_REQUESTED_FIELDS=256` keeps it bounded; add a unit test that asserts generated SQL length stays under a budget for 22 columns.
- Store persistence leakage: a user who manually edits `localStorage` could inject arbitrary `tactic_current.*` IDs with unknown lane suffixes. Mitigation: sanitization filters unknown lane suffixes via neutral allowlist (`TACTIC_LANE_IDS` set) in `sanitizeLayout` and `replaceLayout`.
- Moneyball filtered vs full-CSV scoring interaction: tactic Moneyball columns reuse the same `ComparisonPool`; ensure tactic `moneyball_role.*` underlying scores respect `ComparisonPool::Filtered` computation path when `comparisonPool === "filtered"`.
- Race between tactic save and Search toggle: a concurrent tactic edit invalidates `plannerKeys.tactic({ saveId, contextToken })` mid-render (immutable context, not bare `activeSaveId`). Mitigation: Search toggle reads tactic from Query cache snapshot for the current immutable `{ saveId, contextToken }` at click handler start and validates via `validateTacticDraft` before layout mutation; widths persist while labels update for new context, not leaked, not cleared; a post-mutation tactic invalidation is not reverted.
- Existing `getPlayerMetric` callers (filter validation in `SanitizeLayout`) must not treat synthetic IDs as unknown metrics to drop — the store's sanitize must retain them when their lane suffix is valid; regression would silently strip tactic columns after refresh.
- Sort validation must not regress basic metrics: `isVisibleSortField` must still delegate to `getFilterField` for non-tactic IDs; tactic acceptance is an allowlist branch checked first against `layouts[tableId].columnIds`.

## Walking skeleton

The thinnest end-to-end path that proves the approach:

1. Add `src/utils/tactic-ids.ts` with lane constants, `isTacticColumnId`/`isValidTacticColumnId` guard, and `TACTIC_COLUMN_DEFAULT_WIDTH`, plus a test that asserts prefix non-collision and allows only the 11 canonical lane suffixes.
2. Extend `usePlayerTableStore` with `sanitizeLayout` retaining valid synthetic IDs via the neutral predicate and one atomic `replaceLayout` action that replaces `columnIds` and prunes `widths` in one write, with one unit test proving `sanitizeLayout` keeps `tactic_current.goalkeeper` and the atomic action prunes correctly.
3. Extend `player_metrics::resolver` to parse `tactic_current.*` / `tactic_potential.*` into a guarded `MetricSource::Tactic` (not filter-eligible) and extend `SearchResultsPanel.tableColumnForMetric` to render synthetic columns via the route-passed `laneLabels` map with width `TACTIC_COLUMN_DEFAULT_WIDTH` and `ScoreBadge`/`"—"`.
4. In `src-tauri/src/features/search/query.rs` wire one tactic column to a Rust-generated `CASE` SQL expression (deterministic Moneyball mapping via `attribute_role_id + base_position` for Moneyball view, compact columns for General) that reads the two role columns + positions/foot and emits `NULL` vs `0..100`, then verify virtual paging and null-last sort for that single column across General and Moneyball (mapped numeric vs uncovered `NULL`). The two Search toggle buttons and interleaving polish are not required for the skeleton; one hardcoded lane proves the Rust/UI contract.
5. Proof is a `vitest run` targeting `src/stores/use-player-table-store.test.ts` plus `(cd src-tauri && cargo test --lib -- resolver)` and `(cd src-tauri && cargo test --lib -- search::query)` and `(cd src-tauri && cargo test --lib -- planner::fit)` (or `./scripts/dev check-rust`) with one new test each showing synthetic column retention, header label formation, and valid SQL expression for a tactic sort. Separate filtered Cargo commands are required — `cargo test` accepts only one `TESTNAME` filter.

## Delivery plan

### PR 1 — Tactic columns for player tables (Search, Moneyball, Shortlist)

**Status:** Active

**PR ref:** Not published

**Merge ref:** Not merged

**Branch:** feat/tactic-table-columns

**Base branch:** main

**Publication provider:** GitHub

**PR template:** .github/pull_request_template.md

**Merge method:** squash

**Required checks:** GitHub required checks

**Feature close-out:** Not run

**CI repair rounds:** 0

**Provisional PR title:** `feat(search): add tactic lane columns for player tables`

**Purpose:** Ship tactic-aware evaluation as optional display columns on the three player tables (Search General, Moneyball, Shortlist) with persistence, interleaving, sorting, and blended adjusted scoring. Single PR because there is no cross-cutting migration or dependency that demands an independent review seam; every commit stays trunk-safe without a feature flag. The single docs-only gap commit from the prior draft is folded into implementation commits 5 and 7 plus durable ledger/docs.

**Depends on:** `main` at `406dca7` (synchronized with `origin/main`) plus completed commits 1–5 on `feat/tactic-table-columns` (`HEAD d9d2737`). Historical note: during planning the branch was recorded as intent but not activated; the plan now runs on the active feature branch with failed Commit 6 still uncommitted.

#### Commit 1 — Record the approved feature plan

**Status:** Completed

**Provisional commit:** `docs(tactic-table-columns): record approved feature plan`

**Work:** Commit the independently reviewed planning artifacts on the feature branch before implementation.

**Size assessment:** No implementation code; planning-only commit.

**Out of scope:**

- Implementation, tests, executable configuration, generated files, and unrelated documentation.

**Implementation packet:**

- Preserve the accepted plan-review outcome. Commit only the reviewed planning paths after branch verification.

**Files and responsibilities:**

- `.wiki/features/active/tactic-table-columns.md` — approved feature intent, delivery plan, and packets.
- `.wiki/TODO.md` — move entry to Active with link to the ledger (transfer the resolved developer intent table into ledger Intent verbatim).
- `.wiki/BACKLOG.md` — no change (deferred scope not introduced).
- `.wiki/features/planned/tactic-table-columns.md` — remove after its accepted content is preserved, when present (if absent, record no-op).
- `.wiki/decisions/<id>-tactic-table-columns-display.md` — rejected as unwarranted ADR: decision is tactic-column display plumbing, not a durable structural choice with meaningful alternatives across releases; rationale lives in ledger Uncertainty register instead. No ADR is created.

**Behavior and data flow:**

- Move planning truth from the planned source into one reviewed active ledger and record the exact delivery sequence before implementation.

**Ordered implementation steps:**

1. Verify the active branch and base without changing Git state (`git rev-parse --abbrev-ref HEAD`, `git merge-base --is-ancestor`).
2. Confirm the worktree contains only the reviewed planning paths (`git status --porcelain`).
3. Run the ledger classifier at its installed positional path `/home/jonas/projects/PI_SETUP/scripts/ledger_state.py .wiki/features/active/tactic-table-columns.md` (if the classifier is absent, record the gap and do not block the planning-only commit, but log the missing validator).
4. Stage and inspect the exact planning diff for independent checkpoint review (`git diff --cached --stat`, `--check`).

**Tests and proof:**

- Not applicable — this commit changes planning documents only. The ledger classifier and documentation checks prove structural consistency when available.

**Patterns to verify:**

- The active-ledger template, current TODO/BACKLOG ownership rules, and that no implementation path is staged.

**Constraints and non-goals:**

- Do not alter implementation, tests, executable configuration, plan scope, packet order, or reviewed decisions.

**Dependencies and sequencing:**

- Requires an accepted plan-review verdict, developer acceptance, a valid Delivery fingerprint when the classifier is available, and exact branch activation via the delivery workflow (not planning).

**Validation:** `python3 /home/jonas/projects/PI_SETUP/scripts/ledger_state.py .wiki/features/active/tactic-table-columns.md` when available plus `ls .wiki/features/active/tactic-table-columns.md`; otherwise record validator gap.

**Stop conditions:** Stop on an uncleared review, a classifier error, an unreviewed path, a substantive post-review plan change, or a branch mismatch.

**Review mandate:** Verify that the staged diff contains the complete reviewed planning outcome and no implementation or unrelated files; that TODO linking is correct; and that no ADR was created without justification.

#### Commit 2 — Add neutral tactic-identity helpers and ordering

**Status:** Completed

**Provisional commit:** `feat(search): add neutral tactic column helpers`

**Work:** Add the single neutral owner of the synthetic column identity and lane ordering without yet wiring store persistence or IPC. No Planner or app imports.

**Size assessment:** ~120 non-test lines. Within the soft target.

**Out of scope:**

- Store sanitization for synthetic IDs, Rust resolver, Search query integration, toggle buttons, Moneyball mapping, route composition.

**Implementation packet:**

**Files and responsibilities:**

- `src/utils/tactic-ids.ts` (new, neutral shared owner) — defines canonical `TACTIC_LANE_IDS` (single source) with `TACTIC_CURRENT_PREFIX = "tactic_current."`, `TACTIC_POTENTIAL_PREFIX = "tactic_potential."`, `TACTIC_COLUMN_DEFAULT_WIDTH = 112`, lane allowlist `Set<string>`, helpers `isTacticColumnId(id)`, `tacticGroupForId(id)`, `tacticLaneIdForId(id)`, `tacticColumnId(group, laneId)`, `isValidTacticColumnId(id)` (allowlist check), `allTacticColumnIdsForGroup(group, orderedLaneIds)` (11 IDs in tactic-position order), `isFullTacticGroup(columnIds, group)`, `sanitizeTacticIds(columnIds)` filter. Assert equality with `src-tauri/src/features/planner/tactic.rs` `DEFAULT_LANE_IDS` at test time.
- `src/features/planner/types/tactic.ts` (edit existing) — remove local `TACTIC_LANE_IDS` array definition; add `import { TACTIC_LANE_IDS } from "@/utils/tactic-ids"` and re-export it (`export { TACTIC_LANE_IDS } from "@/utils/tactic-ids"` or `export const TACTIC_LANE_IDS = ...imported...`). No other type changes. Verifies `biome.json` boundary (neutral imports only upward) and single source is preserved. Added to this commit per HIGH1.
- `src/utils/tactic-ids.test.ts` (new) — unit tests: valid vs invalid lane suffix (`not_a_lane` rejected), `isFullTacticGroup` for 10 vs 11, prefix non-collision with `role.`/`moneyball_role.`/`attr.` etc, and lane count 11 matches Rust source (`DEFAULT_LANE_IDS`) and re-exported `src/features/planner/types/tactic.ts`.
- `src/features/search/utils/tactic-columns.ts` (new, feature-local but neutral-import-only) — pure helpers `buildTacticColumnOrder(orderedLaneIds, currentActive, potentialActive)` returning deterministic interleaved vs straight array, and `orderedLaneIdsForColumns(orderedTacticLanes)` that delegates to `compareTacticPositions` with OOP tie-break (no `features/planner` import). Consumes only `src/utils/tactic-ids.ts` and `src/utils/position-order.ts`. Unit-tested for interleaved order `GK Cur, GK Pot, DR Cur, DR Pot,…` and straight order.
- `src/utils/position-order.ts` — no change; read-only ordering contract.
- `src/features/planner/utils/tactic-editor.ts` — no change; verified read-only for `orderedTacticLanes` shape but not imported from shared/search.

**Behavior and data flow:**

- Modules are pure: no React, no Zustand, no IPC, no `@/features/planner` or `@/app` imports. `tactic-ids` is importable by stores and feature helpers without violating `biome.json` boundaries. Header label formation is NOT in these helpers — it is composed in the route passing a normalized view model.

**Ordered implementation steps:**

1. Create `src/utils/tactic-ids.ts` with every exported helper and lane allowlist derived from the canonical IDs. Add `TACTIC_COLUMN_DEFAULT_WIDTH = 112` export. This is the single source for `TACTIC_LANE_IDS`.
2. Edit `src/features/planner/types/tactic.ts` to remove its local `TACTIC_LANE_IDS` array and replace with `import { TACTIC_LANE_IDS } from "@/utils/tactic-ids"` + re-export. Verify `rg -n "@/features" src/utils/tactic-ids.ts` is empty and `src/features/planner/types/tactic.ts` now imports from `@/utils/tactic-ids` only (allowed: features may import from `@/utils`).
3. Create `src/features/search/utils/tactic-columns.ts` with `buildTacticColumnOrder` and ordered-lane helper delegating to `compareTacticPositions` so drift with Planner ordering is impossible.
4. Add unit tests for both modules covering: lane allowlist, `isFullTacticGroup`, interleaved vs straight, assertion that synthetic ID prefixes cannot collide with real catalog prefixes (visual grep in test), and that `src/utils/tactic-ids.ts` `TACTIC_LANE_IDS` equals `src-tauri/src/features/planner/tactic.rs` `DEFAULT_LANE_IDS` and equals the re-export from `src/features/planner/types/tactic.ts`.
5. Run `vitest run` and `tsc -b --noEmit`. Verify `rg -n "from \"@/features" src/utils/tactic-ids.ts` is empty and `rg -n "from \"@/features/planner" src/features/search/utils/tactic-columns.ts` is empty.

**Tests and proof:**

- New tests listed above; expected RED before implementation is missing module/exports. GREEN asserts exact ordering against `TACTIC_POSITION_ORDER` slice and lane allowlist of 11. No obsolete test asset to remove; deliberately retain existing planner utils tests.

**Patterns to verify:**

- `src/utils/position-order.ts` `compareTacticPositions` stable sort contract. Mirror its import, do not re-implement ordering.
- `biome.json` `noRestrictedImports` patterns — verify no shared→features or features→features cross-import remains.

**Constraints and non-goals:**

- Do not import across features (no `@/features/planner` from shared or from search helper beyond allowed shared modules). Tactic-columns must not import `validateTacticDraft` or Planner query options — those stay in `app/routes`.
- Do not add synthetic metrics to `PLAYER_METRICS` / `MONEYBALL_SEARCH_METRICS`.
- Do not expose `TacticLane` type through the neutral module (use `laneId: string` shapes only).

**Dependencies and sequencing:**

- Depends on commit 1 (ledger). No prior implementation commit.

**Validation:** `./scripts/dev check-app` (Biome + `tsc -b`), `./scripts/dev test src/utils/tactic-ids.test.ts src/features/search/utils/tactic-columns.test.ts`, `rg -n "@/features/planner|@/app" src/utils/tactic-ids.ts src/features/search/utils/tactic-columns.ts` must show no import (Biome check would fail if present).

**Stop conditions:** Stop if `TACTIC_LANE_IDS` vs `tactic.rs` `DEFAULT_LANE_IDS` drift is observed (counts or values mismatch) — escalate to ledger discovery before continuing.

**Review mandate:**

1. Synthetic IDs cannot be constructed from arbitrary WebView strings (allowlist is exactly 11 lane IDs).
2. No `@/features/planner` or `@/app` import in `src/utils/tactic-ids.ts` or `src/features/search/utils/tactic-columns.ts` (Biome would error).
3. Ordering delegates to `compareTacticPositions` rather than duplicating sort.
4. Module has no React/Zustand/IPC import.
5. Prefix `tactic_current.` / `tactic_potential.` does not collide with `role.` / `moneyball_role.` checked by grep/test.
6. Tests assert null vs truncated ordering difference.
7. Exports include `isValidTacticColumnId` and `TACTIC_COLUMN_DEFAULT_WIDTH` for store/panel reuse.
8. No behavior is wired into panels yet (isolated helper commit).

#### Commit 3 — Persist synthetic tactic layouts with an atomic replace action

**Status:** Completed

**Provisional commit:** `feat(search): persist tactic column layouts atomically`

**Work:** Make store accept, retain, and re-hydrate synthetic tactic column IDs with valid widths; add one atomic layout-replace action used by all tactic mutations. No other toggle or query logic yet.

**Size assessment:** ~120 non-test lines + ~80 test lines. Within the soft target.

**Out of scope:**

- Rust resolver extension, panel rendering of synthetic columns, toggle button UI, Moneyball scoring, IPC changes, validation UX, route sort gating.

**Implementation packet:**

**Files and responsibilities:**

- `src/stores/use-player-table-store.ts` — extend `sanitizeLayout` for `tableId` in `search | moneyball-search | shortlist`:
  - Accept `tactic_current.<laneId>` / `tactic_potential.<laneId>` when `isValidTacticColumnId` from `src/utils/tactic-ids.ts` holds (lane in `TACTIC_LANE_IDS`), otherwise drop with same dedup logic as existing metrics. Ensure `getPlayerMetric`/`getMoneyballSearchMetric` are NOT consulted for synthetic IDs (they would reject them); synthetic validation is the neutral allowlist only.
  - Preserve `widths` for synthetic IDs with `clampWidth`; drop widths for IDs not in retained `columnIds` (as existing logic does). Keep `defaultLayout` defaults unchanged (no default tactic columns).
  - Add one atomic action `replaceLayout(table: PlayerTableId, nextColumnIds: readonly string[])` (name `setLayout` or `replaceLayout`) that:
    - Validates every `nextColumnIds` entry: either `getPlayerMetric(...).sortable` / `getMoneyballSearchMetric(...).sortable` or `isValidTacticColumnId` for the three tactic-capable tables (otherwise reuses existing `metricId.length>0` for staff tables).
    - Deduplicates preserving first-seen order (`indexOf` guard as in `sanitizeLayout`).
    - Replaces `layouts[table].columnIds` wholesale to `nextColumnIds` (no append), rebuilds `widths` to exactly retained IDs via `clampWidth`, and handles the empty-result fallback: if deduplicated `nextColumnIds` is empty (e.g. tactic-only layout where toggling off the sole full group would leave zero columns), replace with `defaultLayout(table).columnIds` instead of rejecting or persisting empty; otherwise enforces the single-column guard (`length===1` cannot be removed via caller — action rejects empty before fallback unless fallback applies).
    - Is the only tactic mutation path; existing `addColumns`/`removeColumn` remain for non-tactic callers but tactic toggle/header-X code must use this atomic action.
  - Keep `de` and `moveColumn`/`setColumnWidth` behavior for tactic IDs (width 112 default is owned by neutral module, clamped via store's `clampWidth`).
- `src/stores/use-player-table-store.test.ts` — add cases: hydrates valid synthetic group from persisted state, drops invalid lane suffix, clamps width 10_000 -> 360, retains after `sanitizePersistedState` re-run, asserts that a full group of 11 is kept verbatim, that `replaceLayout(table, [...nonTactic, ...straightTactic])` atomically replaces and prunes widths in one write (no intermediate partial visible) including the recompaction case, and that a tactic-only layout (`columnIds` equals exactly one full `tactic_current` group) toggling off via `replaceLayout(table, [])` falls back to `defaultLayout(table).columnIds` (not empty, not rejected) — per MEDIUM4.

**Behavior and data flow:**

- Persisted JSON -> `sanitizePersistedState` -> `sanitizeLayout` retains synthetic IDs when lane valid. `layouts[tableId].columnIds` now may contain tactic IDs; every other `tableId` (squad, staff) rejects them.
- `replaceLayout(table, nextIds)` is a single Zustand `set` that swaps `columnIds` and rebuilds `widths` atomically.

**Ordered implementation steps:**

1. Import `isValidTacticColumnId` from `src/utils/tactic-ids.ts` into `use-player-table-store.ts` (verify `rg -n "@/features" src/stores/use-player-table-store.ts` is empty — neutral import only).
2. Modify `sanitizeLayout` filter branch for the three tableIds to `metrics allowlist OR isValidTacticColumnId`.
3. Add `replaceLayout` atomic action as described.
4. Add unit test fixtures: persist a JSON with `search: { columnIds: ["name","tactic_current.goalkeeper", ...11 current, "tactic_potential.goalkeeper"...], widths: { "tactic_current.goalkeeper": 999, "tactic_current.not_a_lane": 120 } }` and assert retained vs stripped vs clamped, plus atomic replace pruning.
5. Run targeted vitest + `tsc -b`. Verify Biome `noRestrictedImports` passes (stores must not import features).

**Tests and proof:**

- Updated `use-player-table-store.test.ts` with four+ tactic cases listed above plus atomic replace action test; expected RED is `"unknown metric"` style drop before change. GREEN asserts retained 11+width-clamp, invalid suffix stripped, and atomic replace prunes widths without leaving interleaved partial.

**Patterns to verify:**

- Existing `sanitizeLayout` dedup and width-strip pattern — copy exactly for synthetic branch.
- `clampWidth(72..360)` reuse; default width 112 is owned by `src/utils/tactic-ids.ts` not store.
- Keep `PLAYER_TABLE_LAYOUT_VERSION` unchanged; retention is a compatible read of existing persisted JSON (no migration needed). If version bump is needed, justify in commit notes and add `migratePersistedState` entry — avoid speculating.

**Constraints and non-goals:**

- Do not expose tactic columns in the metric picker filter list — store path must not call `getPlayerMetric` for them.
- Do not add default tactic columns; do not change `defaultLayout` for any table.
- Do not write interleaving logic here beyond the atomic replace (panel toggle owns the order computation; store merely persists whatever order the route computes via `replaceLayout`).

**Dependencies and sequencing:**

- Depends on commit 2 (neutral helpers).

**Validation:** `./scripts/dev check-app`, `./scripts/dev test src/stores/use-player-table-store.test.ts`, `./scripts/dev test src/utils/tactic-ids.test.ts src/features/search/utils/tactic-columns.test.ts`.

**Stop conditions:** Import cycle detected between store and tactic helpers -> re-route `isValidTacticColumnId` into a tiny shared `src/utils/tactic-ids.ts` only (already the case). If store were to import planner types, revert.

**Review mandate:**

1. Synthetic IDs retained only on the three player tables, rejected on squad/staff.
2. Invalid lane suffix stripped and its width removed.
3. Dedup preserved (`all.indexOf` guard still applies).
4. Width clamping reused, not duplicated limit.
5. No bump of `PLAYER_TABLE_LAYOUT_VERSION` without a documented reason and migration.
6. `getPlayerMetric` not called for synthetic branch (neutral allowlist only).
7. Persisted-state round-trip via `sanitizePersistedState` retained.
8. No `@/features` import in `src/stores/use-player-table-store.ts` (Biome rule).
9. Atomic action replaces `columnIds` and prunes `widths` in one write, usable by toggles and header-X.

#### Commit 4 — Extend Rust resolver for synthetic tactic metric fields

**Status:** Completed

**Provisional commit:** `feat(player-metrics): support tactic metric fields in resolver`

**Work:** Teach `MetricField::parse_for_moneyball` to recognize `tactic_current.<laneId>` / `tactic_potential.<laneId>` synthetic fields as valid sortable display columns, without making them filter-eligible.

**Size assessment:** ~130 Rust lines + ~40 test lines. Within the soft target.

**Out of scope:**

- Score computation, SQL expression generation, sort integration, filter compilation path, store/panel wiring, route sort gating.

**Implementation packet:**

**Files and responsibilities:**

- `src-tauri/src/features/player_metrics/resolver.rs` — extend `MetricSource` with `Tactic { group: TacticGroup, laneId: String }` (or two variants `TacticCurrent`/`TacticPotential`), add `TACTIC_PREFIXES` constants, `parse_tactic_field_id(field, allowMoneyball: bool)` that:
  - rejects unless `field` starts with `tactic_current.` / `tactic_potential.`,
  - extracts `laneId` and validates it is in `DEFAULT_LANE_IDS` (reusing `planner::tactic::DEFAULT_LANE_IDS` constant or a local allowlist mirroring it; prefer reusing the constant to avoid drift, add cross-module `assert_eq!` test comparing lengths/values),
  - validates lane id snake_case is already safe (it is), returns a new `MetricField { id, kind: Integer, source: MetricSource::TacticRegistered }`.
  - In `MetricField::parse_for_moneyball`, check tactic prefixes before delegation to Moneyball/scalar branches; for Moneyball mode, tactic fields are accepted as display columns in both views (General/Shortlist accepted via General path, Moneyball accepted via Moneyball path) but never as filter targets — tactic sort is routed via `MetricField` -> SQL, not via filter allowlist. Ensure the `unknown player metric` string for non-allowlist tactic suffix matches existing error shape (starts with `"unknown player metric:"` or a distinct `"unknown tactic lane"` — keep one stable string and document it).
  - Add `is_tactic_field()` / `is_tactic_current()` / `tactic_lane_id()` accessors.
  - Ensure `MetricField::kind() == Integer` for tactic fields, and `sql_expression` for this commit returns `"NULL"` (placeholder) — query commit will override with `sql_expression_with_*` specialization; keep placeholder so `MAX_REQUESTED_FIELDS` + parsing does not panic.
- `src-tauri/src/features/player_metrics/resolver.rs` tests — add `#[test]` cases: valid current+potential for every `DEFAULT_LANE_IDS` member, invalid suffix rejected, dedup via `parse_requested_fields*` preserves order, and `is_moneyball_search_field` still excludes tactic fields from filter eligibility (tactic fields are not Moneyball search fields).
- `src-tauri/src/features/search/filter.rs` — verify filter rejection (no code change expected beyond confirming tactic prefix is not in Moneyball search field allowlist). If filter path currently accepts unknown fields as `MetricField` and then filters them, add an explicit guard that `field.starts_with("tactic_")` is rejected at `parse_filter_ast`. Keep change minimal.

**Behavior and data flow:**

- `parse_requested_fields_for_moneyball(&["tactic_current.goalkeeper"], false)` now `Ok(vec![MetricField { id: "tactic_current.goalkeeper", kind: Integer, source: Tactic { ... }}])`.
- `parseFilterAst` with `field: "tactic_current.goalkeeper"` still `Err("unknown filter field")` or similar — tactic columns never become filters.

**Ordered implementation steps:**

1. Create `TACTIC_LANE_IDS` allowlist tied to `planner::tactic::DEFAULT_LANE_IDS` (import or replicate with `assert_eq!` compile test).
2. Extend `MetricSource` enum and `MetricField::{is_tactic_field, tactic_group, tactic_lane_id}` accessors.
3. Implement `parse_tactic_field_id` and wire it in `parse_for_moneyball`.
4. Add the Rust unit tests listed above; ensure `MAX_REQUESTED_FIELDS` bound still enforced for tactic IDs.
5. Run `(cd src-tauri && cargo test resolver)` (or `cargo test --manifest-path src-tauri/Cargo.toml resolver`).

**Tests and proof:**

- New Rust tests assert every laneId valid for both prefixes, invalid `not_a_lane` rejected, `parse_requested_fields` dedup preserves order, and `compile_filters` rejects tactic rule. Expected RED before commit is `unknown player metric: tactic_current.goalkeeper`. GREEN provides same-ID roundtrip `sql_sort_expression` placeholder `"NULL"` (updated in next commit).

**Patterns to verify:**

- `MoneyballStatistic` / `MoneyballRole` branches as nearest analogues for conditional Moneyball gating and `sql_expression` dispatch.
- Shelved field case: `MetricSource::MoneyballRole` returns `"NULL"` for `sql_expression` intentionally — tactic fields copy that, but commit 5 replaces with real CASE.

**Constraints and non-goals:**

- Do not enable tactic fields as filter targets — `FilterAst` compile must continue to reject them.
- Do not derive `MetricField` via WebView SQL: pure allowlist+suffix check only.

**Dependencies and sequencing:**

- Depends on commit 3. Frontend helpers are not required for this Rust commit, but lane ID truth must align between TS and Rust.

**Validation:** `./scripts/dev check-rust` (includes `cargo fmt --check`, clippy, and `(cd src-tauri && cargo test)`), focused `(cd src-tauri && cargo test resolver)`.

**Stop conditions:** If `planner::tactic::DEFAULT_LANE_IDS` is not `pub` or cannot be reused without import cycle, replicate with a local `const` and add a cross-module `assert_eq!` test comparing lengths/values.

**Review mandate:**

1. Allowlist is the exact 11 lane IDs, not open-ended string suffix.
2. `MetricField::parse_for_moneyball` gate does not accidentally permit tactic fields as Moneyball metrics in `is_moneyball_search_field`.
3. Filter path rejects tactic rule (error string stable and user-visible not leaked as SQL).
4. Placeholder `NULL` expression does not escape into user-visible sort.
5. No new migration or column.
6. Tests cover all 11 lanes for both prefixes.
7. Error strings match existing `"unknown player metric: {field}"` shape.
8. `MAX_REQUESTED_FIELDS` cap still enforced.

#### Commit 5 — Implement tactic lane scoring and query sort (Rust, Moneyball mapping)

**Status:** Completed

**Provisional commit:** `feat(search): compute tactic lane scores and sort`

**Work:** Realize the blended, familiarity/foot-adjusted score for each tactic column and wire it into `search_players_in_view` so sortable tactic columns produce numeric values for display and correct null-last `ORDER BY`. Record the deterministic Moneyball mapping contract and make uncovered combos return `NULL`.

**Size assessment:** ~180 Rust lines + ~90 tests. Near soft limit; atomicity (single scorer + SQL expression + sort wiring + mapping contract) justifies keeping it as one commit rather than splitting scorer and query. The former standalone gap doc commit is folded here: Moneyball mapping notes live as inline comments plus the uncovered-`NULL` test and ledger invariant, not as a separate commit.

**Out of scope:**

- Frontend toggle/interleaving UI, route sort validation, store atomic action beyond what exists, no-snapshot composition (owned by next commit).

**Implementation packet:**

**Files and responsibilities:**

- `src-tauri/src/features/planner/fit.rs` — add `pub(crate) fn tactic_adjusted_score(ip: Option<u8>, oop: Option<u8>, ip_weight: f64, player_foot: &str, positions: &BTreeMap<String, Option<i64>>, lane: &TacticLane) -> Option<u8>` composing `combine_role_scores` then `suitable_familiarity` check on both bases (`base_position` per `tactic.rs`), `foot_matches` strict-null path, then `familiarity_penalty` per phase (+ foot soft -5) with `adjust_score` saturation. Keep `lane_fit_score` private but verify same behavior; if `lane_fit_score` currently expects `score: Option<u8>` pre-combined, this new function is the lane helper that owns combine first. Preserve `age >= 29` fallback outside `fit.rs` (in compact column read selection), not inside `adjust_score`.
- `src-tauri/src/features/player_metrics/resolver.rs` — extend `sql_sort_expression_with_club_dna` for `MetricSource::Tactic` to return a Rust-generated `CASE` expression rather than `"NULL"`. For General/Shortlist builds the expression references `player_metrics.<ip_role_col>`, `player_metrics.<oop_role_col>`; include `players.age` read for potential fallback. For Moneyball view, the expression references `moneyball.statistics_json` vs percentile-derived role scores via the post-score path — determine in `query.rs` which branch applies and make resolver return `"NULL"` for Moneyball tactic fields while query post-scores them (alternatively return a sentinel that query substitutes).
- `src-tauri/src/features/search/query.rs` (and `src-tauri/src/features/squad/query.rs` is NOT touched — Squad stays without tactic columns):
  - Extend prerequisites discovery: detect `uses_tactic_current` / `uses_tactic_potential` from `requested_fields` and `sort_by`. Call `assert_read_models_complete` with `require_score_model = uses_tactic_current || uses_tactic_potential` and `require_projection_model = uses_tactic_potential`.
  - Fetch tactic once per query: `let tactic = load_tactic(conn, save_id)?` (save-scoped). If fetch fails (no save or tactic corruption), surface `Err` that the frontend will render as error banner (distinct from layout mutation error).
  - For Moneyball view: route tactic columns through the same bounded cohort path that `moneyball_role.*` uses when `view == Moneyball` and tactic column is present. Deterministically select the Moneyball role def by `(attribute_role_id, base_position(placement))` using `tactic.rs::base_position`; for the 8 uncovered combos emit `None` -> `NULL`. Reuse `filtered_moneyball_percentiles` + `score_role` per lane then `combine + adjust`; do not duplicate Moneyball cohort logic. Ensure `ComparisonPool` selection (`FullCsv` vs `Filtered`) flows through. Inline comment: `"// If tactic role id has no Moneyball catalog entry for this position tag, score is None -> \"—\" (by design — 8 uncovered combos enumerated in ledger Invariants)."`
  - For General/Shortlist: for each tactic field in `dynamicFields` + `sort_by`, build one `CASE` expression per lane via helper `tactic_sql_expression(...)` that mirrors `combine_role_scores` + `fit` penalties. Keep null-last: `ORDER BY (<expr> IS NULL) ASC, <expr> <dir>, players.uid ASC`.
  - Map each requested tactic column into `dynamic_values[fieldId]` as `Some(Integer(score_i64))` or `None`. For `Potential` with age fallback: expression uses `CASE WHEN players.age >= 29 THEN player_metrics.<current_col> ELSE player_metrics.<potential_col> END` for each of the two role reads before combine.
- `src-tauri/src/features/scoring/combine.rs` — no change, verified read-only.
- `src-tauri/src/features/planner/tactic.rs` — expose `base_position` as `pub(crate)` if not already (`pub(super)` today); make it `pub(crate)` so `fit.rs` and `query.rs` and Moneyball mapping share it. If visibility change is needed, keep it minimal and add a unit test that `base_position("DCR")=="DC"` holds in both modules.
- `src/utils/tactic-ids.ts` — add doc comment block referencing the Moneyball contract: `"88 Moneyball defs (77 mapped, 11 Moneyball-only), 103/111 General combos mapped, 8 unavailable by design"` and list the 8 combos for durable surface without a standalone commit. Single ownership for this doc comment per NITPICK (commit 7 must not duplicate it).
- `src-tauri/src/features/moneyball/role_catalog.rs` (extend existing tests, or `src-tauri/src/features/moneyball/tactic_mapping.rs` helper if extracted) — add regression proof for the deterministic Moneyball mapping contract (HIGH2): compound-key `(attribute_role_id, base_position)` uniqueness (no duplicate Moneyball def shares the same `(attribute_role_id, position_tag)` pair), 103/111 coverage (count of distinct General `(role_id, base_position)` combos that have a matching Moneyball def via that key), and exact 8 uncovered pairs (`holding_wing_back_oop`+`DL`, `holding_wing_back_oop`+`DR`, `pressing_wing_back_oop`+`DL`, `pressing_wing_back_oop`+`DR`, `box_to_box_midfielder_ip`+`MC`, `box_to_box_playmaker_ip`+`MC`, `deep_lying_playmaker_ip`+`MC`, `second_striker_ip`+`ST`) asserted as the precise set difference. Existing `role_catalog.rs` tests cover 88/77/11 but not compound uniqueness/coverage — these new tests close the gap. Keep the existing mapped-numeric and uncovered-NULL query tests as behavioural proof (see below); the catalog-level tests ensure the 103/111 + 8 invariant cannot silently drift.

**Behavior and data flow:**

- Example `tactic_current.goalkeeper` where lane = `goalkeeper` (GK/GK, weight 0.5): read `player_metrics.goalkeeper_ip` (current) + `player_metrics.line_holding_keeper_oop` (current OOP) as IP/OOP, combine 50/50, check `positions_json` has `GK >=12` both placements, check foot `any` -> no penalty, emit blended score 0..100 or NULL.
- For `tactic_potential.left_back`: read `player_metrics.potential_full_back_ip` + `player_metrics.potential_holding_full_back_oop` when `age <29`, else current cols; same foot/familiarity rules.
- Moneyball view: same lane weight/positions/foot, but IP/OOP role scores come from `moneyball_role_scores` cohort selected by the compound key; one uncovered combo yields `NULL`/`"—"`.

**Ordered implementation steps:**

1. Expose or reuse `base_position` as `pub(crate)` in `tactic.rs`; add compile guard asserting `TACTIC_LANE_IDS` alignment across TS/Rust.
2. Create `tactic_adjusted_score` pure function in `fit.rs` composing combine + penalties; add `fit_tests.rs` cases covering familiarity `<16` per phase, strict foot -> None, soft foot -5, saturation at 0.
3. Extend `resolver.rs` tactic `MetricSource` branch to produce a placeholder that `query.rs` replaces; keep `sql_expression_with_club_dna` for tactic returning a sentinel that `query.rs` detects and substitutes with the real CASE before building `select_sql` and `order_sql`.
4. In `query.rs` implement helper that, given `MetricField` tactic + `tactic: PlannerTactic`, returns the CASE expression and updates prerequisites. Ensure `player_metrics_join` predicates are already present for tactic columns. Include the 8-case `None` path for Moneyball.
5. Wire `dynamicFields` expansion to project each tactic column as one SELECT expression; wire sort expression for `SortField::Dynamic(tacticField)` with null-last and `players.uid ASC` tie-breaker.
6. Add Rust tests:
   - `fit.rs` cases for every penalty combination (both familiarities <16 => -10, plus foot soft => -15 -> saturates).
   - `query.rs` integration: ingest a fixture snapshot with 3 players via `ingest_dump_file` (use `memory_read/fixtures/golden_dump_v8.json` plus `prepare_player_derived` shape), call `search_players_in_view` with `requested_fields=[tactic_current.goalkeeper]` and assert one player's dynamicValue is `Some` blended and another with foot strict mismatch is `None`.
   - Moneyball path: two tests — one mapped combo yields numeric, one uncovered combo (`holding_wing_back_oop` at `DL` or `second_striker_ip` at `ST`) yields `NULL`/`"—"`, proving the mapping contract.
   - Mapping-contract regression (HIGH2, in `role_catalog.rs` or mapping helper): assert compound-key uniqueness (no duplicate `(attribute_role_id, position_tag)` among the 88 defs), 103/111 coverage, and exact 8 uncovered pairs enumerated above — deterministic set equality, not just count.

**Tests and proof:**

- New `fit_tests.rs` cases (positive + saturated). Integration query cases listed above; each expected RED before this commit is `unknown player metric: tactic_current.*` or `NULL` placeholder not sorting. GREEN asserts exact numeric score for at least one General `tactic_current` column and correct null-last sort, plus one uncovered Moneyball combo is `NULL`. The uncovered-`NULL` test is non-skippable per dispatch HIGH 2. Additionally, `role_catalog::tests` now prove compound-key uniqueness, 103/111 coverage, and exact 8 uncovered pairs — preventing silent catalog drift.

**Patterns to verify:**

- Existing `Club DNA` `LEFT JOIN` + `ORDER BY ... IS NULL` path as the null-last pattern to copy.
- `player_metrics_join` `require_score_model`/`require_projection_model` predicate builder — reuse without duplicating.
- Moneyball `search_players_with_roles` filtered vs full-CSV branching.
- `assert_read_models_complete` call before reading role metrics.

**Constraints and non-goals:**

- Do not add per-role indexes; rely on existing virtual paging and `SortField` SQL path.
- Do not change catalog or `VISIBLE_ATTRIBUTE_KEYS`; do not add tactic fields to moneyball metric catalog beyond the deterministic compound-key mapping.
- Do not make tactic columns groupable in OR filter AST.
- Keep one SQLite writer, one transaction semantics (applies only to read helper, not writes).

**Dependencies and sequencing:**

- Depends on commit 4.

**Validation:** `./scripts/dev check-rust` + `(cd src-tauri && cargo test query)` + `(cd src-tauri && cargo test fit)` + `./scripts/dev test` (ensures no frontend breakage from this backend-only commit).

**Stop conditions:** Strict foot check requires `preferred_foot` values outside `any/left/right/both` — abort if observed.

**Review mandate:**

1. SQL `CASE` does not interpolate WebView input (laneId allowlisted, role columns closed-catalog).
2. Familiarity base normalization matches `tactic.rs::base_position`, not frontend `basePosition` typo.
3. Penalties and saturation mirror `fit.rs` exactly (no off-by-5).
4. `age >= 29` fallback reads `players.age` column, not compact potential column for that row.
5. Moneyball tactic mapping uses `(attribute_role_id, base_position(placement))` compound key; missing-role -> `NULL` for the 8 uncovered combos, documented with inline comment and ledger invariant.
6. Sort `NULLS LAST` regardless of `ASC`/`DESC`, with `players.uid ASC` tie.
7. No new column in `player_role_metrics`; computation uses existing columns + `positions_json`/`preferred_foot` already in `players`.
8. Completeness guard fails before values are read (wrong-version compact row never leaks as NULL).

#### Commit 6 — Explicit save-scoped Planner tactic IPC, route sort gating, and no-snapshot composition

**Status:** Active

**Provisional commit:** `feat(search): scope tactic IPC by save and gate tactic sort`

**Work:** Replace the implicit-active-save Planner tactic and tactic-options reads plus `save_planner_tactic` mutation IPC with the project’s existing immutable save context `{ saveId, contextToken }` contract (analogue of `ClubDnaContext` from `src/features/club-dna/types/club-dna.ts` and `clubDnaKeys.definition` from `src/features/club-dna/api/club-dna-keys.ts`, validated in Rust by snapshot-owned `pub(crate) ensure_active_save_context(&Transaction, save_id, context_token)` at `src-tauri/src/features/snapshot/service.rs` (snapshot owns `saves` table) — `SELECT EXISTS(SELECT 1 FROM saves WHERE id=?1)` first distinguishes unknown numeric save (`Save {id} not found`), then `SELECT EXISTS(... WHERE id=?1 AND context_token=?2 AND is_active=1)` distinguishes existing-but-stale/inactive token (`Save changed or is no longer active`) inside exactly one `unchecked_transaction` under the single `Db` `Mutex` lock. One transaction owner per Rust command under that lock: commands open exactly one `tx = conn.unchecked_transaction()` after `db.0.lock()`, call `ensure_active_save_context(&tx, save_id, context_token)` first, then the planner transaction helpers, then `tx.commit()` exactly once on success — no nesting, no duplicate command-local validator, no `active_save_id` lookup. Planner transaction helpers preserve trusted public `Connection` wrappers/callers: define `pub(crate) fn load_or_initialize_tactic_in_tx(tx: &Transaction, save_id: i64) -> Result<PlannerTactic, String>` (checks `planner_tactic_lanes` existence, inserts `default_tactic()` via low-level `insert_default_in_tx` in same `tx` when missing; does not call `validate_tactic`) and `pub(crate) fn save_tactic_in_tx(tx: &Transaction, save_id: i64, tactic: &PlannerTactic) -> Result<(), String>` (writes all 11 lanes in same `tx`; does not duplicate `validate_tactic` nor `ensure_save_exists`). Trusted `pub fn get_tactic(conn: &Connection, save_id: i64)` retains `ensure_save_exists(&conn, save_id)?` exactly once before `let tx = conn.unchecked_transaction()?` then `load_or_initialize_tactic_in_tx(&tx, save_id)` then `tx.commit()` exactly once. Trusted `pub fn save_tactic(conn: &Connection, save_id: i64, tactic: &PlannerTactic)` retains `ensure_save_exists(&conn, save_id)?` and one `validate_tactic(&tactic)?` exactly once before writes, then `let tx = conn.unchecked_transaction()?` then `save_tactic_in_tx(&tx, save_id, &tactic)` (no duplicate validation) then `tx.commit()` exactly once. No `ensure_active_save_context` inside `tactic.rs` helpers — context validation is owned by commands at `src-tauri/src/features/snapshot/service.rs`. Commands own exactly one transaction: `let conn = db.0.lock()?; let tx = conn.unchecked_transaction()?; ensure_active_save_context(&tx, save_id, &context_token)?;` then `load_or_initialize_tactic_in_tx(&tx, _)` / `validate_tactic` once + `save_tactic_in_tx` / static `get_tactic_options()` then `tx.commit()` exactly once; no nesting, no duplicate validator, no `active_save_id` lookup. Frontend fetchers/query options/query keys/mutation (`fetchPlannerTactic`/`fetchPlannerTacticOptions`/`savePlannerTactic`, `plannerKeys.tactic`/`tacticOptions`, `plannerTacticQueryOptions`/`plannerTacticOptionsQueryOptions`, `PlannerTacticEditor` `save` mutation), Search/My Club callers, and editor cache writes all carry immutable `{ saveId, contextToken }` (exact camelCase, required/validated) for reads and mutation. Rust validates `saveId+contextToken+is_active` with those two distinct errors for reads and mutation; numeric ID reuse is isolated because query/mutation keys include `contextToken` (`{1,"tokenA"}` vs `{1,"tokenB"}` distinct, `contextA-recreated` also distinct). Frontend tactic/options fetching is component-owned non-throwing: initial tactic OR options `isError` with no cached `data` renders actionable `Could not load tactic` + `Retry` that refetches both context-scoped keys (`queryClient.refetchQueries({ queryKey: plannerKeys.tactic(context) })` and `queryClient.refetchQueries({ queryKey: plannerKeys.tacticOptions(context) })`), cached `isRefetchError` retains `data` read-only and blocks writes; mutation `save_planner_tactic` stale/inactive/unknown is rejected in same `tx` with same distinct errors (tested, including `PlannerTacticEditor` error surface and options-only recovery). Preserve existing `currentSnapshotQueryOptions`/`savesQueryOptions` loader error propagation and existing Define DNA `clubDnaQueryOptions` behavior — no `try/catch`/`Promise.all(...).catch(()=>null)` that swallows snapshot/saves/tactic context errors. Add layout-aware tactic sort validation and keep the filter strip mounted when no snapshot is present (no tactic fetch, no layout mutation). Header-X synthetic rendering/removal/recompaction is NOT in this commit (moved to Commit 7). This is an existing IPC signature change (reads + mutation, not a new command) and therefore changes the prior no-IPC packet boundary. This is a developer-authorized fresh planning cycle after the prior replan correction cap; no implementation is claimed.

**Size assessment:** ~275 non-test lines + ~155 tests (adds snapshot-owned `ensure_active_save_context` at `src-tauri/src/features/snapshot/service.rs` with distinct-error tests, `pub(crate) load_or_initialize_tactic_in_tx`/`save_tactic_in_tx` in `tactic.rs` preserving `get_tactic`/`save_tactic` wrappers that retain `ensure_save_exists` and one `validate_tactic` exactly once before `tx` (helpers do not duplicate validation) with one-transaction delegation, and end-to-end read + mutation `{ saveId, contextToken }` IPC including `save_planner_tactic`, plus non-null-only query option factories, single typed `TacticContextBoundary` (`{ tactic, options, isPending, initialError, refreshError, readOnly, retryBoth }` render-prop, mounted only with non-null matched context, unconditional hooks, no casts/sentinels) wrapping My Club subtree (Search wiring deferred to Commit 7 for smallest coherent sequencing), `mutationKey: ["planner","tactic", context]` + `savePlannerTactic({ saveId, contextToken, tactic })` via parent-owned `currentContextRef` with `contextKey` stale guard (stale A: no cache/no UI, B untouched), and boundary-driven `initialError`/`refreshError`/`readOnly`/`retryBoth` handling with options-only recovery). Slightly above the soft target but kept atomic because the snapshot-boundary validator, Rust `*_in_tx` helpers, read + mutation IPC signatures `{ saveId, contextToken }`, `plannerKeys`/`queryOptions`/`fetch`/`save` plumbing, `TacticContextBoundary` hooks ownership, mutation identity/stale guard, and route sort/no-snapshot wiring must land together to remain trunk-safe and to avoid a half-migrated IPC state.

**Out of scope:**

- Toggle button rendering, interleaving arithmetic, and all synthetic tactic column rendering/interleaving/re-compaction and tests (moved to Commit 7 — this commit is IPC/context, sort gating, no-snapshot composition only), Rust scorer beyond what commit 5 provides, store atomic action definition beyond what commit 3 provides, metric picker promotion, new IPC commands beyond the two existing tactic reads, client-side cancellation/AbortSignal wrappers.

**Implementation packet:**

**Files and responsibilities:**

- `src-tauri/src/features/snapshot/service.rs` — add reusable `pub(crate) fn ensure_active_save_context(tx: &Transaction<'_>, save_id: i64, context_token: &str) -> Result<(), String>` at the snapshot boundary (saves owned by snapshot). Under the command-held `Db` lock (`db.0.lock()` → `tx = conn.unchecked_transaction()`), first `SELECT EXISTS(SELECT 1 FROM saves WHERE id=?1)` → if false `Err(format!("Save {save_id} not found"))` (unknown numeric save), else `SELECT EXISTS(SELECT 1 FROM saves WHERE id=?1 AND context_token=?2 AND is_active=1)` → if false `Err("Save changed or is no longer active".to_string())` (existing-but-stale/inactive token). No `active_save_id` lookup. Reuse analogue is `src-tauri/src/features/club_dna/service.rs:139` but snapshot is the correct owner; club DNA remains a consumer.
- `src-tauri/src/features/planner/tactic.rs` — preserve trusted `pub fn get_tactic(conn: &Connection, save_id: i64) -> Result<PlannerTactic, String>` and `pub fn save_tactic(conn: &Connection, save_id: i64, tactic: &PlannerTactic) -> Result<(), String>` signatures and all Search/depth/tests callers (`src-tauri/src/features/search/query.rs:481,909`, `src-tauri/src/features/planner/depth.rs:542`, `src-tauri/src/features/planner/tactic.rs` tests, `depth_tests.rs`, `optimizer_tests.rs`, `role_reference_tests.rs`). Add `pub(crate) fn load_or_initialize_tactic_in_tx(tx: &Transaction<'_>, save_id: i64) -> Result<PlannerTactic, String>` (checks `planner_tactic_lanes` for `save_id`, inserts `default_tactic()` via low-level `insert_default_in_tx` in same `tx` when missing; does not call `validate_tactic` nor `ensure_save_exists`) and `pub(crate) fn save_tactic_in_tx(tx: &Transaction<'_>, save_id: i64, tactic: &PlannerTactic) -> Result<(), String>` (writes all 11 lanes in same `tx`; does not duplicate `validate_tactic` nor `ensure_save_exists`) plus internal `load_tactic_from_tx(tx, save_id)`. Trusted wrappers retain exactly once before `tx`: `get_tactic` does `ensure_save_exists(&conn, save_id)?` once, then `let tx = conn.unchecked_transaction()?; load_or_initialize_tactic_in_tx(&tx, save_id)?; tx.commit()?`; `save_tactic` does `ensure_save_exists(&conn, save_id)?; validate_tactic(&tactic)?` exactly once before `let tx = conn.unchecked_transaction()?; save_tactic_in_tx(&tx, save_id, &tactic)?; tx.commit()?`. Each wrapper commits its own one transaction exactly once, retains existing behavior for Search/depth/tests and guarantees no nesting and no duplicate validation. No `ensure_active_save_context` inside `tactic.rs` helpers — context validation is owned by commands at `src-tauri/src/features/snapshot/service.rs`.
- `src-tauri/src/features/planner/commands.rs` — change `get_planner_tactic(db: State<'_, Db>)` to `get_planner_tactic(save_id: i64, context_token: String, db: State<'_, Db>)`, `get_planner_tactic_options(save_id: i64, context_token: String, db: State<'_, Db>)`, and `save_planner_tactic(save_id: i64, context_token: String, tactic: PlannerTacticInputDto, db: State<'_, Db>)` to accept explicit immutable context `{ saveId, contextToken }` (exact camelCase, required). Each handler owns exactly one transaction under the `Db` `Mutex`: `let conn = db.0.lock()?; let tx = conn.unchecked_transaction()?; ensure_active_save_context(&tx, save_id, &context_token)?;` then `get_tactic_in_tx(&tx, save_id)` / `save_tactic_in_tx(&tx, save_id, &tactic)` / (for options) after `ensure_active_save_context` returns static `get_tactic_options()` — no nested transactions, no duplicate command-local validator, no `active_save_id` lookup. All three return distinct errors `Save {id} not found` (unknown numeric save) vs `Save changed or is no longer active` (stale/inactive token) from the single snapshot-owned validator.
- `src/features/planner/api/fetch-planner-tactic.ts`, `src/features/planner/api/fetch-planner-tactic-options.ts`, and `src/features/planner/api/save-planner-tactic.ts` — change to `export function fetchPlannerTactic({ saveId, contextToken }: { saveId: number, contextToken: string }): Promise<PlannerTactic>` / `fetchPlannerTacticOptions({ saveId, contextToken }): Promise<TacticOptions>` / `savePlannerTactic({ saveId, contextToken, tactic }: { saveId: number, contextToken: string, tactic: PlannerTactic }): Promise<PlannerTactic>` that call `invokeCommand("get_planner_tactic", { saveId, contextToken })` / `invokeCommand("get_planner_tactic_options", { saveId, contextToken })` / `invokeCommand("save_planner_tactic", { saveId, contextToken, tactic })` with required validated camelCase args. Delete `import { withAbortSignal }` and `src/lib/with-abort.ts` usage (remove file entirely). No `signal` param, no abort wrapper. Exact consumers after source inspection: `fetchPlannerTactic`, `fetchPlannerTacticOptions`, `savePlannerTactic` are the sole IPC callers for reads/mutation.
- `src/features/planner/api/planner-keys.ts` — change `tactic: (activeSaveId: number) => [...]` to `tactic: (context: { saveId: number, contextToken: string }) => [...plannerKeys.all, "tactic", { saveId: context.saveId, contextToken: context.contextToken }]` and `tacticOptions: (context: { saveId: number, contextToken: string }) => [...plannerKeys.all, "tactic-options", { saveId: context.saveId, contextToken: context.contextToken }]` (analogue of `clubDnaKeys.definition({ saveId, contextToken })` at `src/features/club-dna/api/club-dna-keys.ts:5`), no zero-arg overload, no `?? -1` sentinel, no alternate `activeSaveId` form. Numeric ID reuse isolation: `tactic({ saveId:1, contextToken:"a" })` vs `{ saveId:1, contextToken:"b" }` are distinct keys.
- `src/features/planner/api/planner-tactic-query-options.ts` and `src/features/planner/api/planner-tactic-options-query-options.ts` — change to `export const plannerTacticQueryOptions = (context: { saveId: number, contextToken: string }) => queryOptions({ queryKey: plannerKeys.tactic(context), queryFn: () => fetchPlannerTactic(context) })` and matching options helper `plannerTacticOptionsQueryOptions(context)` with `plannerKeys.tacticOptions(context)` / `fetchPlannerTacticOptions(context)` — non-null immutable context only, no `null`/`undefined` param, no sentinel `?? -1`, no `"none"` placeholder key, no internal `enabled`, no nullable factories, no casts, no sentinel keys/data. Query keys require context and queryFn captures only valid context. Inspect current TanStack use: `src/features/club-dna/api/club-dna-query-options.ts:13` shows `clubDnaQueryOptions(context: ClubDnaContext)` takes non-null context with `queryKey: clubDnaKeys.definition(context)` and `queryFn: () => getClubDna(context)`, no internal `enabled`; callers gate externally without constructing invalid keys. Planner follows same: factories accept non-null only.
- `src/features/planner/components/TacticContextBoundary.tsx` (new, owned by `src/features/planner`) — define one typed owned-subtree/render-prop `TacticContextBoundary` mounted only with non-null matched context: `function TacticContextBoundary({ context, children }: { context: { saveId: number; contextToken: string }, children: (props: { tactic: PlannerTactic | undefined; options: TacticOptions | undefined; isPending: boolean; initialError: unknown | null; refreshError: unknown | null; readOnly: boolean; retryBoth: () => void }) => ReactNode })`. Inside, unconditionally call `const tacticQuery = useQuery(plannerTacticQueryOptions(context))` and `const optionsQuery = useQuery(plannerTacticOptionsQueryOptions(context))` with non-null factories (no ternary hooks, no casts, no sentinel keys/data, no nullable factories, no `enabled` inside factories). Derive `isPending = tacticQuery.isPending || optionsQuery.isPending`, `initialError = (tacticQuery.isError && !tacticQuery.data) || (optionsQuery.isError && !optionsQuery.data) ? (tacticQuery.error ?? optionsQuery.error) : null`, `refreshError = (tacticQuery.isRefetchError || optionsQuery.isRefetchError) ? (tacticQuery.error ?? optionsQuery.error) : null`, `readOnly = !!refreshError` (cached data retained read-only blocks writes while refresh error persists), `retryBoth = () => { queryClient.refetchQueries({ queryKey: plannerKeys.tactic(context) }); queryClient.refetchQueries({ queryKey: plannerKeys.tacticOptions(context) }) }`. Parent never duplicates hooks, mirrors effects, casts, or uses sentinels; `TacticContextBoundary` is the single owner that calls both hooks and passes the typed render props to its child subtree. `skipToken` compatibility proof: `@tanstack/react-query@5.101.4` re-exports `skipToken` from `@tanstack/query-core` (verified in `node_modules/@tanstack/react-query/build/modern/_tsup-dts-rollup.d.ts:133` `import { skipToken } from '@tanstack/query-core'` and `:683` `export { skipToken }`, with `UnusedSkipTokenOptions` overload at `:647` accepting `queryFn: skipToken`). While `skipToken` is technically available, this plan prefers the boundary because it already proves no factory invocation with non-null types and keeps error/pending surfaces explicit; if `skipToken` were used it would be inside the boundary as `queryFn: skipToken` without casts, still behind the same mounted-only-when-context gate. `setQueryData(plannerKeys.tactic(context), ...)` and `mutationKey: ["planner","tactic", context]` likewise require non-null context.
- `src/features/search/utils/dynamic-columns.ts` — extend `isVisibleSortField(value, _filters, view)` plus companion `isTacticSortVisible(value, tableId)` to accept canonical tactic IDs `tactic_current.*` / `tactic_potential.*` only when the exact ID is present in `usePlayerTableStore.getState().layouts[tableId].columnIds` via `isValidTacticColumnId`. All other non-basic sorts still delegate to `getFilterField`. Keep the original `(value, filters, view)` fallback for non-tactic callers but ensure `validateSearch` uses the layout-aware path.
- `src/app/routes/search.tsx` — extend `validateSearch` to compute `tableIdForView(view)` and accept tactic `sort` only when `isValidTacticColumnId(sort) && layouts[tableId].columnIds.includes(sort)` (read via `usePlayerTableStore.getState()`; avoid render-loop). Invalid/removed/wrong-table tactic sorts fallback to `defaultSearchSort(view)`. Derive immutable save context `context = activeSave ? { saveId: activeSave.id, contextToken: activeSave.contextToken } : null` (from `SaveSummary` at `src/features/snapshot/types/save.ts:1` via `savesQuery`) and `isMatchedSnapshot = snapshot != null && context != null && snapshot.saveId === context.saveId` but defer `TacticContextBoundary` mount to Commit 7 — this commit proves the non-null-only factories exist (`plannerTacticQueryOptions(context)`/`plannerTacticOptionsQueryOptions(context)` with `plannerKeys.tactic(context)` require context, no `null`/`?? -1`/sentinel) via unit test without mounting a Search boundary or firing IPC. Refactor `if (!snapshot)` early-return to keep tabs + `SearchFilterBar` mounted above `Panel EmptyState No data loaded` (only `SearchResultsPanel` branches); filter strip + tabs remain present when active save context present + snapshot null/mismatch, zero `get_planner_tactic`/`get_planner_tactic_options`/`save_planner_tactic` IPC because no `TacticContextBoundary` is mounted for Search in this commit (verified via `getPlannerTacticFetchCount==0` and boundary-absent assertion). Labels derive from save tactic role selection (`PlannerTactic.lanes[].ipRoleId/oopRoleId`) + shared options catalog `TacticOptions.roles[].displayName` via `src/features/planner/types/tactic.ts` — not invented; actual label/toggle wiring via `TacticContextBoundary` render props lands in Commit 7.
- `src/app/routes/my-club.tsx` — migrate to immutable context: derive `activeTacticContext = activeSave ? { saveId: activeSave.id, contextToken: activeSave.contextToken } : null` and `isTacticContextMatchedSnapshot = snapshot != null && activeTacticContext != null && snapshot.saveId === activeTacticContext.saveId`; conditionally mount the single typed `TacticContextBoundary` wrapping the My Club tactic subtree (editor + definition) only when `activeTacticContext != null && isTacticContextMatchedSnapshot && isMatchedSettled` (settled = `!savesQuery.isFetching && !snapshotQuery.isFetching && !snapshotQuery.isError && !savesQuery.isError`). Inside `TacticContextBoundary` both queries run unconditionally and the typed render prop `{ tactic, options, isPending, initialError, refreshError, readOnly, retryBoth }` is passed to the child subtree; parent (`my-club.tsx`) does not duplicate hooks, mirror effects, cast, or use sentinels. Preserve `initialError` vs `refreshError` semantics for `Loading tactic…` vs `Could not load tactic` with `retryBoth` (initial error actionable `Retry` refetches both `plannerKeys.tactic(context)` and `plannerKeys.tacticOptions(context)`, `refreshError` retains cached data read-only and sets `readOnly=true` blocking writes). Loader preserves existing `currentSnapshotQueryOptions`/`savesQueryOptions` propagation — do not wrap tactic prefetch in `void Promise.all(...).catch(()=>null)` that swallows context errors; tactic is boundary-prefetched, not loader-swallowed. Update `planner-tactic-editor.tsx` to be keyed/remounted by full context key (`key={contextKey(context)}` or `key={`${context.saveId}:${context.contextToken}`}`) so a save/context switch unmounts prior editor state, and to thread `context` from the boundary.
- `src/features/planner/components/planner-tactic-editor.tsx` — bind mutation identity and variables to full immutable context: change to `useMutation({ mutationKey: ["planner", "tactic", context], mutationFn: (vars: { saveId: number; contextToken: string; tactic: PlannerTactic }) => savePlannerTactic(vars) })` where `savePlannerTactic({ saveId, contextToken, tactic })` requires exact camelCase `{ saveId, contextToken }` + `tactic` and is called with `mutate({ saveId: context.saveId, contextToken: context.contextToken, tactic: draft })`. Thread `context: { saveId: number; contextToken: string }` prop from `my-club.tsx` (or from `TacticContextBoundary` render prop) and make the component keyed/remounted by full context key (`key={contextKey(context)}` — analogue at `src/features/club-dna/components/club-dna-definition.tsx:67` — or `key={`${context.saveId}:${context.contextToken}`}`) so a switch `A→B` or same numeric ID with new token (`{1,"tokenA"}`→`{1,"tokenB"}`) unmounts prior editor state (draft, success/error UI, pending mutation). Parent (`my-club.tsx` or `TacticContextBoundary`) owns a `currentContextRef = useRef(context)` / `contextKey(context)` checked at settlement. Migrate `queryClient.setQueryData(plannerKeys.tactic(context), nextTactic)` to context-scoped key and invalidate with same context. Add stale result guard with exactly one policy: on settlement compare `contextKey(vars) === contextKey(currentContextRef.current)` (full `{ saveId, contextToken }` key, not `saveId` alone); if stale A's completion arrives after switch to B (or after same-ID token rotation), stale completion updates no cache and no draft/success/error UI — `queryClient.setQueryData` not called, `setDraft`/`setSuccess`/`setError` not called; B's cache/UI remain untouched. Current completion (`contextKey(vars) === contextKey(current)`) updates only the matching context key's cache (`plannerKeys.tactic(vars)`) and originating draft/success/error UI. Stale/inactive/unknown mutation rejected in same Rust `tx` with distinct errors `Save {id} not found` / `Save changed or is no longer active` and surfaced via `save.isError` for the originating context only.
- `src/app/routes/my-club-squad.test.tsx` (and any other `plannerKeys.tactic()` test consumers) — update `setQueryData`/`getQueryData`/`getQueryState` calls to `plannerKeys.tactic(context)` with fixture `CONTEXT_A = { saveId:1, contextToken:"save-token-a" }`, `CONTEXT_B`; add save-switch coverage there.
- `src/stores/use-player-table-store.ts` — no new width logic here (atomic action already in commit 3).
- `src/testing/setup.ts` — extend global `invoke` mock to validate exact camelCase `{ saveId, contextToken }` for `get_planner_tactic`/`get_planner_tactic_options`/`save_planner_tactic` (reject missing/wrong case, reject stale token / inactive save / unknown save via mock `ensure_active_save_context` analogue with distinct `Save {id} not found` vs `Save changed or is no longer active`, including mutation path `save_planner_tactic`), and reuse `src/testing/planner-ipc-mock.ts` context-keyed store. Delete global pending helpers.
- `e2e/tauri-ipc-stub.ts` — extend Playwright stub to handle `get_planner_tactic`/`get_planner_tactic_options`/`save_planner_tactic` with `{ saveId, contextToken }` (+ `tactic` for save) validation and context-keyed responses (fallback to `Save {id} not found` / `Save changed or is no longer active` for reads and mutation).
- `src/testing/planner-ipc-mock.ts` — update mocks to context-keyed store `Map<string, { tactic, options }>` keyed by `contextKey({ saveId, contextToken })` (analogue of `contextKey` at `src/features/club-dna/components/club-dna-definition.tsx:67`), `resolvePlannerTacticIpcMock(context)`/`resolvePlannerTacticOptionsIpcMock(context)`/`resolveSavePlannerTacticIpcMock({ saveId, contextToken, tactic })` assert exact `{ saveId, contextToken }` (+ `tactic` for save), `peekPlannerTactic(context)`, test-local delayed A/B via per-context `Deferred` inside test (not global `plannerTacticPending`/`pendingPlannerTacticDeferred` / `setPlannerTacticPending`/`resolvePending` shims which are deleted). Keep `peek` for direct inspection without fetch counting; support immutable context-keyed delayed responses for reads and mutation.
- `src/lib/with-abort.ts` — delete file (abort wrapper removed) and assert absence via fixed-string `rg -F "with-abort"` / `rg -F "withAbortSignal"` zero.

**Behavior and data flow:**

- IPC is now explicit immutable context for reads and mutation: `invoke("get_planner_tactic", { saveId, contextToken })`, `invoke("get_planner_tactic_options", { saveId, contextToken })`, and `invoke("save_planner_tactic", { saveId, contextToken, tactic })` enter Rust with immutable `{ saveId, contextToken }` (exact camelCase, required, mutation includes `tactic`). Each handler owns exactly one `unchecked_transaction` under the single `Db` `Mutex`: `let conn = db.0.lock()?; let tx = conn.unchecked_transaction()?; ensure_active_save_context(&tx, save_id, &context_token)?;` from snapshot-owned `ensure_active_save_context` at `src-tauri/src/features/snapshot/service.rs` (snapshot is the correct boundary; analogue formerly at `src-tauri/src/features/club_dna/service.rs:139`). That validator first checks unknown save (`Save {id} not found` distinct) then existing-but-stale/inactive token (`Save changed or is no longer active` distinct) inside the same `tx` before any read/default/mutation. Reads call `load_or_initialize_tactic_in_tx(&tx, save_id)` (default via low-level insert in same `tx`, one commit after success; `load_or_initialize` does not call `validate_tactic`); mutation `save_planner_tactic` does one `validate_tactic(&tactic)?` exactly once after `ensure_active_save_context` and before `save_tactic_in_tx` (helper does not duplicate validation) then `tx.commit()` exactly once; options validates in one read `tx` then returns static `get_tactic_options()` — no nested transactions, no duplicate command-local validator, no `active_save_id` lookup. Trusted wrappers retain `ensure_save_exists` and one `validate_tactic` exactly once before `tx` — `get_tactic` does `ensure_save_exists` once then `load_or_initialize_tactic_in_tx` + `commit`; `save_tactic` does `ensure_save_exists` + `validate_tactic` once then `save_tactic_in_tx` (no duplicate) + `commit`; `save_tactic_in_tx`/`load_or_initialize_tactic_in_tx` never duplicate validation. Query/mutation keys are `plannerKeys.tactic({ saveId, contextToken })` / `tacticOptions({ saveId, contextToken })` / mutation `save_planner_tactic` with same context (analogue `clubDnaKeys.definition` at `src/features/club-dna/api/club-dna-keys.ts:5`), so numeric ID reuse is isolated (`{1,"tokenA"}` vs `{1,"tokenB"}` and `contextA-recreated` distinct) for reads and mutation. Delayed A `{1,"tokenA"}` that resolves after switch to B `{2,"tokenB"}` populates only `plannerKeys.tactic({1,"tokenA"})` and is never read as B's tactic because active view queries `plannerKeys.tactic({2,"tokenB"})`; stale value discarded, not mislabeled. Mutation stale/inactive/unknown is rejected in the same `tx` with the same two distinct errors (`Save {id} not found` / `Save changed or is no longer active`).
- Fetching via one typed `TacticContextBoundary` (Rules of Hooks safe, no ternary hooks, no casts, no sentinel keys/data, no nullable factories, no mirrored effects) mounted only with non-null matched context: parent derives `context = activeSave ? { saveId, contextToken } : null` and `isMatchedSnapshot = snapshot != null && context != null && snapshot.saveId === context.saveId`, then conditionally mounts `<TacticContextBoundary context={context}>{ (props) => <ChildSubtree {...props} /> }</TacticContextBoundary>` only when `isMatchedSnapshot && isMatchedSettled`. Inside the boundary both `useQuery(plannerTacticQueryOptions(context))` and `useQuery(plannerTacticOptionsQueryOptions(context))` are called unconditionally with non-null factories and the typed render prop `{ tactic, options, isPending, initialError, refreshError, readOnly, retryBoth }` is passed to the child subtree; parent never duplicates hooks. For reads: `isPending` true shows `Loading…`, `initialError` (no cached `data`) renders actionable `Could not load tactic` + `retryBoth` (`queryClient.refetchQueries` for both `plannerKeys.tactic(context)` and `plannerKeys.tacticOptions(context)`), `refreshError` with cached `data` retains data read-only, sets `readOnly=true` and blocks writes (save/toggle disabled via `readOnly`/`mutationKey` check and editor guard), covers options-only recovery (tactic succeeds, options fails → `retryBoth` recovers). Query factories are non-null-only (`plannerTacticQueryOptions(context: {saveId, contextToken})` with `queryKey: plannerKeys.tactic(context)` and `queryFn: () => fetchPlannerTactic(context)`). Commit 6 mounts this boundary only for My Club wrapping the editor subtree; Search fetch wiring is deferred to Commit 7 (Search boundary wrapping `TacticColumnToggles` + `SearchResultsPanel`) — Commit 6 proves Search factories via unit test without mounting a Search boundary, so no Search IPC fires. Commit 7 reuses the same `TacticContextBoundary` for Search: `search.tsx` mounts it around `TacticColumnToggles` + panel subtree and consumes the same render props for `laneLabels`/`onToggle`/`validationError`/`readOnly`. `savePlannerTactic` uses `mutationKey: ["planner","tactic", context]` with variables `({ saveId, contextToken, tactic })` threaded from the boundary/parent-owned `currentContextRef`; stale policy: stale A completion updates no cache and no draft/success/error UI, B cache/UI untouched — `onSuccess`/`onError` compare `contextKey(vars) === contextKey(currentContextRef.current)` and discard if mismatched, never calling `setQueryData`/`setDraft`/`setError`. Current completion updates only the matching context key's cache (`plannerKeys.tactic(vars)`). Preserve existing `currentSnapshotQueryOptions`/`savesQueryOptions` loader error propagation (initial snapshot/saves `isError` propagates to `useSuspenseQuery` error boundaries / `savesQuery.isError` UI, no swallowed `try/catch`), and Define DNA non-null pattern — loaders keep tabs/filter bar mounted while only `SearchResultsPanel` branches, and boundary unmounted => zero IPC.
- URL entry with `?sort=tactic_current.goalkeeper` while that ID is not in `layouts[search].columnIds` (or is for `moneyball-search` while view is `general`) -> `validateSearch` rejects -> route navigates with `defaultSearchSort(view)` fallback, never issuing a tactic SQL sort that would error.
- Save switch `A {1,"tokenA"} -> B {2,"tokenB"}` -> `context` changes -> `plannerKeys.tactic(contextB)` cache miss -> fetch for B runs; `tacticQuery` for A remains cached but inactive and mutation `savePlannerTactic` results are context-scoped via `mutationKey: ["planner","tactic", context]` and stale guard (delayed A completion does not write `plannerKeys.tactic(contextB)` nor update B's draft/success/error). Editor is keyed by full context key `contextKey(context)` so it remounts on switch (including same numeric ID/new token `{1,"tokenA"}`→`{1,"tokenB"}`) and draft/error/success state does not leak. Labels update for new context (`laneLabels` from `B`'s `PlannerTactic` lanes + `TacticOptions.roles[].displayName` catalog), while table-scoped `columnIds`/`widths` are preserved across save switches (commit 7 explicitly asserts). Layout not mutated on save switch.
- Active immutable save context present + snapshot null/mismatch: `saves=[{id:1, contextToken:"save-token-a", isActive:true}]` valid, `snapshot==null` or `snapshot.saveId !== context.saveId`, `context={ saveId:1, contextToken:"save-token-a" }` exists but no matched snapshot — `TacticContextBoundary` (My Club in Commit 6; Search in Commit 7) is not mounted because `!isMatchedSnapshot`, so zero `get_planner_tactic`/`get_planner_tactic_options`/`save_planner_tactic` IPC fires, filter strip (`SearchFilterBar`) + tabs remain present above `Panel EmptyState No data loaded` (only `SearchResultsPanel` branches). In Commit 6 Search has no boundary yet, so the same zero-IPC is proved via factory-absent + boundary-absent assertions and `SearchFilterBar` presence; My Club proves boundary unmounted. Gated toggle click (Commit 7) when active save context present + snapshot null/mismatch shows `"No snapshot loaded — use Load Data"` via the boundary-less gated handler without mutating layout; Commit 6 limits proof to no read IPC + `SearchFilterBar` presence + no layout mutation + no save IPC, without asserting toggle UI (toggle UI lands in Commit 7) to avoid scope contradiction.
- No abort wrapper: `src/lib/with-abort.ts` deleted; cancellation relies on Query's built-in `signal` or discarding stale context keys, not a custom `withAbortSignal` promise wrapper. My Club/Search preserve existing `isFetching`/`isError`/`isRefetchError` pending/error surfaces with `Retry`; no `try/catch` in loaders that swallows `snapshot`/`saves`/`tactic` errors into non-actionable `Loading…` state.

**Ordered implementation steps:**

1. Add snapshot-owned `pub(crate) fn ensure_active_save_context(tx: &Transaction<'_>, save_id: i64, context_token: &str) -> Result<(), String>` to `src-tauri/src/features/snapshot/service.rs` with two-step distinguish: `SELECT EXISTS(SELECT 1 FROM saves WHERE id=?1)` → `Save {id} not found` vs `SELECT EXISTS(... WHERE id=?1 AND context_token=?2 AND is_active=1)` → `Save changed or is no longer active`, both inside the single `unchecked_transaction` under the `Db` `Mutex`; add tests `cargo test snapshot::service` for unknown vs stale/inactive (including inactive `is_active=0`). In `src-tauri/src/features/planner/tactic.rs` preserve `pub fn get_tactic(conn: &Connection, save_id)` / `pub fn save_tactic(conn: &Connection, save_id, &tactic)` and add `pub(crate) fn load_or_initialize_tactic_in_tx(tx: &Transaction<'_>, save_id: i64) -> Result<PlannerTactic, String>` (checks existence, inserts `default_tactic()` via low-level `insert_default_in_tx` in same `tx` when missing; does not call `validate_tactic` nor `ensure_save_exists`) and `pub(crate) fn save_tactic_in_tx(tx: &Transaction<'_>, save_id: i64, tactic: &PlannerTactic) -> Result<(), String>` (writes all 11 lanes in same `tx`; does not duplicate `validate_tactic` nor `ensure_save_exists`) plus `load_tactic_from_tx`; trusted wrappers each retain exactly once before `tx` (`get_tactic` does `ensure_save_exists` once then `load_or_initialize_tactic_in_tx` + `commit`; `save_tactic` does `ensure_save_exists` + `validate_tactic` once then `save_tactic_in_tx` (no duplicate) + `commit`), committing exactly once on success, so Search/depth/tests callers unchanged, no nesting, no duplicate validation. Then extend `src-tauri/src/features/planner/commands.rs` to `get_planner_tactic(save_id: i64, context_token: String, db: State<'_, Db>)` / `get_planner_tactic_options(save_id: i64, context_token: String, db: State<'_, Db>)` / `save_planner_tactic(save_id: i64, context_token: String, tactic: PlannerTacticInputDto, db: State<'_, Db>)` — each owns exactly one transaction: `let conn = db.0.lock()?; let tx = conn.unchecked_transaction()?; ensure_active_save_context(&tx, save_id, &context_token)?;` then `load_or_initialize_tactic_in_tx(&tx, save_id)` / one `validate_tactic` + `save_tactic_in_tx(&tx, save_id, &tactic)` (no duplicate) / static `get_tactic_options()` after validation, then `tx.commit()?` exactly once; no nested transactions, no duplicate validator, no `active_save_id`. Update `fetch-planner-tactic.ts`/`fetch-planner-tactic-options.ts`/`save-planner-tactic.ts` to `fetchPlannerTactic(context: {saveId, contextToken})`/`fetchPlannerTacticOptions(context)`/`savePlannerTactic({ saveId, contextToken, tactic })` with exact camelCase required args; delete `src/lib/with-abort.ts` and remove `withAbortSignal` imports. Update `planner-keys.ts` to `tactic(context: {saveId, contextToken})`/`tacticOptions(context)` (analogue `clubDnaKeys.definition`) with distinct keys per immutable `{ saveId, contextToken }`; create `src/features/planner/components/TacticContextBoundary.tsx` as defined above and migrate every consumer enumerated after source inspection (`src/features/planner/api/*`, `src/features/planner/components/TacticContextBoundary.tsx`, `src/features/planner/components/planner-tactic-editor.tsx`, `src/app/routes/my-club.tsx`, `src/testing/setup.ts`, `src/testing/planner-ipc-mock.ts`, `e2e/tauri-ipc-stub.ts`; `src/app/routes/search.tsx` wiring deferred to Commit 7 for the Search boundary).
2. Update `planner-tactic-query-options.ts`/`planner-tactic-options-query-options.ts` to `export const plannerTacticQueryOptions = (context: { saveId: number, contextToken: string }) => queryOptions({ queryKey: plannerKeys.tactic(context), queryFn: () => fetchPlannerTactic(context) })` and matching options helper — non-null immutable context only (no `null`/`undefined`/`?? -1`/"none" placeholder, no nullable factories, no casts, no sentinel keys/data), query keys require context, no internal `enabled` or sentinel. Create `src/features/planner/components/TacticContextBoundary.tsx` as the single typed boundary that unconditionally calls both `useQuery` hooks and passes `{ tactic, options, isPending, initialError, refreshError, readOnly, retryBoth }` to its render-prop child; parent never duplicates hooks. Verify fixed-string `rg -F "plannerKeys.tactic" -g "*.ts" -g "*.tsx" src` shows only `plannerKeys.tactic({ saveId, contextToken })` context-keyed calls, `rg -F "plannerKeys.tactic()" -g "*.ts" -g "*.tsx" src` is zero (no zero-arg), `rg -F "?? -1" -g "*.ts" -g "*.tsx" src/features/planner` is zero (no sentinel), `rg -F "plannerKeys.tactic({" -g "*.ts" -g "*.tsx" src` shows contextToken, `rg -F "as unknown as PlannerTactic" -g "*.ts" -g "*.tsx" src` is zero and `rg -F "as any" -g "*.ts" -g "*.tsx" src/features/planner` has no tactic casts, and `rg -F "withAbortSignal" -g "*.ts" -g "*.tsx" src`, `rg -F "with-abort" -g "*.ts" -g "*.tsx" src`, `rg -F "pendingPlannerTacticDeferred" -g "*.ts" -g "*.tsx" src`, `rg -F "setPlannerTacticPending" -g "*.ts" -g "*.tsx" src` are zero (obsolete abort/global pending helpers absent). Also verify no ternary hook pattern: `rg -F "? useQuery" -g "*.ts" -g "*.tsx" src` shows zero tactic ternary hooks, `rg -F "[\"planner\",\"tactic\",\"disabled\"]" -g "*.ts" -g "*.tsx" src` zero sentinel keys, and `rg -F "TacticContextBoundary" -g "*.ts" -g "*.tsx" src` shows the single boundary definition plus My Club mount in Commit 6 and absent Search mount (Search deferred to Commit 7, verified via `rg -F "TacticContextBoundary" -g "*.tsx" src/app/routes/search.tsx` zero in Commit 6).
3. Extend `dynamic-columns.ts` with the layout-aware tactic branch and add `isTacticSortVisible(value, tableId)` used by route; wire `validateSearch` to accept tactic sort only when `layouts[tableId].columnIds.includes(sort)` (exact `isValidTacticColumnId` allowlist). Labels source is save tactic role selection + shared `TacticOptions` catalog (no invented helper).
4. Refactor `search.tsx` `validateSearch` + `isMatchedSettled` to use immutable `context = activeSave ? { saveId: activeSave.id, contextToken: activeSave.contextToken } : null` and defer `TacticContextBoundary` mount to Commit 7: in this commit keep `validateSearch` layout-aware and keep tabs + `SearchFilterBar` mounted above `Panel EmptyState No data loaded` (only `SearchResultsPanel` branches) when active save context present + snapshot null/mismatch, but do not mount `TacticContextBoundary` for Search yet — prove `plannerTacticQueryOptions`/`plannerTacticOptionsQueryOptions` non-null factories via unit test and that no `get_planner_tactic`/`get_planner_tactic_options`/`save_planner_tactic` IPC fires via boundary-absent + fetch-count-zero assertions (`getPlannerTacticFetchCount(context)==0` etc., not global counters) and `rg -F "TacticContextBoundary"` shows no Search mount in this commit. Commit 7 will mount `<TacticContextBoundary context={context}>` around the Search header/toggle/panel subtree when `isMatchedSnapshot && isMatchedSettled`, passing `{ tactic, options, isPending, initialError, refreshError, readOnly, retryBoth }` to derive `laneLabels`/`onToggle`/`readOnly`. Ensure search loader preserves `currentSnapshotQueryOptions`/`savesQueryOptions` error propagation (initial snapshot `isError` shows `Could not load players` / `Loading…` via `useSuspenseQuery` boundary, not swallowed to `null`), no `try/catch` returning `null` or `Promise.all(...).catch(()=>null)` swallowing, and no `withAbortSignal`. Also assert no ternary hook pattern, no `as unknown as PlannerTactic`, no `["planner","tactic","disabled"]`. Preserve Define DNA `clubDnaQueryOptions(context)` non-null pattern.
5. Refactor `my-club.tsx` to mount the single typed `TacticContextBoundary` only when `activeTacticContext != null && isTacticContextMatchedSnapshot && isMatchedSettled` (`snapshot != null && snapshot.saveId === context.saveId` plus settled = `!savesQuery.isFetching && !snapshotQuery.isFetching && !snapshotQuery.isError && !savesQuery.isError`). The boundary unconditionally calls both `useQuery` hooks and passes `{ tactic, options, isPending, initialError, refreshError, readOnly, retryBoth }` to its child subtree wrapping `PlannerTacticEditor` + definition; parent never duplicates hooks. Preserve `isPending`/`initialError` vs `refreshError`/`readOnly` distinction: initial tactic OR options `initialError` (no cached `data`) shows `Could not load tactic` + actionable `retryBoth` that refetches both `plannerKeys.tactic(context)` and `plannerKeys.tacticOptions(context)`, cached `refreshError` with `data` retains data read-only, sets `readOnly=true` and blocks writes (`savePlannerTactic` disabled via `readOnly`/mutation guard). `PlannerTacticEditor` receives `context` from the boundary render prop, is keyed/remounted by full context key (`key={contextKey(context)}`), and uses `useMutation({ mutationKey: ["planner","tactic", context], mutationFn: (vars: { saveId, contextToken, tactic }) => savePlannerTactic(vars) })` called as `mutate({ saveId: context.saveId, contextToken: context.contextToken, tactic: draft })`; parent-owned `currentContextRef` (`useRef(context)` synced to latest `contextKey`) is checked at settlement (`contextKey(vars) === contextKey(currentContextRef.current)`) with exactly one stale policy: stale A completion updates no cache and no draft/success/error UI, B cache/UI untouched; current completion updates only `plannerKeys.tactic(vars)`. Add `onSuccess`/`onError` guards that discard stale A's `setQueryData`/`setDraft`/`setError`. No `void Promise.all(...).catch(()=>null)` swallowing; loader preserves `currentSnapshotQueryOptions`/`savesQueryOptions` propagation (no swallow). Update `my-club-squad.test.tsx` to `plannerKeys.tactic(context)` with `{ saveId, contextToken }` fixtures, mutation variable assertions (`{ saveId, contextToken, tactic }`) and `mutationKey` assertions, context-keyed editor remount tests, and `initialError`/`refreshError`/`readOnly`/`retryBoth` tests including options-only recovery.
6. Update `src/testing/planner-ipc-mock.ts` to context-keyed `Map` via `contextKey({ saveId, contextToken })` (analogue `src/features/club-dna/components/club-dna-definition.tsx:67`), `resolvePlannerTacticIpcMock(context)` validates exact camelCase args and `ensure_active_save_context` analogue (reject stale/inactive/unknown), and supports immutable context-keyed delayed A/B via test-local `Deferred` per context (no global `plannerTacticPending` / `pendingPlannerTacticDeferred` / `setPlannerTacticPending` helpers). Update `src/testing/setup.ts` global invoke mock and `e2e/tauri-ipc-stub.ts` Playwright stub with same validation/context-keyed responses. Delete `src/lib/with-abort.ts`.
7. Run focused tests: unit for `isVisibleSortField`/`isTacticSortVisible`, route URL sort hydration/fallback/save-switch with context-keyed labels (`PlannerTactic` + `TacticOptions` catalog), `my-club-squad` context-keyed key migration, `PlannerTacticEditor` save mutation success and stale/inactive/unknown `save_planner_tactic` rejection in same `tx`, and the new REDs below (A/B crossing with context for reads + mutation, backend context validation/rejections for reads and mutation, query-key isolation with `contextToken` for reads and mutation, no-snapshot suppression, existing-contract retention with `isRefetchError`). Header-X recompaction is tested in Commit 7, not here.

**Tests and proof:**

Exact REDs required (all must fail before this commit):

- `isVisibleSortField` unit (existing `dynamic-columns.test.ts`): valid tactic ID `tactic_current.goalkeeper` present in `layouts[search].columnIds` -> `true`; same ID not in layout -> `false`; same ID present in `moneyball-search` but view `general` -> `false`; `isTacticSortVisible("tactic_current.not_a_lane", "search")` -> `false`; non-tactic `role.deep_lying_playmaker_ip` still requires `getFilterField`. RED: `isVisibleSortField("tactic_current.goalkeeper", [], "general")` incorrectly `false` or unconditionally `true`.
- Route URL sort validation: seed `localStorage` with `search: { columnIds: [..., "tactic_current.goalkeeper"] }`, navigate `/search?sort=tactic_current.goalkeeper` -> stays; `?sort=tactic_current.not_a_lane` -> falls back to `defaultSearchSort(view)`; invalid wrong-table tactic sort also falls back. Proves sort never reaches Rust when not in layout. Header-X sort fallback is tested in Commit 7, not here.
- A/B crossing with immutable context (save-scoping) RED — IPC/context-keyed caches/stale isolation only at My Club (the only Commit 6 `TacticContextBoundary` consumer); Search asserts zero tactic IPC/cache activity through Commit 6 (visible labels remain Commit 7): use fixtures `contextA={ saveId:1, contextToken:"save-token-a" }` / `contextB={ saveId:2, contextToken:"save-token-b" }` and `contextA-recreated={ saveId:1, contextToken:"save-token-a2" }`. Render My Club with `saves=[{id:1, contextToken:"save-token-a", isActive:true}]` + `snapshot.saveId=1`, mock `fetchPlannerTactic(contextA)` with test-local `Deferred` per immutable `{ saveId, contextToken }` (not global `plannerTacticPending`). Switch immutable context `contextA {1,"tokenA"} -> contextB {2,"tokenB"}` before A resolves. Assert `invoke("get_planner_tactic", { saveId:1, contextToken:"save-token-a" })` with exact camelCase `contextToken` and not reused for B (and likewise `get_planner_tactic_options`), `queryClient.getQueryData(plannerKeys.tactic(contextA))` vs `plannerKeys.tactic(contextB)` distinct and vs `contextA-recreated` distinct (numeric ID reuse isolation via `contextToken`), and resolving stale A does not overwrite `plannerKeys.tactic(contextB)` nor `plannerKeys.tacticOptions(contextB)` caches. Assert Search route renders with zero `get_planner_tactic`/`get_planner_tactic_options`/`save_planner_tactic` IPC and `queryClient.getQueryData(plannerKeys.tactic(anySearchContext))` remains `undefined` (no Search cache activity, `rg -F "TacticContextBoundary" -g "*.tsx" src/app/routes/search.tsx` shows no mount in Commit 6). Assert `rg -F "contextToken" -g "*.ts" -g "*.tsx" src/features/planner/api` shows exact camelCase. No assertion on header labels here — labels prove in Commit 7.
- Backend save context validation (Rust) via snapshot-owned `pub(crate) ensure_active_save_context(&Transaction, save_id, context_token)` at `src-tauri/src/features/snapshot/service.rs` under exactly one `unchecked_transaction` per command held under single `Db` `Mutex` with one transaction owner per command: `get_planner_tactic({ saveId:1, contextToken:"tokenA" })` when active `{1,"tokenA"}` succeeds; `get_planner_tactic({ saveId:1, contextToken:"stale-token" })` with stale token -> `Err("Save changed or is no longer active")` (existing-but-stale/inactive token distinct); `get_planner_tactic({ saveId:999, contextToken:"x" })` -> `Err("Save 999 not found")` (unknown numeric save distinct); inactive save `{ saveId:1, contextToken:"tokenA" }` where `is_active=0` -> `Err("Save changed or is no longer active")`; `get_planner_tactic_options` same distinct rejections; `save_planner_tactic({ saveId:1, contextToken:"stale", tactic })` / unknown / inactive also rejected in same `tx` with same distinct errors — one `validate_tactic` exactly once after `ensure_active_save_context` and before `save_tactic_in_tx` (helper does not duplicate validation) then `tx.commit()` exactly once (mutation path, including `PlannerTacticEditor` `save` error surface via `save.isError`, proved via `cargo test --lib -- snapshot` and `tactic` distinct-error tests plus `save_planner_tactic` integration tests). Also proves transaction design: trusted `get_tactic(&Connection, save_id)` retains `ensure_save_exists` once before `tx` then `load_or_initialize_tactic_in_tx` (no validation duplicate) + `commit`; trusted `save_tactic(&Connection, save_id, &tactic)` retains `ensure_save_exists` + one `validate_tactic` exactly once before `tx` then `save_tactic_in_tx` (no duplicate) + `commit`, each wrapper commits its own one transaction exactly once, no nesting; commands own exactly one `tx` (`db.0.lock()` → `tx = conn.unchecked_transaction()` → `ensure_active_save_context` → `load_or_initialize_tactic_in_tx` / one `validate_tactic` + `save_tactic_in_tx` (no duplicate) / static `get_tactic_options` after validation → `tx.commit()` exactly once) — no nested transactions, no duplicate validator, no `active_save_id`.
- Query-key isolation: unit for `plannerKeys.tactic(contextA)` vs `plannerKeys.tactic(contextB)` inequality and vs `contextA-recreated` inequality (numeric ID reuse), and `plannerTacticQueryOptions(contextB).queryKey` contains `{ saveId:2, contextToken:"save-token-b" }`; `fetchPlannerTactic(contextB)` calls `invokeCommand` with exact `{ saveId:2, contextToken:"save-token-b" }` (spy via `invokeCommand` mock in `src/testing/setup.ts` and `e2e/tauri-ipc-stub.ts`), not bare or snake_case.
- TacticContextBoundary fetch errors: `initialError` with no cached `data` (`tacticQuery.isError && !data` or `optionsQuery.isError && !data`) renders actionable `Could not load tactic` + `retryBoth` (`queryClient.refetchQueries({ queryKey: plannerKeys.tactic(context) })` and `plannerKeys.tacticOptions(context)`), including options-only recovery where `tactic` succeeds and `options` fails — test asserts `tacticQuery.data` defined, `tacticOptionsQuery.data` undefined before `retryBoth`, after `retryBoth` both succeed and `initialError` clears. Cached `refreshError` with cached `data` retains data read-only, sets `readOnly=true` and blocks writes (save/toggle disabled via `readOnly`, no `savePlannerTactic` dispatched while `refreshError`/`readOnly`). Boundary unconditionally calls both `useQuery` hooks; parent never duplicates hooks. Test uses test-local `Deferred` per immutable context for both tactic and options, not global pending helpers, and asserts `TacticContextBoundary` render prop receives `{ isPending, initialError, refreshError, readOnly, retryBoth }` typed.
- Active immutable save context present + snapshot null/mismatch (proves boundary unmounted, zero IPC, filter/tabs present): mount Search with `saves=[{id:1, contextToken:"save-token-a", isActive:true}]` (valid active save context `contextA` present) but `snapshot==null` or `snapshot.saveId !== contextA.saveId`, assert `SearchFilterBar` + tabs remain in DOM, `Panel EmptyState No data loaded` shows (only `SearchResultsPanel` branches), `TacticContextBoundary` not mounted for Search (deferred to Commit 7, verified via `rg -F "TacticContextBoundary"` boundary-absent + factory-absent) and for My Club (`my-club.tsx` boundary) not mounted because `!isMatchedSnapshot` — so `getPlannerTacticFetchCount(contextA)==0` / `getPlannerTacticOptionsFetchCount==0` / `getSavePlannerTacticCount==0`, no `invoke("get_planner_tactic")`/`get_planner_tactic_options`/`save_planner_tactic`, no `plannerKeys.tactic(context)` fetch, `isPending`/`initialError` not rendered, and `usePlayerTableStore.getState().layouts[tableId].columnIds` unchanged (no layout mutation). Filter bar presence asserted here (Commit 6). Gated toggle click (Commit 7) when active save context present + snapshot null/mismatch shows `"No snapshot loaded — use Load Data"` via gated handler without mutating layout — proven in Commit 7 together with zero IPC; Commit 6 limits proof to filter/tabs present + boundary unmounted + zero IPC + no layout mutation, without asserting toggle UI (toggle UI lands in Commit 7) to avoid scope contradiction. Also proves no swallowing of `snapshot`/`saves` errors (initial `currentSnapshotQueryOptions` `isError` still propagates to error boundary, not `null`). In My Club boundary mount condition is `context != null && isMatchedSnapshot && isMatchedSettled`; `snapshot==null` alone prevents mount, so `initialError`/`refreshError` not shown, `readOnly` false, no IPC.
- Existing-contract retention (My Club + Search): My Club with `savesQuery.isFetching==true` or `tacticQuery.isError==true` (no cached data) shows `Loading tactic…` / `Could not load tactic` with actionable `Retry` (`queryClient.refetchQueries`); with cached `data` and `isRefetchError==true` shows read-only cached tactic with background refresh (retains data). Search with `currentSnapshotQueryOptions` `isError` still shows `Could not load players` / `Loading player results…` per Search boundaries and `clubDnaQueryOptions(context)` `isError` still shows Define DNA error. Assert no new `try/catch` in loaders that converts `snapshot`/`saves` errors to `null` empty state without actionable error. Plus delete `src/lib/with-abort.ts` — fixed-string `rg -F "with-abort"`, `rg -F "withAbortSignal"`, `rg -F "pendingPlannerTacticDeferred"`, `rg -F "setPlannerTacticPending"` all zero (obsolete abort/global pending helpers absent). Also assert `QueryClient` API is `useQueryClient().getQueryData` / `getQueryState` / `refetchQueries` (not deprecated `QueryClient` singleton) and `e2e/tauri-ipc-stub.ts` stub returns context-keyed `{ saveId, contextToken }` validated responses.
- Mutation identity and variables bound to full context (stale guard, keyed editor, `savePlannerTactic({context,tactic})`): assert `useMutation` has `mutationKey: ["planner","tactic", contextA]` (read via `queryClient.getMutationCache().find({ mutationKey: ["planner","tactic", contextA] })` or spy), `savePlannerTactic` variables are `({ saveId: contextA.saveId, contextToken: contextA.contextToken, tactic: draft })` with exact camelCase and not bare `tactic`; editor component is keyed by full context key (`key={contextKey(context)}`, analogue `contextKey` at `src/features/club-dna/components/club-dna-definition.tsx:67`) so it remounts on `contextA→contextB` and on same numeric ID/new token `{1,"tokenA"}`→`{1,"tokenB"}` (assert draft/success/error reset, prior `save.isError` not shown for new context). Add delayed A→B mutation proof: start `savePlannerTactic({ saveId:1, contextToken:"tokenA", tactic: draftA })` with test-local `Deferred` per immutable context, switch to `contextB={ saveId:2, contextToken:"tokenB" }` (or `{1,"tokenB"}` for same-ID rotation) before A resolves, assert `getSavePlannerTacticCount(contextA)==1` / `contextB==0`, then resolve delayed A; assert stale result guard discards: `queryClient.getQueryData(plannerKeys.tactic(contextB))` remains `undefined` (not `draftA`), `queryClient.getQueryData(plannerKeys.tactic(contextA))` remains `undefined` (no cache update for stale completion, both preexisting A and B caches exactly unchanged), and editor for B still shows `save.isPending==false`, `save.isError==false`, `saveSucceeded==false`, draft unchanged (not updated to `draftA`). Add same numeric ID/new token mutation proof: `contextA={1,"tokenA"}` → `contextA2={1,"tokenA2"}`: start mutation for A, rotate token, resolve A stale; assert `plannerKeys.tactic(contextA2)` not updated, new mutation for `contextA2` with new `tactic` succeeds and writes only `plannerKeys.tactic(contextA2)`. Both cases assert exact `mutationKey` contains `contextToken` and `savePlannerTactic` invocation carries `{ saveId, contextToken, tactic }` (spy via `invokeCommand` mock, exact camelCase, not `saveId` alone). Expected RED before this commit is `mutationKey` lacks `contextToken`, `savePlannerTactic(tactic)` without context, editor not keyed (draft leaks across context), and stale A completion overwrites `plannerKeys.tactic(contextB)`/editor success UI.
- Expected RED before this commit: `isVisibleSortField("tactic_current.goalkeeper", [], "general")` wrong, `plannerKeys.tactic({ saveId, contextToken })` collision across tokens (numeric ID reuse not isolated), `fetchPlannerTactic` invoked without `contextToken` or with snake_case, `context==null` or `snapshot==null` with valid save context still invokes `get_planner_tactic` (child not gated), ternary hook `context ? useQuery(...) : useQuery(...)` present, `as unknown as PlannerTactic` or sentinel `["planner","tactic","disabled"]` present, `rg -F "withAbortSignal"` finds file, `rg -F "pendingPlannerTacticDeferred"` finds global helper, initial tactic `isError` shows non-actionable `Loading…` without `Retry`, cached `isRefetchError` clears data, and `mutationKey` not context-scoped / stale A overwrites B.

**Patterns to verify:**

- Existing `validateSearch` pattern for `isVisibleSortField` + `defaultSearchSort` fallback — extend, not replace.
- Existing `useQuery(currentSnapshotQueryOptions)` + `savesQueryOptions` pattern for gating.
- Store atomic action `replaceLayout` from commit 3 is the only writer for tactic layout.

**Constraints and non-goals:**

- Do not make tactic columns filter-eligible.
- Do not persist tactic sort across tableIds (wrong-table fallback).
- Do not leak previous save's tactic after switch (enabled gate).

**Dependencies and sequencing:**

- Depends on commits 2-4 (neutral IDs, store atomic action, resolver). Must land before the toggle UI commit so that commit 7's exposed header sort never reaches an unvalidated SQL path.

**Validation:** `./scripts/dev check-app`, `./scripts/dev test src/features/search/utils/dynamic-columns.test.ts src/app/routes/search.test.tsx src/features/search/components/search-results-panel.test.tsx src/stores/use-player-table-store.test.ts src/app/routes/my-club-squad.test.tsx src/features/planner/components/planner-tactic-editor.test.tsx`, `(cd src-tauri && cargo test --lib -- tactic)` / `(cd src-tauri && cargo test --lib -- resolver)` and `./scripts/dev check-rust`, plus `./scripts/dev smoke` (Playwright product smoke with IPC stub; requires `pnpm exec playwright install chromium` once, without weakening required `check` gates). Fixed-string checks (both `.ts` and `.tsx` with correct `rg -g`): `rg -F "withAbortSignal" -g "*.ts" -g "*.tsx" src` empty, `rg -F "with-abort" -g "*.ts" -g "*.tsx" src` empty, `rg -F "pendingPlannerTacticDeferred" -g "*.ts" -g "*.tsx" src` empty, `rg -F "setPlannerTacticPending" -g "*.ts" -g "*.tsx" src` empty (obsolete abort/global pending helpers absent), `rg -F "plannerKeys.tactic" -g "*.ts" -g "*.tsx" src` shows only `plannerKeys.tactic({ saveId, contextToken })` immutable-context-keyed calls, `rg -F "plannerKeys.tactic()" -g "*.ts" -g "*.tsx" src` is zero (no zero-arg), `rg -F "?? -1" -g "*.ts" -g "*.tsx" src/features/planner` is zero (no sentinel), `rg -F "as unknown as PlannerTactic" -g "*.ts" -g "*.tsx" src` zero and `rg -F "as any" -g "*.ts" -g "*.tsx" src/features/planner` has no tactic casts, `rg -F "? useQuery" -g "*.ts" -g "*.tsx" src` zero tactic ternary hooks, `rg -F "[\"planner\",\"tactic\",\"disabled\"]" -g "*.ts" -g "*.tsx" src` zero sentinel keys, `rg -F "skipToken" -g "*.ts" -g "*.tsx" src` shows type proof if used (or zero if child boundary preferred, both acceptable), plus `rg -F "contextToken" -g "*.ts" -g "*.tsx" src/features/planner/api` shows exact camelCase `{ saveId, contextToken }` in `fetchPlannerTactic`/`fetchPlannerTacticOptions`/`savePlannerTactic` and `e2e/tauri-ipc-stub.ts` stub, and `rg -F "mutationKey" -g "*.ts" -g "*.tsx" src/features/planner` shows context-scoped `mutationKey: ["planner","tactic", context]`.

**Stop conditions:** If `usePlayerTableStore.getState().layouts[*]` is unavailable inside `validateSearch` (static function), create a module-level helper `isTacticSortVisibleFromPersistedState` that reads the persisted storage key directly or add the check inside the component's `useEffect` navigation fallback rather than inside `validateSearch`.

**Review mandate:**

1. IPC is explicit immutable context: `fetchPlannerTactic({ saveId, contextToken })`/`fetchPlannerTacticOptions({ saveId, contextToken })` invoke `get_planner_tactic`/`get_planner_tactic_options` with exact camelCase `{ saveId, contextToken }`; Rust handlers validate `saveId+contextToken+is_active` via `ensure_active_save_context` (`SELECT EXISTS(... WHERE id=?1 AND context_token=?2 AND is_active=1)`) inside `unchecked_transaction` under DB lock before read/default init and return `Save {id} not found` (unknown) or `Save changed or is no longer active` (inactive/stale-token); no `active_save_id` lookup remains.
2. `plannerKeys.tactic({ saveId, contextToken })` / `tacticOptions({ saveId, contextToken })` contain the immutable context with no `?? -1` sentinel (analogue `clubDnaKeys.definition`); `plannerTacticQueryOptions(context)`/`plannerTacticOptionsQueryOptions(context)` accept non-null context only (no nullable factories); callers use child-boundary gating (conditionally mount `SearchTacticQueries`/`MyClubTacticQueries` only when `context != null && isMatchedSettled`, inside child `useQuery(plannerTacticQueryOptions(context))` unconditionally, no ternary hooks `context ? useQuery(...) : useQuery(...)`, no casts `as unknown as PlannerTactic`/`as any`, no sentinel keys `["planner","tactic","disabled"]`/data `Promise.resolve(null)`/nullable factories); numeric ID reuse (`{1,"a"}` vs `{1,"b"}`) is isolated; switching `contextA`->`contextB` yields distinct keys and does not reuse stale cache; delayed A `{1,"tokenA"}` does not mislabel as B `{2,"tokenB"}`; `skipToken` proof from `@tanstack/react-query@5.101.4` types (`_tsup-dts-rollup.d.ts:133`/`683`/`647`) is documented if `skipToken` appears, but child boundary is preferred.
3. `isVisibleSortField` + `validateSearch` accept canonical tactic sort only when present in the current view's persisted layout (`isValidTacticColumnId` + `layouts[tableId].columnIds.includes`), and reject invalid/removed/wrong-table IDs (fallback verified) for `search`/`moneyball-search`/`shortlist`.
4. No-snapshot with valid save context keeps tabs + `SearchFilterBar` mounted while only `SearchResultsPanel` shows empty state; no tactic/options/save IPC fires when `snapshot==null` even though `context` (valid save `isActive`) exists (child not mounted, no factory invocation, no sentinel); no `try/catch`/`Promise.all(...).catch(()=>null)` swallows `snapshot`/`saves`/`tactic` context errors into non-actionable `null`/`Loading…`; toggle gated error and no layout mutation on toggle are proven in Commit 7 (Commit 6 proves filter bar + no read IPC + no layout mutation + no save IPC without asserting toggle UI).
5. My Club/Search preserve existing `isFetching`/`isError`/`isRefetchError` pending and error surfaces: initial `isError` (no cached `data`) -> actionable `Could not load tactic` + `Retry` refetching both context-scoped keys; cached `isRefetchError` retains `data` read-only with background `isFetching`; not swallowed into `contextTacticError`/`void Promise.all(...).catch(()=>null)`; `savePlannerTactic` mutation uses `mutationKey: ["planner","tactic", context]` with full `context` variables and editor keyed by `contextKey(context)` with stale guard (delayed A completion does not update B cache/draft/success/error, same-ID token rotation also guarded).
6. `src/lib/with-abort.ts` deleted; fixed-string `rg -F "withAbortSignal"`, `rg -F "with-abort"`, `rg -F "pendingPlannerTacticDeferred"`, `rg -F "setPlannerTacticPending"` all zero (obsolete abort/global pending helpers absent); `rg -F "plannerKeys.tactic"` shows only context-keyed calls with `contextToken`; `rg -F "as unknown as PlannerTactic"`/`as any` zero no tactic casts; `rg -F "? useQuery"` zero ternary hooks; `rg -F "[\"planner\",\"tactic\",\"disabled\"]"` zero sentinel keys; `rg -F "mutationKey"` shows context-scoped mutationKey.
7. `biome.json` boundaries still hold (route owns Planner imports `ClubDnaContext` analogue, shared/store remain clean `src/utils/tactic-ids.ts` neutral; `tactic::get_tactic_options` stays pure or thinly guarded via `ensure_active_save_context`).
8. Tests cover A/B crossing with immutable context-keyed delayed `Deferred` per context for reads (not global), plus delayed A→B and same-ID token rotation for mutation `savePlannerTactic` (assert stale guard, editor remount, cache not overwritten, exact `mutationKey`/variables), backend context validation/rejections (inactive/stale/unknown for reads and mutation), query-key isolation with `contextToken` numeric ID reuse, no-snapshot with valid save context (`snapshot==null` but `saves` valid, assert `SearchFilterBar` present, no read/save IPC, no layout mutation — toggle gated error lives in Commit 7), and My Club/Search `isRefetchError` retention including options-only recovery; `src/testing/setup.ts` and `e2e/tauri-ipc-stub.ts` validate exact camelCase `{ saveId, contextToken }` for reads and mutation and are context-keyed with immutable context-keyed delayed A/B per context; `savePlannerTactic({ saveId, contextToken, tactic })` exact variables proven; label source is save tactic role selection (`PlannerTactic.lanes`) + shared `TacticOptions` catalog (`displayName`) via `src/features/planner/types/tactic.ts`; invalid tactic sorts never reach Rust query.

#### Commit 7 — Toggle buttons, interleaving, header wiring, widths and null-last polish

**Status:** Pending

**Provisional commit:** `feat(search): add tactic lane toggles and interleaving`

**Work:** Surface the two buttons next to `Edit Filters` on the three player tables, define the complete Search tactic loading/error state model, implement append/remove/interleaving/re-compaction via the atomic store action, complete header-X synthetic rendering/removal/recompaction (moved from Commit 6), and ensure column widths and null-last sorting polish with full test coverage. Explicitly preserves table-scoped tactic IDs/widths across save switches while labels (`laneLabels` from `PlannerTactic` lanes + `TacticOptions` catalog) update for the new immutable context `{ saveId, contextToken }`.

**Size assessment:** ~220 non-test lines + ~125 tests. Above the soft target because the UI must land atomically with its typed ready/pending/error state model, header-X wiring/tests moved from Commit 6, and cross-save width/label preservation; splitting would expose tactic controls without a complete query/error contract or expose synthetic columns without their atomic layout behavior.

**Out of scope:**

- Additional backend logic beyond what commit 5 provides; Store atomic action definition beyond what commit 3 provides; route sort validation beyond what commit 6 provides; metric picker promotion.

**Implementation packet:**

**Files and responsibilities:**

- `src/features/search/components/tactic-column-toggles.tsx` (new) — prop-only presentational component `TacticColumnToggles { currentActive, potentialActive, onToggleGroup }`. It renders two `Button`s (secondary when inactive, primary when active) with accessible names `"Add Tactic (Current)"` / `"Add Tactic (Potential)"` and `aria-pressed={active}`, then delegates each click with the selected group and add/remove intent. It owns no store, query, Planner, validation, label, or error state.
- `src/features/search/components/tactic-column-toggles.test.tsx` (new) — directly proves both accessible buttons, active/inactive variants and `aria-pressed`, plus exact add/remove callback delegation. Ordering, validation, atomic replacement, header-X, and persistence stay at the route/store integration seams rather than being mocked into this presentational unit.
- `src/app/routes/search.tsx` — owns a discriminated `SearchTacticUiState`: `{ status: "no-save" }`, `{ status: "no-snapshot" }`, `{ status: "context-changing" }`, `{ status: "pending" }`, `{ status: "error"; error: Error; retryBoth: () => void }`, or `{ status: "ready"; tactic: PlannerTactic; options: TacticOptions; readOnly: boolean; refreshError: Error | null; retryBoth: () => void }`. The route maps the `TacticContextBoundary` render prop explicitly: `initialError` -> `error`; `isPending || !tactic || !options` -> `pending`; otherwise -> `ready`. This narrowing is the only path that constructs `ready`, so `SearchPageLayout` never validates undefined tactic data. `SearchPageContent` remains the hook owner: it calls `useState` for `tacticActionError` and an unconditional `useEffect` that clears that error when the immutable context key or snapshot identity changes. Its hook-free `renderLayout(state)` closure builds `onToggleGroup` from the discriminated state and returns one pure `SearchPageLayout`; no hook lives in the closure or in a conditional branch, and no query data is mirrored through effects. Route composition is `context == null` -> `renderLayout({status:"no-save"})`; active context with no snapshot -> `no-snapshot`; mismatched/refetching context -> `context-changing`; otherwise mount `TacticContextBoundary` and call `renderLayout` with the narrowed `pending`/`error`/`ready` state. `SearchPageLayout` renders the tabs, `SearchFilterBar`, `TacticColumnToggles`, state feedback, and results region once for General, Moneyball, and Shortlist. Exact unavailable actions are distinct: no save -> `"No active save — configure a save before adding tactic columns"`; no/mismatched snapshot -> `"No snapshot loaded — use Load Data"`; changing context -> `"Active save is changing — wait for refresh"`; pending -> `"Tactic is still loading — try again shortly"`. The `error` state renders an inline `Could not load tactic` panel whose Retry calls `retryBoth`; `ready.refreshError` renders the same retry affordance while preserving labels/cells and setting controls read-only. Every non-ready toggle path mutates no layout; only `ready && !readOnly` may validate `tactic/options` and call `replaceLayout`. The boundary is not mounted for `no-save`, `no-snapshot`, or `context-changing`, so those states issue no tactic/options/save IPC.
- `src/features/search/components/search-results-panel.tsx` — extend `tableColumnForMetric` fallback to produce synthetic columns: when `isTacticColumnId(metricId)` and `isValidTacticColumnId(metricId)` found, return `PlayerTableColumn { id: metricId, label: laneLabels.get(laneId) ?? laneId, align: "right", width: widths[metricId] ?? TACTIC_COLUMN_DEFAULT_WIDTH }`; otherwise filter out unknown tactic id. Extend `renderCells` branch for `isTacticColumnId(column.id)` to render right-aligned `ScoreBadge` when `typeof dynamicValue === "number"` else `"—"` span (`text-on-surface-variant`). Wire `onRemoveColumn` to tactic-aware header-X recompaction (moved from Commit 6) and pass `canMutateTacticLayout = state.status === "ready" && !state.readOnly`: tactic X is disabled/no-op unless authoritative `orderedLaneIds` are ready. When a ready tactic column is removed, compute `nonTacticIds = columnIds.filter(id => !isTacticColumnId(id))` + surviving tactic group straight ordering via `allTacticColumnIdsForGroup`/`orderedLaneIds`, then `replaceLayout(tableId, [...nonTacticIds, ...survivingStraight])` so survivor re-compacts without gaps; when removed sort was tactic sort, fallback to `ca`/`moneyball.average_rating`. Ensure `setColumnWidth(tableId, columnId, width)` is wired for `isTacticColumnId` exactly as other role columns; default width `TACTIC_COLUMN_DEFAULT_WIDTH` (112) travels with ID and is clamped `72..360`. Explicitly preserves table-scoped `columnIds`/`widths` across save switches: `layouts[tableId].columnIds` and `widths` remain after `contextA->{saveId,contextToken}` to `contextB` switch (not cleared), while `laneLabels` (from `PlannerTactic` lanes + `TacticOptions.roles[].displayName` catalog) recompute for new context via `plannerKeys.tactic(context)` / `tacticOptions(context)`.
- `src/components/player-table/player-table-header.tsx` — read-only inspection: confirm resize and sort caret work for synthetic columns (no column-type-specific logic required); X button has accessible name and triggers `onRemoveColumn` with tactic-aware recompaction.
- `src/testing/setup.ts` + `e2e/tauri-ipc-stub.ts` — already updated in Commit 6 for `{ saveId, contextToken }` validation; this commit adds header-X interaction stubs if needed (no new abort helpers).
- `src/features/search/utils/dynamic-columns.ts` / `src-tauri/src/features/search/query.rs` — no further change in this commit beyond width/sort polish verification (Moneyball mapping doc comment lives in commit 5 per NITPICK; do not duplicate in commit 7).

**Behavior and data flow:**

- Entry: immutable save context `context={ saveId, contextToken }` (from `SaveSummary`) and snapshot state produce one `SearchTacticUiState`. `SearchPageContent` owns `tacticActionError`; `renderLayout(state)` passes the state, current error, and hook-free callbacks to one pure `SearchPageLayout`. For `ready`, the layout derives `orderedLaneIds` via `orderedTacticLanes` + `compareTacticPositions` and `laneLabels` from `PlannerTactic.lanes` + `TacticOptions.roles[].displayName`. For `pending` or `error`, tactic columns already persisted may render their neutral lane-ID fallback but cannot be added, removed, or re-compacted until a ready tactic supplies its authoritative order.
- Toggle click: `no-save`, `no-snapshot`, `context-changing`, and `pending` set their distinct exact feedback string and perform no IPC or layout mutation. `error` and `ready` with `refreshError` expose `retryBoth`; `readOnly` blocks layout mutation. Only `ready && !readOnly` validates `tactic/options`, clears prior action feedback, then performs one `replaceLayout` atomic write (`nonTacticIds + buildTacticColumnOrder(...)`). Validation failure sets the exact returned error and leaves layout/localStorage unchanged.
- Header X click (moved from Commit 6): `onRemoveColumn(tactic_current.left_back)` -> `replaceLayout(tableId, [...nonTacticIds, ...survivingStraight])` where `survivingStraight` is straight survivor group's 11 IDs in tactic-position order, contiguous at far-right (no interleaving holes) -> `isFullTacticGroup` flips to false -> button `aria-pressed=false`; sort fallback if removed tactic was sorted. Verified via `usePlayerTableStore.getState().layouts[tableId].columnIds`.
- Save switch `contextA {1,"tokenA"} -> contextB {2,"tokenB"}`: `layouts[tableId].columnIds`/`widths` preserved (table-scoped, not cleared), `laneLabels` update for new context because `plannerKeys.tactic(contextB)` resolves new `PlannerTactic` + `TacticOptions` catalog via `tacticState`; `widths["tactic_current.goalkeeper"]=200` travels with ID across switch. `SearchPageLayout` re-renders with new `tacticState` without remounting layout state.
- Resize lifecycle: drag -> `setColumnWidth(tableId, tactic_current.goalkeeper, newWidth)` -> `layouts[tableId].widths[...] = clampWidth(newWidth)` -> panel applies inline width and persists.

**Ordered implementation steps:**

1. Create `tactic-column-toggles.tsx` as presentational component with derived state and delegated callbacks; unit-test its rendering and callback delegation without mounting the store (prop-driven).
2. Add `SearchResultsPanel` synthetic column dispatch and cell rendering for `tactic_*` with `ScoreBadge`; verify story still renders `…` placeholder when `player === undefined`.
3. In `src/app/routes/search.tsx`, define `SearchTacticUiState` as the six-state discriminated union and a pure `SearchPageLayout` that receives the already-narrowed state plus callbacks. Keep `useState`/`useEffect` in `SearchPageContent`, before any return; clear `tacticActionError` on immutable context-key or snapshot identity change. Use one hook-free `renderLayout(state)` closure. Map `TacticContextBoundary` output to `pending`, `error`, or `ready` only after checking both `tactic` and `options`; map missing save, missing/mismatched snapshot, and context refresh separately to `no-save`, `no-snapshot`, and `context-changing`. Render an actionable retry panel for initial query error and refresh error. No conditional hooks, casts, sentinel query data, effect-based query mirroring, or undefined tactic validation.
4. Implement the route-owned `onToggleGroup` callback as an exhaustive switch over `SearchTacticUiState`. Assert every non-ready/read-only branch produces its exact feedback and never calls `replaceLayout`; assert `error`/refresh-error Retry invokes `retryBoth`; assert only `ready && !readOnly` can validate and replace. Verify immutable-context data, state transitions, and interactions through `src/testing/planner-ipc-mock.ts` + `mockIPC`.
5. Run `./scripts/dev test` focused then full `check-app`.

**Tests and proof:**

- `src/features/search/components/tactic-column-toggles.test.tsx`: prop-driven proof for both accessible buttons, active/inactive style and `aria-pressed`, and exact add/remove delegation. It does not pretend to prove ordering, persistence, validation, or header-X behavior owned by the route/panel.
- `src/app/routes/search.test.tsx`: render Search with mocked immutable contexts and cover every discriminant. No active save shows `"No active save — configure a save before adding tactic columns"`; active save plus null/mismatched snapshot shows `"No snapshot loaded — use Load Data"`; context refresh shows `"Active save is changing — wait for refresh"`; pending tactic/options shows `"Tactic is still loading — try again shortly"`; tactic-only or options-only initial failure shows `Could not load tactic` and Retry fetches both; cached refetch failure retains labels/cells but blocks layout changes until Retry succeeds; ready state enables validation and atomic replacement. For every unavailable state, clicking either toggle and attempting tactic header removal leave the store/localStorage byte-for-byte unchanged and do not invoke save IPC.
- Ready-state route integration then proves append current, interleaved current+potential (`GK Cur, GK Pot, …` in tactic-position order), toggle-off recompaction, Moneyball and Shortlist table-bucket independence, validation failure with no mutation, synthetic header labels from `TacticOptions`, numeric `ScoreBadge` versus unavailable `"—"`, and header-X through `SearchResultsPanel`/`PlayerTableHeader`. Removing one X while both groups are full discards that partial group, keeps the opposite group straight at far-right, prunes the removed width, deactivates the affected toggle, and applies sort fallback. Context A→B and same-ID/new-token transitions preserve table-scoped column IDs and widths while labels update from B; delayed A data cannot overwrite B's labels/cache.
- Width/sort polish tests: `widths["tactic_current.goalkeeper"]=200` survives re-hydrate and travels with id; clicking tactic header toggles `asc`/`desc` with `NULLS LAST` (fixtures: one player 70, one `—`), asserted via sorted `page.players` order; removed tactic sort falls back to `defaultSearchSort`.

**Patterns to verify:**

- Existing `SearchFilterBar` compact strip pattern — buttons sit alongside `Edit Filters`, not inside the modal.
- `SearchResultsPanel` `removeColumn` + sort fallback pattern — now tactic-aware via `replaceLayout`.
- App existing error/feedback: inline `Panel` with `error` tone matching Shortlist load errors.

**Constraints and non-goals:**

- Do not make `isTacticColumnId` columns available in `PLAYER_METRICS` picker; do not change `FilterAst` or `search-url-search.ts`.
- Do not style tactic columns differently than other role `ScoreBadge` columns (same ramp).
- Do not persist active booleans; derive strictly from `columnIds`.
- Do not modify Squad's layout.
- Do not create a standalone docs-only commit — mapping docs live inline in commit 5 and durable ledger/architecture notes.

**Dependencies and sequencing:**

- Depends on commits 2-6 (helpers + store atomic action + resolver + query scorer + explicit immutable context `{ saveId, contextToken }` IPC via `ensure_active_save_context` in commit 6). Commit 7 reuses `plannerKeys.tactic(context)`/`plannerTacticQueryOptions(context)` (context-keyed, numeric ID reuse isolated) from commit 6 plus the atomic `replaceLayout` for header-X recompaction; no new IPC; ensure `src/lib/with-abort.ts` remains deleted (fixed-string `rg -F "with-abort"` zero).

**Validation:** `./scripts/dev check-app`, `./scripts/dev test src/utils/tactic-ids.test.ts src/features/search/utils/tactic-columns.test.ts src/stores/use-player-table-store.test.ts src/features/search/components/tactic-column-toggles.test.tsx src/features/search/components/search-results-panel.test.tsx src/app/routes/search.test.tsx src/app/routes/my-club-squad.test.tsx`, `./scripts/dev check-rust` (or separate `(cd src-tauri && cargo test --lib -- resolver)` + `(cd src-tauri && cargo test --lib -- query)` + `(cd src-tauri && cargo test --lib -- fit)` + `(cd src-tauri && cargo test --lib -- tactic)`) still green, plus `./scripts/dev smoke` (Playwright product smoke with IPC stub; requires `pnpm exec playwright install chromium` once, without weakening required `check` gates). Do not use a single `cargo test query fit resolver` invocation (invalid — one `TESTNAME`). Fixed-string checks (both `.ts` and `.tsx` with correct `rg -g`): `rg -F "withAbortSignal" -g "*.ts" -g "*.tsx" src` empty, `rg -F "with-abort" -g "*.ts" -g "*.tsx" src` empty, `rg -F "pendingPlannerTacticDeferred" -g "*.ts" -g "*.tsx" src` empty, `rg -F "setPlannerTacticPending" -g "*.ts" -g "*.tsx" src` empty (obsolete abort/global pending helpers absent), `rg -F "plannerKeys.tactic" -g "*.ts" -g "*.tsx" src` shows only `plannerKeys.tactic({ saveId, contextToken })` immutable-context-keyed calls with `contextToken`, `rg -F "plannerKeys.tactic()" -g "*.ts" -g "*.tsx" src` zero (no zero-arg), `rg -F "?? -1" -g "*.ts" -g "*.tsx" src/features/planner` zero (no sentinel).

**Stop conditions:** `plannerKeys.tactic(context)` must be available with explicit `{ saveId, contextToken }`; if `fetchPlannerTactic` still expects implicit active save or bare `saveId` without `contextToken`, the commit cannot proceed — replanned commit 6 must have landed. Route must not assume planner loader exists without immutable context. Header-X recompaction must use `replaceLayout` atomically, not successive `removeColumn`.

**Review mandate:**

1. Button active state derived solely from `columnIds` (no stray Zustand bool).
2. Append is always far-right (after non-tactic) and cleared partial first via atomic `replaceLayout`.
3. Interleaving order is `GK Cur, GK Pot, DR Cur, DR Pot,...` (not alphabetical) when both active, straight otherwise.
4. Removing one X immediately deactivates that group's button and re-compacts survivor straight at far-right without gaps.
5. Removing a group when both active re-compacts via same atomic action (no intermediate interleaved partial observable).
6. The discriminated UI state is exhaustive and type-safe: only `ready` contains defined tactic/options; no-save, no-snapshot, context-changing, pending, initial error, and refresh error remain distinct. Initial tactic or options errors retry both queries; refresh errors retain labels/cells read-only. Every unavailable/read-only/invalid path mutates no layout (verified against the store and localStorage).
7. Shortlist's identical persistence bucket `shortlist` owns its own toggles (cross-table independence); save switching does not leak tactic between saves.
8. Width default `TACTIC_COLUMN_DEFAULT_WIDTH = 112` and right-align verified; `NULL` `"—"` rows are last in both ASC and DESC.
9. No `biome.json` boundary violation: Planner imports only in `app/routes`, shared/store remain clean, search helpers import only neutral shared modules.
10. No new behavior introduced without the required Moneyball mapped vs unmatched, save-switching, all six tactic UI states, no-snapshot, and post-X recompaction proofs. The exact focused command includes `tactic-column-toggles.test.tsx`.

## Active work

**PR:** PR 1 — Tactic columns for player tables (Search, Moneyball, Shortlist)

**Commit:** Explicit save-scoped Planner tactic IPC, route sort gating, and no-snapshot composition

### RED or removal proof

Exact REDs for this commit (before GREEN): (1) A/B crossing with immutable test-local deferred context responses and exact non-vacuous cache assertions — IPC/context-keyed caches/stale isolation via `TacticContextBoundary` (visible labels remain Commit 7): fixtures `contextA={ saveId:1, contextToken:"save-token-a" }` / `contextB={ saveId:2, contextToken:"save-token-b" }` and `contextA-recreated={ saveId:1, contextToken:"save-token-a2" }`; render My Club with `saves=[{id:1, contextToken:"save-token-a", isActive:true}]` + `snapshot.saveId=1` and `TacticContextBoundary` mounted for My Club (Search boundary deferred to Commit 7, verified absent via `rg -F "TacticContextBoundary"` + factory-not-invoked), mock `fetchPlannerTactic(contextA)` and `fetchPlannerTacticOptions(contextA)` with test-local `Deferred` per immutable `{ saveId, contextToken }` (stored in test-local `Map<string, Deferred>` via `contextKey`, not global `pendingPlannerTacticDeferred`/`setPlannerTacticPending`), switch immutable context `contextA {1,"tokenA"} -> contextB {2,"tokenB"}` before A resolves, assert `invoke("get_planner_tactic", { saveId:1, contextToken:"save-token-a" })` and `get_planner_tactic_options` with exact camelCase `contextToken` not reused for B and likewise `save_planner_tactic` with `{ saveId, contextToken, tactic }` carries context, `queryClient.getQueryData(plannerKeys.tactic(contextA))` vs `plannerKeys.tactic(contextB)` distinct and vs `contextA-recreated` distinct with non-vacuous `expect(dataA).toEqual(expectedTacticA)` and `expect(dataB).toBeUndefined()` until B resolves (not just `toBeDefined`), and resolving stale A populates only `plannerKeys.tactic(contextA)` with `expectedTacticA` (boundary's `tactic` prop) and does not overwrite `plannerKeys.tactic(contextB)` nor `plannerKeys.tacticOptions(contextB)` nor mutation cache (assert `getQueryData(tactic(contextB))` still `undefined` and `getQueryData(tacticOptions(contextB))` still `undefined` after stale A resolves, not vacuous `not.toBe`); `src/testing/planner-ipc-mock.ts` context-keyed via `contextKey({ saveId, contextToken })` (analogue `src/features/club-dna/components/club-dna-definition.tsx:67`), `src/testing/setup.ts` and `e2e/tauri-ipc-stub.ts` validate exact camelCase `{ saveId, contextToken }` for reads and mutation; (2) backend save context validation via snapshot-owned `pub(crate) ensure_active_save_context(&Transaction, save_id, context_token)` at `src-tauri/src/features/snapshot/service.rs` under exactly one `unchecked_transaction` per command held under `Db` lock with two-step distinct errors and one transaction owner per Rust command: `get_planner_tactic({1,"tokenA"})` when active `{1,"tokenA"}` succeeds; stale-token `{1,"stale"}` -> `Err("Save changed or is no longer active")` (existing-but-stale/inactive token distinct); unknown `{999,"x"}` -> `Err("Save 999 not found")` (unknown numeric save distinct); inactive save `{1,"tokenA"}` where `is_active=0` -> `Err("Save changed or is no longer active")` (distinct), same distinct rejections for `get_planner_tactic_options` and `save_planner_tactic({ saveId, contextToken, tactic })` stale/inactive/unknown rejected in same `tx` with same distinct errors — one `validate_tactic` exactly once after `ensure_active_save_context` and before `save_tactic_in_tx` (helper does not duplicate) then `tx.commit()` exactly once (mutation path, including `PlannerTacticEditor` `save` error surface via `save.isError`); also proves transaction design: trusted `get_tactic(&Connection, save_id)` retains `ensure_save_exists` once before `tx` then `load_or_initialize_tactic_in_tx` + `commit`; trusted `save_tactic(&Connection, save_id, &tactic)` retains `ensure_save_exists` + one `validate_tactic` exactly once before `tx` then `save_tactic_in_tx` (no duplicate) + `commit`, each wrapper commits its own one transaction exactly once, no nesting; commands own exactly one `tx` via `ensure_active_save_context(&tx, ...)` then `load_or_initialize_tactic_in_tx` / one `validate_tactic` + `save_tactic_in_tx` (no duplicate) / static `get_tactic_options()` after validation → `tx.commit()` exactly once — no nested transactions, no duplicate validator, no `active_save_id`, commit exactly once after success; (3) query-key isolation with non-null-only factories and single `TacticContextBoundary` gating (no ternary hooks, no casts, no sentinel keys/data, no nullable factories) — `plannerTacticQueryOptions(context)`/`plannerTacticOptionsQueryOptions(context)` accept non-null immutable `{saveId, contextToken}` only (no `null`/`undefined`/sentinel/`?? -1`/`"none"` placeholder, no nullable factories), `plannerKeys.tactic(contextA) != tactic(contextB)` and `!= tactic(contextA-recreated)` (numeric ID reuse via `contextToken`), `plannerTacticQueryOptions(contextB).queryKey` contains `{ saveId:2, contextToken:"save-token-b" }` and factory `queryFn` captures `contextB` only with no casts, `fetchPlannerTactic(contextB)` invokes `get_planner_tactic` with exact `{ saveId:2, contextToken:"save-token-b" }` and `savePlannerTactic({ saveId, contextToken, tactic })` via `useMutation({ mutationKey: ["planner","tactic", context], mutationFn: (vars) => savePlannerTactic(vars) })` with exact `{ saveId, contextToken, tactic }` validated in `src/testing/setup.ts`/`e2e/tauri-ipc-stub.ts`/`planner-ipc-mock.ts` (no zero-arg `plannerKeys.tactic()` / no `?? -1` sentinel, checked via `rg -F` with `-g "*.ts" -g "*.tsx"`), `rg -F "plannerKeys.tactic({"` shows `contextToken`, `rg -F "as unknown as PlannerTactic"` zero and `rg -F "as any"` no tactic casts, `rg -F "? useQuery"` zero ternary hooks, `rg -F "[\"planner\",\"tactic\",\"disabled\"]"` zero sentinel keys, `rg -F "mutationKey"` shows context-scoped `mutationKey: ["planner","tactic", context]` with full `context`, and `rg -F "TacticContextBoundary"` shows single boundary definition plus My Club mount in Commit 6 and absent Search mount (deferred); `skipToken` proof from `@tanstack/react-query@5.101.4` types (`_tsup-dts-rollup.d.ts:133`/`683`/`647`) is documented, boundary preferred over sentinel; (4) `TacticContextBoundary` fetch errors via boundary render prop: `initialError` with no cached `data` (`tacticQuery.isError && !data` or `optionsQuery.isError && !data`) shows actionable `Could not load tactic` + `retryBoth`; `retryBoth` refetches both context-scoped keys (`refetchQueries({ queryKey: plannerKeys.tactic(context) })` and `refetchQueries({ queryKey: plannerKeys.tacticOptions(context) })`), including options-only failure where `tactic` succeeds and `options` fails — test asserts `props.tactic` defined while `props.options` undefined, after `retryBoth` both defined (options-only recovery). Cached `refreshError` with cached `data` retains `data` read-only, sets `readOnly=true` and blocks writes (`savePlannerTactic` disabled via `readOnly`/`mutationKey` guard, toggle blocked in Commit 7 via `readOnly`); boundary unconditionally calls both `useQuery` hooks with non-null factories (proof: no ternary hooks inside boundary); (5) active immutable save context present + snapshot null/mismatch (boundary unmounted, zero IPC, filter/tabs present): `saves=[{id:1, contextToken:"save-token-a", isActive:true}]` valid, `snapshot==null` or `snapshot.saveId !== context.saveId` (`context={ saveId:1, contextToken:"save-token-a" }` exists but no matched snapshot), assert `SearchFilterBar` + tabs remain in DOM, `Panel EmptyState No data loaded` shows (only `SearchResultsPanel` branches), `TacticContextBoundary` not mounted for Search (deferred) nor for My Club because `!isMatchedSnapshot` (no factory invocation, no `get_planner_tactic`/`get_planner_tactic_options`/`save_planner_tactic` counts 0, no sentinel keys/casts), `usePlayerTableStore.getState().layouts[tableId].columnIds` unchanged (no layout mutation), `isVisibleSortField` still falls back; toggle gated error `"No snapshot loaded — use Load Data"` and no layout mutation on toggle click are proven in Commit 7 (Commit 6 keeps proof to no read IPC + filter bar + no layout mutation + no save IPC, without asserting toggle UI itself) to avoid scope contradiction; (6) mutation identity and stale guard (`mutationFn` exact `{ saveId, contextToken, tactic }`, parent-owned `currentContextRef` full key, stale A: no cache/no UI, B untouched): assert `useMutation` has `mutationKey: ["planner","tactic", context]` with full context and `mutationFn: (vars: { saveId, contextToken, tactic }) => savePlannerTactic(vars)` called as `mutate({ saveId: contextA.saveId, contextToken: contextA.contextToken, tactic: draft })` with exact camelCase; parent owns `currentContextRef = useRef(context)` synced to `contextKey(context)`; `onSuccess`/`onError` compare `contextKey(vars) === contextKey(currentContextRef.current)` (full key); delayed A→B: start `savePlannerTactic({ saveId:1, contextToken:"tokenA", tactic: draftA })` with test-local `Deferred` per context, switch to `contextB={2,"tokenB"}` before A resolves, resolve stale A then assert stale completion updates no cache and no draft/success/error UI — `getQueryData(plannerKeys.tactic(contextB))` still `undefined` (not `draftA`), `getQueryData(plannerKeys.tactic(contextA))` still not written? Actually current completion updates only matching key: stale A discarded so `plannerKeys.tactic(contextA)` remains not updated via stale path (no `setQueryData`), B's `plannerKeys.tactic(contextB)` remains `undefined`, editor for B still `save.isPending==false`/`isError==false`/`succeeded==false` and draft unchanged; same-ID/new-token: `contextA={1,"tokenA"}`→`contextA2={1,"tokenA2"}` delayed A stale discarded (no cache/no UI), new mutation for `contextA2` writes only `plannerKeys.tactic(contextA2)`; both with exact assertions `expect(getQueryData(tactic(contextB))).toBeUndefined()` and `expect(getQueryData(tactic(contextA))).toBeUndefined()` for stale case and `expect(mutationKey).toContain(contextToken)`; (7) sort validation — `isVisibleSortField("tactic_current.goalkeeper", [], "general")` with layout containing it is `true`, without is `false`, wrong-table is `false`, invalid lane is `false`; (8) search initial snapshot/saves errors retain existing route error propagation with no swallowed catches: `currentSnapshotQueryOptions` `isError` still shows `Could not load players` / `Loading player results…` via `useSuspenseQuery` error boundary (no `try/catch` returning `null`), `savesQuery.isError` shows `Could not load saves`, Define DNA `clubDnaQueryOptions(context)` error preserved; mutation `save_planner_tactic` stale/inactive/unknown shows inline error via `save.isError` for originating context only (not B); `src/lib/with-abort.ts` absent (`rg -F "with-abort" -g "*.ts" -g "*.tsx"` zero, `rg -F "withAbortSignal" -g "*.ts" -g "*.tsx"` zero), `rg -F "pendingPlannerTacticDeferred"`/`setPlannerTacticPending` zero, `rg -F "as unknown as PlannerTactic"`/`as any` zero no tactic casts, `rg -F "? useQuery"` zero ternary hooks, `rg -F "[\"planner\",\"tactic\",\"disabled\"]"` zero sentinel keys. Exact consumers after source inspection: Rust `get_planner_tactic`/`get_planner_tactic_options`/`save_planner_tactic` at `src-tauri/src/features/planner/commands.rs`; frontend `fetchPlannerTactic`/`fetchPlannerTacticOptions`/`savePlannerTactic`, `plannerKeys.tactic`/`tacticOptions`, `plannerTacticQueryOptions`/`plannerTacticOptionsQueryOptions`, `PlannerTacticEditor` mutation (`mutationKey`, `mutationFn` with full context, keyed remount, stale guard) + `setQueryData`, `my-club.tsx`/`search.tsx` child boundaries (`SearchTacticQueries`/`MyClubTacticQueries`), `src/testing/setup.ts`, `src/testing/planner-ipc-mock.ts`, `e2e/tauri-ipc-stub.ts`. No header-X proof in this commit; header interaction/order proves in Commit 7 via `SearchResultsPanel`/`search.test.tsx`. No custom abort-wrapper.

### Expected outcome

Planner tactic reads (`get_planner_tactic`, `get_planner_tactic_options`) and mutation (`save_planner_tactic`) carry explicit immutable context `{ saveId, contextToken }` (exact camelCase, required/validated for reads and mutation, mutation includes `tactic`) and Rust validates via snapshot-owned `pub(crate) ensure_active_save_context(&Transaction, save_id, context_token)` at `src-tauri/src/features/snapshot/service.rs` (snapshot boundary) with two-step distinct errors `Save {id} not found` (unknown numeric save) vs `Save changed or is no longer active` (existing-but-stale/inactive token) inside exactly one `unchecked_transaction` under the single `Db` `Mutex` lock with one transaction owner per command; trusted `get_tactic(&Connection, save_id)` / `save_tactic(&Connection, save_id, &tactic)` are preserved for Search/depth/tests and delegate by opening one `tx` to `pub(crate) load_or_initialize_tactic_in_tx`/`save_tactic_in_tx` (default initialization via `save_tactic_in_tx` in same `tx`, validation before writes), each wrapper commits its own one transaction exactly once, no nesting, no duplicate validator, no `active_save_id`; commands own exactly one `tx` (`db.0.lock()` → `tx = conn.unchecked_transaction()` → `ensure_active_save_context` → `load_or_initialize_tactic_in_tx`/`save_tactic_in_tx`/options static `get_tactic_options()` → `tx.commit()`) and commit exactly once after success. Frontend: `fetchPlannerTactic(context)`/`fetchPlannerTacticOptions(context)`/`savePlannerTactic({ saveId, contextToken, tactic })` invoke `get_planner_tactic`/`get_planner_tactic_options`/`save_planner_tactic` with exact camelCase `{ saveId, contextToken }` (+ `tactic`), no new command. Query option factories `plannerTacticQueryOptions(context: {saveId, contextToken})` and `plannerTacticOptionsQueryOptions(context)` accept non-null immutable context only (no `null`/`undefined`/sentinel/`?? -1`/`"none"`, no nullable factories, no casts, no sentinel keys/data), query keys `plannerKeys.tactic(context)`/`tacticOptions(context)` require context (analogue `clubDnaKeys.definition` at `src/features/club-dna/api/club-dna-keys.ts:5`) with numeric ID reuse isolation (`{1,"tokenA"}` vs `{1,"tokenB"}` and `contextA-recreated` distinct) for reads and mutation; callers mount the single typed `TacticContextBoundary` only with non-null matched context (`context != null && isMatchedSnapshot && isMatchedSettled` where `isMatchedSnapshot = snapshot != null && snapshot.saveId === context.saveId`), inside which both `useQuery(plannerTacticQueryOptions(context))` and `useQuery(plannerTacticOptionsQueryOptions(context))` are called unconditionally and the typed render prop `{ tactic, options, isPending, initialError, refreshError, readOnly, retryBoth }` is passed to the child subtree — Commit 6 mounts it for My Club, Search mount is deferred to Commit 7 (smallest coherent sequencing) and proved via `rg -F "TacticContextBoundary"` and factory-absent assertions, so factories never accept invalid context and active save context present + snapshot null/mismatch => boundary unmounted, zero `get_planner_tactic`/`get_planner_tactic_options`/`save_planner_tactic` IPC, filter strip + tabs remain present; `skipToken` is available (`@tanstack/react-query@5.101.4` re-exports `skipToken` per `_tsup-dts-rollup.d.ts:133`/`683`/`647`) but `TacticContextBoundary` is preferred. Inside the boundary `initialError` with no cached `data` shows actionable `Could not load tactic` + `retryBoth` that refetches both `plannerKeys.tactic(context)` and `plannerKeys.tacticOptions(context)` (covers options-only recovery), cached `refreshError` retains `data` read-only, sets `readOnly=true` and blocks writes via `readOnly`/`mutationKey` guard. Search initial `currentSnapshotQueryOptions`/`savesQueryOptions` errors retain existing route error propagation (no `try/catch` returning `null`, no `Promise.all(...).catch(()=>null)`), active save context present + snapshot null/mismatch => boundary unmounted, zero IPC, filter strip + tabs remain present. Mutation `save_planner_tactic` stale/inactive/unknown rejected in same `tx` with same distinct errors and surfaced via `PlannerTacticEditor` `save.isError` for originating context only (not B). `PlannerTacticEditor` is keyed/remounted by full context key `contextKey(context)` and uses `mutationKey: ["planner","tactic", context]` with exact `mutationFn: (vars: { saveId, contextToken, tactic }) => savePlannerTactic(vars)` + parent-owned `currentContextRef` full-key compare; stale guard: `contextKey(vars) === contextKey(currentContextRef.current)` — if stale A's completion arrives after switch to B (or same-ID token rotation), stale completion updates no cache and no draft/success/error UI (no `setQueryData` for any key, no `setDraft`/`setSuccess`/`setError`), B cache/UI untouched; current completion (`contextKey(vars) === contextKey(current)`) updates only the matching context key's cache (`plannerKeys.tactic(vars)`) and originating draft/success/error UI. `queryClient.setQueryData(plannerKeys.tactic(context), nextTactic)` uses same immutable context key only on current match. Complete consumers after source inspection: Rust `get_planner_tactic`/`get_planner_tactic_options`/`save_planner_tactic` at `src-tauri/src/features/planner/commands.rs`; frontend `fetchPlannerTactic`/`fetchPlannerTacticOptions`/`savePlannerTactic`, `plannerKeys`, `plannerTacticQueryOptions`/`plannerTacticOptionsQueryOptions`, `PlannerTacticEditor` mutation + `setQueryData`, `my-club.tsx`/`search.tsx` callers, `src/testing/setup.ts`, `src/testing/planner-ipc-mock.ts` (context-keyed `contextKey` analogue `src/features/club-dna/components/club-dna-definition.tsx:67` with test-local `Deferred` per immutable context, exact non-vacuous `getQueryData` assertions), `e2e/tauri-ipc-stub.ts` (exact camelCase validation, context-keyed responses). No header-X proof in this commit; header `X` rendering/removal/recompaction and synthetic order/width/labels are Commit 7. `src/lib/with-abort.ts` deleted with fixed-string `rg -F "with-abort" -g "*.ts" -g "*.tsx"`/`rg -F "withAbortSignal"`/`rg -F "pendingPlannerTacticDeferred"`/`rg -F "setPlannerTacticPending"` zero. Validation: `./scripts/dev check-app` (Biome + `tsc -b` + secretlint), `./scripts/dev check-rust` or focused `(cd src-tauri && cargo test --lib -- snapshot)` / `tactic` / `resolver` suites as justified, `./scripts/dev smoke` (Playwright smoke with IPC stub), plus full fixed-string obsolete checks (`rg -F "plannerKeys.tactic()"` zero, `rg -F "?? -1"` zero, `rg -F "withAbortSignal"` zero, `rg -F "with-abort"` zero, `rg -F "pendingPlannerTacticDeferred"` zero, `rg -F "setPlannerTacticPending"` zero, `rg -F "contextToken"` shows camelCase). This is a developer-authorized fresh planning cycle after the prior replan correction cap; no implementation is claimed.

### Explicit exclusions

Toggle button presentation, synthetic tactic headers/cells, all header-X synthetic rendering/removal/recompaction and tests (moved to Commit 7 — this commit is IPC/context, sort gating, no-snapshot composition only), score computation beyond the explicit IPC, backend scorer beyond commit 5, store schema changes beyond the atomic `replaceLayout`, metric picker promotion, and any `withAbortSignal`/`AbortSignal` wrapper.

## Discoveries and replanning

- Scope expansion vs original TODO Next: the provisional TODO `Next` entry was a gender data investigation; this feature is tactic columns, a dispatch-driven override. The ledger deliberately does not reference gender scope; TODO placement is under `Active` as the dispatched feature.
- Backend scorer choice required reuse of `base_position` from `tactic.rs` which is currently `pub(super)`. The plan promotes it to `pub(crate)` in commit 5 to share normalization exactly — recorded as the only architectural visibility change.
- Moneyball deterministic mapping discovery: `src-tauri/src/features/moneyball/builtin_role_definitions_v1.json` has 88 defs, 77 map via `attribute_role_id` to the 68 General roles, 11 are Moneyball-only (enumerated in tests), all 68 General role IDs represented; matching by `(attribute_role_id, base_position(placement))` covers 103/111 General `(role, position_tag)` combos, 8 unavailable by design (`holding_wing_back_oop`+DL/DR, `pressing_wing_back_oop`+DL/DR, `box_to_box_midfielder_ip`+MC, `box_to_box_playmaker_ip`+MC, `deep_lying_playmaker_ip`+MC, `second_striker_ip`+ST). Prior plan treated mapping as an unknown gap; correction folds the gap doc into implementation commits 5/7 and durable invariants with non-skippable mapped vs unmatched tests.
- `biome.json` boundary violation discovered: shared/stores may not import features and features may not import across features. Prior plan had Search helpers importing Planner internals and `use-player-table-store` importing a Search helper. Correction introduces neutral `src/utils/tactic-ids.ts` (canonical lane IDs, prefix constants, predicates, default width) and keeps Planner query/validation in `src/app/routes/search.tsx` which passes a normalized view model into Search components; stores import only the neutral module.
- Route/sort boundary discovered: `isVisibleSortField` gated non-basic sorts on `getFilterField`, so synthetic tactic IDs cannot pass. Correction assigns the layout-aware sort acceptance change to commit 6 landing before the toggle UI (commit 7), ensuring every exposed sort is already trunk-safe and tested for header, URL restoration, and fallback after removal.
- Store atomicity discovered: `removeColumn`/`addColumns` only drop one ID/width each, leaving interleaved partials after header-X. Correction defines one atomic `replaceLayout` action in commit 3 validated and used by both toggle and tactic-aware header-X paths (commit 6/7) with deterministic post-X straight recompaction.
- Save-scoping and no-snapshot discovered: `plannerTacticQueryOptions` is save-independent while `get_planner_tactic` resolves `active_save_id` implicitly, and `search.tsx` early-returns before the filter strip when `!snapshot`, contradicting the toggle-remaining promise. Correction makes the query explicitly immutable-context-scoped (`plannerKeys.tactic({ saveId, contextToken })` analogue `clubDnaKeys.definition` at `src/features/club-dna/api/club-dna-keys.ts:5`, `fetchPlannerTactic({ saveId, contextToken })` with exact camelCase `{ saveId, contextToken }`, Rust `get_planner_tactic(save_id, context_token)`/`get_planner_tactic_options(save_id, context_token)` validating via reusable `pub(crate) ensure_active_save_context` at `src-tauri/src/features/snapshot/service.rs` (snapshot boundary) with two-step distinct `Save {id} not found` vs `Save changed or is no longer active` inside the command-held `Db` lock + `unchecked_transaction` before read/default init, with `get_tactic` default initialization using single `tx` via `save_tactic_in_tx`/`load_tactic_from_tx` without nesting, and numeric ID reuse isolated via immutable `{ saveId, contextToken }` query keys) in commit 6 and refactors `search.tsx` to always render the filter strip + toggles (only the results panel branches). Component-owned non-throwing prefetch (`useQuery({ ...plannerTacticQueryOptions(context), enabled: context != null && isMatched })`) preserves initial `isError` -> `Retry` and cached `isRefetchError` retains data read-only, and preserves existing `currentSnapshotQueryOptions`/`savesQueryOptions` loader propagation and Define DNA `clubDnaQueryOptions(context)` behavior (no `catch(()=>null)` swallowing). This is an existing IPC signature change (not a new command) but changes the prior no-IPC packet boundary. Consumers/mocks `src/testing/setup.ts` + `e2e/tauri-ipc-stub.ts` and test-local immutable context-keyed delayed A/B via `contextKey({ saveId, contextToken })` (analogue `src/features/club-dna/components/club-dna-definition.tsx:67`) are wired in the same packet.
- Commit 6 three correction rounds exhausted and replanning triggered: the Active commit 6 (`feat/tactic-table-columns`, HEAD `d9d2737` `feat(search): compute tactic lane scores and sort`, base `main` `406dca7` synchronized with `origin/main`) remained `Active` with a large uncommitted diff (15 files, `src/lib/with-abort.ts` untracked) after three `workflow-fix`/`commit-reviewer` rounds. Round 1–3 attempted to fix save-scoped frontend query keys (`plannerKeys.tactic(activeSaveId ?? -1)`, `enabled: activeSaveId != null`) plus a custom `src/lib/with-abort.ts`/`withAbortSignal` wrapper around `fetchPlannerTactic`/`fetchPlannerTacticOptions` to cancel stale IPC, without adding `{ saveId, contextToken }` immutable context. Final independent review (2026-09-02) found the approach structurally insufficient: Rust `get_planner_tactic`/`get_planner_tactic_options` still resolve `service::active_save_id(&conn)` implicitly (validated only by `ensure_save_exists` on `id` alone, not `id+token+is_active` under DB lock), so a delayed A request fired before an `A→B` save switch can return after the switch and be cached/derived under B's active-save resolution (mislabeling A's tactic as B's, wrong `laneLabels` from stale `PlannerTactic` + `TacticOptions` catalog, wrong scores/sort, cache poisoning) — no frontend key or abort can fix an IPC that is authoritative on the implicit active save, and numeric ID reuse (`save 1` recreated with new `contextToken`) is not isolated. Rounds also exposed brittle abort wrappers (double `signal` handling, untested late-A deferred promise mocks `setPlannerTacticPending`/`pendingPlannerTacticDeferred` global pending helpers), `My Club` (`my-club.tsx`) and `Search` (`search.tsx`) loaders/handlers that swallowed initial `snapshot`/`saves`/`tactic` `isError`/`isFetching`/`isRefetchError` states into non-actionable `null`/`Loading…`/`void Promise.all(...).catch(()=>null)` without actionable `Retry` and without cached `isRefetchError` retaining data read-only, and fake late-A test infrastructure that asserted on the global deferred mock rather than on explicit immutable context `{ saveId, contextToken }` invocation counts via `src/testing/setup.ts`/`e2e/tauri-ipc-stub.ts` and `contextKey({ saveId, contextToken })` (analogue `src/features/club-dna/components/club-dna-definition.tsx:67`). Workflow policy (`AGENTS.md` — after two failed corrections, stop and reassess; replan when an invariant/architectural seam changes) requires replanning now. This entry records the failure without claiming uncommitted work completed; Commit 5 (`d9d2737 feat(search): compute tactic lane scores and sort`) remains `Completed` at `HEAD d9d2737` and Commit 6 remains `Active` (uncommitted diff with `src/lib/with-abort.ts` untracked, 15 files) with the revised packet that carries immutable `{ saveId, contextToken }` via reusable `pub(crate) ensure_active_save_context` at `src-tauri/src/features/snapshot/service.rs` (snapshot boundary, two-step distinct `Save {id} not found` vs `Save changed or is no longer active` under command-held DB lock + `unchecked_transaction` before read/default, with `get_tactic` default initialization using single `tx` via `save_tactic_in_tx`/`load_tactic_from_tx` without nesting), reuse `ClubDnaContext` at `src/features/club-dna/types/club-dna.ts`, `clubDnaKeys.definition` at `src/features/club-dna/api/club-dna-keys.ts:5`, with inactive-save / stale-token / unknown-save distinct rejections and numeric ID reuse isolation via `contextToken`-keyed query keys, plus `src/testing/setup.ts` + `e2e/tauri-ipc-stub.ts` exact camelCase `{ saveId, contextToken }` validation and immutable context-keyed delayed A/B test-local `Deferred` per immutable `{ saveId, contextToken }`. The replanned Commit 6 deletes `src/lib/with-abort.ts`, removes the abort wrapper and global pending helpers (`pendingPlannerTacticDeferred` etc., verified via fixed-string `rg -F "withAbortSignal" -g "*.ts" -g "*.tsx"`/`rg -F "with-abort" -g "*.ts" -g "*.tsx"`/`rg -F "pendingPlannerTacticDeferred" -g "*.ts" -g "*.tsx"` zero), makes the two IPC calls carry explicit immutable `{ saveId, contextToken }` with Rust-side reusable validator (no `active_save_id` lookup, no swallowing), transaction ordering avoids nesting `get_tactic` → `save_tactic`, makes fetching component-owned non-throwing (`useQuery` with `enabled` + `isError` -> `Retry` + `isRefetchError` retains data), preserves My Club/Search pending/error/refetch contracts and Define DNA behavior, and adds the exact REDs above (A/B crossing — IPC/context-keyed caches/stale isolation only, visible labels remain Commit 7 — backend context validation/rejections with distinct errors and nesting-free ordering, query-key isolation with `contextToken` numeric ID reuse and zero-arg/sentinel checks, no-snapshot suppression, sort validation, existing-contract retention with `isRefetchError`); header-X synthetic rendering/removal/recompaction and interaction/order proof are fully in Commit 7 (`SearchResultsPanel`/`search.test.tsx` route, not only prop toggle unit) which also explicitly preserves table-scoped tactic `columnIds`/`widths` (widths persist) across immutable context switches while `laneLabels` update for new context. If split was needed for size, statuses/delivery sequence would be updated consistently — kept as one atomic commit 6 here because the immutable context IPC signature and its frontend plumbing must land together.
- Tooling/branch factual corrections (2026-09-03): classifiers are positional at `/home/jonas/projects/PI_SETUP/scripts/ledger_state.py` and `/home/jonas/projects/PI_SETUP/scripts/delivery_state.py` (with `publication_state.py` alongside); Rust crate is `app` (`app_lib`) with no repo-root `Cargo.toml` — run Rust tests via `(cd src-tauri && cargo test <filter>)` or `--manifest-path src-tauri/Cargo.toml`, never `cargo test -p fm-valuescout` or bare `cargo test` at root; feature branch `feat/tactic-table-columns` is active (`HEAD d9d2737`, Commit 5 completed, failed Commit 6 remains uncommitted with 15-file diff and `src/lib/with-abort.ts` untracked), base `main` `406dca7` is synchronized with `origin/main` (verified `git branch -vv` and `git merge-base --is-ancestor`); historical planning-time review branch was `release/v0.15.0` with `main` behind `origin/main` and intent to activate feature branch from synchronized `main` via delivery workflow — corrected throughout validation fields and PR base notes. Fixed-string grep checks now use `rg -F` / `grep -F` and include obsolete abort/global pending helper absence checks (`withAbortSignal`, `with-abort`, `pendingPlannerTacticDeferred`, `setPlannerTacticPending`).
- Developer-authorized fresh planning cycle for Commit 6 (2026-09-03): after the prior replan correction cap (three `workflow-fix` rounds exhausted on implicit `active_save_id` + sentinel/abort approach), developer explicitly authorized a fresh planning cycle for Commit 6 only, on the active ledger draft (not accepted authority). Planner produced one clean, executable Commit 6 packet preserving completed commits 1–5 and Commit 7 sequencing, incorporating all established facts and resolving final review blockers: one transaction owner per Rust command under `Db` lock with snapshot-owned `ensure_active_save_context(&Transaction, save_id, context_token)` two-step distinct errors, planner `load_or_initialize_tactic_in_tx`/`save_tactic_in_tx` with validation-before-write and no nesting and trusted `Connection` wrappers committing their own one transaction, both read/options/save IPC signatures carrying immutable `{saveId, contextToken}` exact camelCase end-to-end (including `PlannerTacticEditor` mutation/cache with `mutationKey: ["planner","tactic", context]` and stale guard and keyed remount, `src/testing/setup.ts`, `planner-ipc-mock.ts`, `e2e/tauri-ipc-stub.ts`), query option factories accepting non-null immutable context only with child-boundary gating (no ternary hooks, no casts, no sentinel keys/data, no nullable factories; `skipToken` proven available in `@tanstack/react-query@5.101.4` types `_tsup-dts-rollup.d.ts:133`/`683`/`647` but child boundary preferred), component-owned tactic fetch errors via child mount with `Retry` refetching both context-scoped keys and cached `isRefetchError` retaining data blocking writes (including options-only recovery and mutation stale guard for delayed A→B and same-ID token rotation), search `currentSnapshotQueryOptions`/`savesQueryOptions` error propagation retained with no swallowed catches and no-snapshot with valid save context keeps tabs/filter bar but child not mounted (no tactic/options/save IPC, `SearchFilterBar` present, no layout mutation, toggle gated error lives in Commit 7), Commit 6 owning context IPC/sort/no-snapshot only and Commit 7 owning synthetic rendering/toggles/header-X/order/width/labels, delayed A/B/numeric ID reuse tests using immutable test-local deferred per-context responses with exact non-vacuous cache/assertion for reads and mutation, complete consumers and validation (`check-app`, `check-rust` or focused suites as justified, `smoke`, full fixed-string obsolete checks for `as unknown as PlannerTactic`/`as any`/`? useQuery`/`["planner","tactic","disabled"]`/`mutationKey`). Sizes, files, steps, tests, review mandates, downstream wording and fingerprints corrected; no implementation is claimed.

## Completed work

| PR | Commit | Git ref | Implementation | Validation | Test portfolio | Review | Fix rounds | Deviations |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PR 1 — Tactic columns for player tables (Search, Moneyball, Shortlist) | Commit 1 — Record the approved feature plan | 944551c774cb30c0d7f2bcb42f232572680e870d | Recorded the reviewed schema 2 ledger and activated the feature in TODO without changing executable behavior. | `ledger_state.py`; `delivery_state.py`; `git diff --check 406dca7..944551c` — passed. | Not applicable | Clear | 0 | None |
| PR 1 — Tactic columns for player tables (Search, Moneyball, Shortlist) | Commit 2 — Add neutral tactic-identity helpers and ordering | d8be592b873076aa8611d569ecd8badb38a4e021 | Added the closed 11-lane synthetic ID contract, Planner re-export, and deterministic straight/interleaved tactic ordering. | Focused Vitest: 11 passed; `./scripts/dev check-app`; boundary import search; `git diff --cached --check` — passed. | Pass | Clear | 1 | Corrected constructors and order builders to reject unknown runtime lane IDs. |
| PR 1 — Tactic columns for player tables (Search, Moneyball, Shortlist) | Commit 3 — Persist synthetic tactic layouts with an atomic replace action | 83bdccd0db2b4707a416a1e78fc36b0088cb7493 | Retained canonical tactic IDs on the three Search layouts and added one atomic, validated layout replacement with width pruning and default fallback. | Store Vitest: 27 passed; tactic helpers: 11 passed; `./scripts/dev check-app`; boundary import search; `git diff --cached --check` — passed. | Pass | Clear | 1 | Unified all store mutators on one table-specific allowlist and added single-notification proof. |
| PR 1 — Tactic columns for player tables (Search, Moneyball, Shortlist) | Commit 4 — Extend Rust resolver for synthetic tactic metric fields | d83a06250244829c08cb2c66c9b344084e2dd167 | Added closed Search-only tactic metric parsing with integer/NULL placeholder semantics while preserving generic Squad and filter rejection. | Resolver: 18 passed; Squad: 33 passed; filter: 59 passed; `./scripts/dev check-rust`: 737 passed, 2 ignored; `git diff --cached --check` — passed. | Pass | Clear | 1 | Split Search-specific parsing from generic parsing and consolidated exact boundary tests. |
| PR 1 — Tactic columns for player tables (Search, Moneyball, Shortlist) | Commit 5 — Implement tactic lane scoring and query sort (Rust, Moneyball mapping) | d9d27377fcc47e42b1b4e34bb2fc7c57e890b8bd | Added Planner-equivalent tactic scoring for General/Shortlist SQL and Moneyball cohort scoring, deterministic compound mapping, and null-last stable sorting. | Query: 115 passed; fit: 12 passed; catalog: 6 passed; `./scripts/dev check-rust`: 745 passed, 2 ignored; frontend Vitest: 740 passed; `git diff --cached --check` — passed. | Pass | Clear | 1 | Consolidated Moneyball requested/sort work on exact field IDs after review found lane-only keying; expanded focused parity, completeness, Shortlist, and comparison-pool proofs. |

## Final validation

- `./scripts/dev check-app` (Biome format/lint + `tsc -b` + full-tree secretlint) — frontend gate; must pass for every frontend commit and at close-out. Verifies `biome.json` `noRestrictedImports` boundaries.
- `./scripts/dev check-rust` (`cargo fmt --check` + `cargo clippy -- -D warnings` + `(cd src-tauri && cargo test)`) — Rust gate; required for commits 4–7 and at close-out. The `cargo test` invocation is `cd src-tauri && cargo test` or `cargo test --manifest-path src-tauri/Cargo.toml`, never `cargo test -p fm-valuescout` or bare `cargo test` at repo root.
- `./scripts/dev check` — full commit gate before each PR publication (runs `check-app` + `check-rust` + secretlint).
- `./scripts/dev test` — targeted `src/utils/tactic-ids.test.ts`, `src/features/search/utils/tactic-columns.test.ts`, `src/stores/use-player-table-store.test.ts`, `src/app/routes/search.test.tsx`, `src/features/search/components/search-results-panel.test.tsx`, plus `src/features/search/utils/dynamic-columns.test.ts` for sort validation; full suite at close-out.
- `./scripts/dev smoke` — Playwright product smoke with IPC stub (used because `e2e/tauri-ipc-stub.ts` is changed; requires `pnpm exec playwright install chromium` once, without weakening required `check` gates; runs after `check-app`/`check-rust` and targeted `test`).
- `(cd src-tauri && cargo test resolver)` + `(cd src-tauri && cargo test query)` + `(cd src-tauri && cargo test fit)` for commits 4–5; all `(cd src-tauri && cargo test)` at close-out.
- Classifiers: `python3 /home/jonas/projects/PI_SETUP/scripts/ledger_state.py .wiki/features/active/tactic-table-columns.md` then `python3 /home/jonas/projects/PI_SETUP/scripts/delivery_state.py .wiki/features/active/tactic-table-columns.md /home/jonas/projects/fm-valuescout` — record the returned `delivery_fingerprint` under Delivery authorization and rerun both after any change.
- Moneyball mapping proof: at least one Moneyball-mapped tactic lane returns numeric in General vs Moneyball view and one of the 8 uncovered combos returns `NULL`/`"—"` (non-skippable, commit 5).
- Route/sort proof: tactic header sort toggles with `NULLS LAST` both directions, valid tactic sort restores from URL only when present in the persisted layout, invalid/removed/wrong-table tactic sort falls back to `defaultSearchSort(view)`, and header-X on one tactic while both active leaves the survivor straight at far-right without gaps.
- No-snapshot/save-switch proof: active immutable save context present + snapshot null/mismatch (`saves=[{id:1, contextToken:"tokenA", isActive:true}]` valid, `snapshot==null` or `snapshot.saveId !== context.saveId`) -> `TacticContextBoundary` not mounted, zero `get_planner_tactic`/`get_planner_tactic_options`/`save_planner_tactic` IPC, filter strip + tabs remain present; gated toggle click (Commit 7) shows `"No snapshot loaded — use Load Data"` without mutating layout; switching immutable context `{ saveId, contextToken }` (`contextA {1,"tokenA"} -> contextB {2,"tokenB"}` and `contextA {1,"tokenA"} -> {1,"tokenA2"}` numeric ID reuse) changes `plannerKeys.tactic(context)` / `tacticOptions(context)` (analogue `clubDnaKeys.definition`) with distinct keys and does not leak prior save's tactic; `laneLabels` update for new context (save role selection + shared `TacticOptions` catalog `displayName`) while table-scoped `columnIds`/`widths` are preserved across save switches (commit 7 asserts) via fixed-string `rg -F "contextToken"` checks and `queryClient.getQueryData(plannerKeys.tactic(context))` assertions.
- Manual smoke (keyboard): with a fixture snapshot + a configured Planner tactic, toggle `Add Tactic (Current)` in Search General, Moneyball, and Shortlist — verify 11 columns appear at far-right in tactic order with header `Position (Role) / Position (Role)`, that `Potential` appends correctly, both interleaves as `GK Cur, GK Pot...`, that header X deactivates its group's button and re-compacts the survivor straight at far-right, that invalid tactic shows an inline error and mutates no layout, that widths persist after reload, and that sorting a tactic column keeps Nulls-last (`"—"` rows last) in both asc and desc.

## Documentation impact

Complete during reconciliation. Expected: no `ARCHITECTURE.md` or `DESIGN.md` structural change until implementation lands. At close-out, `ARCHITECTURE.md § Search` notes tactic synthetic columns, the neutral `src/utils/tactic-ids.ts` ownership, save-scoped tactic loading, deterministic `(attribute_role_id, base_position)` Moneyball mapping, and Rust scorer; `DESIGN.md` notes the two toggle buttons and header format if one is added. The Moneyball mapping contract's durable surface is `src-tauri/src/features/player_metrics/resolver.rs` / `query.rs` inline comment plus this ledger's Invariants; no standalone docs-only commit is created and no catalog counts are copied into `src/utils/tactic-ids.ts`. No ADR is required for this display plumbing; rationale stays in the ledger.
