# Lockfiles can contain high-entropy strings that resemble secrets.
pnpm-lock.yaml
src-tauri/Cargo.lock

# C# build output under bridge/ (also gitignored; listed so scans stay quiet if present)
bridge/**/bin/**
bridge/**/obj/**
